# Changelog

- Centralized API, platform, Studio, and workflow RBAC/ABAC/SoD enforcement; added a fail-closed digest-addressed inventory for all API authorization contracts and redacted versioned policy-decision records.

- Added signed, bounded, context-bound keyset cursors with stable duplicate tie-breaking and an opt-in local evidence API path that preserves offset compatibility and fails explicitly where native PostgreSQL keyset execution is not yet available.

- Added atomic backend-neutral request idempotency with tenant/operation scoping, request conflict detection, in-progress ownership, expiry/rebind, bounded response replay, hashed owner capabilities, SQLite backup/restore retention, and forced-RLS PostgreSQL parity.

- Added a backend-neutral immutable object-store contract with a fail-closed offline local adapter, hardened S3 conditional writes and bounded verification, real MinIO retention/tamper tests, evidence-registry integration, and locked boto3 server support.

- Added backend-aware operational diagnostics: SQLite remains `local_only`, PostgreSQL is reported accurately, tenant-scoped PostgreSQL job/error records match SQLite shapes, audit health uses the verified chain, and Alembic migration status is read without exposing connection settings to Application code.

- Added a PostgreSQL adapter for the workspace/initial-period unit of work with forced tenant RLS, atomic workspace-period-audit persistence, concurrency-safe audit chaining, SQLite semantic parity, fail-closed rollback, immutable audit evidence, and Alembic downgrade/re-upgrade coverage.

- Added a PostgreSQL durable-job adapter and Alembic migration with tenant RLS, atomic concurrent idempotency, optimistic transitions, fenced worker leases, append-only partition effects, live crash/resume parity with SQLite, and verified downgrade/re-upgrade behavior.

- Added atomic, immutable partition effects bound to durable-job checkpoints and final completion. A two-partition workload now proves connection-close/restart takeover skips the committed partition, produces the same effect semantics and manifest identity as uninterrupted execution, and rolls effect plus progress back together on injected failure. External side effects and host loss remain outside this local proof.

- Added generation-fenced, expiring durable-job worker leases with atomic claim/takeover, heartbeat extension, stale-worker rejection, lease release evidence, retry reclaim, and connection-close/reopen checkpoint recovery. This is local SQLite process-restart proof, not PostgreSQL parity, host-loss durability, or exactly-once transport.

- Added a schema-v1 durable-job aggregate and SQLite migration with seven fail-closed states, tenant-scoped idempotency, optimistic state versions, monotonic digest-addressed checkpoints, retry ceilings, safe error codes, completion manifests, immutable transition evidence, and backup/restore/export coverage. Generic worker leases, PostgreSQL parity, and real crash recovery remain open.

- Extracted operational health, job, and sanitized-error composition into a backend-neutral Application service with explicit repository and migration-status ports. The existing connection-based `OperationsService` remains a compatibility adapter over SQLite; no PostgreSQL parity or production-operability claim is made.

- Added the first Phase 1 backend-neutral application boundary: a typed unit-of-work port and SQLite adapter atomically create a workspace, initial period, and two chained audit events, with pre-transaction validation and fail-closed rollback. Existing direct repository autocommit remains compatible; broader service migration and PostgreSQL parity remain open.

- Added a bounded exact-text FI-008 business-record CSV/JSON reader with stable parsed-byte provenance, strict ambiguity/shape/resource controls, and compatibility routing for platform matching, finance/control imports, ledger, inventory, and receivables CLI inputs. This is a parser boundary, not a semantic-validation, source-authentication, malware, authorization, or scale claim.
- Added a bounded FI-007 Studio generated-artifact CSV/JSON reader with stable pre/post fingerprints, strict resource/header/ambiguity controls, safe empty-state rendering, preserved pandas display typing, and optional companion shape/count checks. Companion consistency is not a signature, authenticated provenance, malware scan, disclosure authorization, or throughput claim.

- Bounded the remaining FI-009 legacy database JSON imports through database-legacy-import-json-ingress-v1 with fixed byte/node/depth/collection/scalar limits, stable regular non-reparse reads, duplicate/non-finite rejection, explicit list/envelope/keyed-map contracts, ambiguity refusal, and parsed-byte digest/size/profile evidence before database inspection or mutation. Two published schemas retain valid legacy shapes; arbitrary objects no longer silently import zero records. Authentication, centralized authorization, encryption, malware scanning, full record semantics, and real DR remain open.

- Bounded FI-009 local database restore `manifest.json` and `backup.json` through a named duplicate-safe structured profile with 64 MiB/file, node/depth/collection/scalar ceilings, reparse and concurrent-change refusal, closed manifest/backup contracts, checksum/byte/schema agreement, registered table names, and published current-writer schemas before restore allocation or target mutation. The current implementation accepts no archive; the subsequent legacy-import slice closes the adjacent parser gap, while encryption/authenticated provenance, centralized authorization, malware scanning, and real DR/host-loss exercises remain open.

- Added explicit integrity-checked recovery for interrupted existing-output client-pack publication. A path-minimized schema-v1 local marker binds exact sibling names and bounded old/staged tree digests; `reconforge report client-pack-recover` accepts four closed states and refuses tampering, unknown siblings, reparse points, or ambiguity. This is simulated local recovery with unkeyed hashes—not authentication, crash-atomic replacement, real host/filesystem-loss durability, provenance, malware scanning, or disclosure approval.

## v0.7.1 — Evidence-Bounded Phase 0 Foundation

- **Breaking (direct Python API):** all financial-input policy defaults now use `strict-financial-input-v2`. Binary Python/NumPy floats fail before financial conversion; use exact text, `Decimal`, integers, or minor units. `legacy-financial-input-v1` remains only as an explicit compatibility choice and for versioned historical readers. Default direct artifacts now use their current strict schemas/policies. See `docs/financial-input-v2-migration.md` and ADR 0097.
- Bounded FI-012 client-pack redistribution to 10,000 selected regular non-symlink files, 20,000 traversed entries, 64 MiB per file, and 512 MiB aggregate. Generic text redaction is strict-UTF-8 and line-streamed; non-redacted bytes use bounded chunked temporary copies; late-created files are excluded from the frozen fingerprint set. Complete packs now build in a sibling staging directory, preserve/restore prior output on handled failure, and remove stale content only after successful publication. Existing-directory replacement on Windows is a two-rename rollback protocol rather than observer/crash atomicity; authenticated provenance, malware quarantine, disclosure authorization, and redaction completeness remain open.
- Routed FI-012 client-pack CSV redaction through `tabular-file-ingress-v1` before fingerprint hashing/output preparation, added duplicate-header rejection, and changed row accumulation/direct writes to exact-field streaming through a temporary file with per-file atomic replace. Empty, malformed-shape, invalid-UTF-8, type-mismatched, oversized-field, and oversized CSV now fail generically without clearing an existing destination. The later bounded-copy/staging slice closes unbounded text/non-redacted copying and handled-failure pack rollback; crash-atomic replacement, authenticated provenance, malware quarantine, uploads, and disclosure authorization remain open.
- Routed FI-012 client-pack manifest, evidence-index verification, and JSON redaction through the bounded structured-document policy. Strict redaction preserves exact fractional/exponent lexemes and legacy redaction preserves historical finite-float behavior; malformed, ambiguous, invalid-UTF-8, deep, or oversized JSON now fails with a generic error before ordinary output preparation instead of falling back to unrestricted text redaction. Text/non-redacted copying, full output transactionality, authenticated provenance, malware quarantine, uploads, and redistribution authorization remain open after the subsequent CSV slice.
- Routed FI-011 close checklist JSON and JSON/YAML templates through the versioned bounded structured-document boundary while preserving valid task normalization, the legacy title alias, and existing generic CLI parse errors. Duplicate/deep/oversized/non-finite JSON and duplicate/deep/alias-heavy/unsafe/multi-document YAML now fail before task normalization; template authenticity, semantic correctness, generated/report/restore/Studio readers, malware quarantine, uploads, and connector safety remain open.
- Added versioned bounded JSON/YAML parsing with byte, node, depth, collection, scalar, and alias ceilings; duplicate-key, non-finite, merge, cycle, unsafe-tag, and multiple-document rejection; stable non-sensitive errors; strict financial lexeme preservation; and an exact direct-PyYAML AST inventory. Configuration, mapping, control-pack, and Reconciliation-as-Code YAML now use this boundary. Generated/restore/Studio/report JSON/CSV, legacy XLS internals, malware quarantine, upload, authenticity, and connector safety remain open.
- Added universal `uv.lock` resolution for runtime, server, dev, docs, and DuckDB profiles; exact uv/cutoff/archive identity; locked normal/server/security/candidate CI paths; checksum-pinned container installation; weekly pip/npm/Docker/Actions updates; checksum-verified Gitleaks history/tree scans; and closed audit/exception policy with pre-registry release gates. Identified hosted Python 3.11/3.12, Docker, policy, and secret gates pass. The npm lock now records HTTPS resolution and SRI for all 209 non-root registry entries, with zero declared integrity gaps.
- Replaced the floating, runner-environment SBOM workflow with release-integrated exact-subject CycloneDX 1.7 generation for source, wheel, sdist, and digest-addressed image candidates. The source document verifies Python package-metadata parity and inventories matching npm-lock versions; the image path checksum/commit/platform-pins Syft 1.49.0. A closed manifest, deterministic checksums, four separate keyless SBOM attestations, strict predicate/identity verification, and negative contracts fail closed before non-publishing upload. Clean-HEAD source/package outputs are deterministic and official-schema valid, but the local image inventory is synthetic because Docker is unavailable; completeness, hosted signing, vulnerability/license assurance, publication, and SLSA levels remain unverified.
- Added a tag-only, non-publishing signed-release-candidate workflow definition: it requires a clean GitHub-verified annotated version tag on `main`, hash-locks the Python release toolchain, pins all actions by commit, records commit-time `SOURCE_DATE_EPOCH`, fail-closed normalizes sdist metadata, creates a closed source/wheel/sdist/image manifest, requests GitHub keyless SLSA provenance, and verifies exact repository/workflow/ref/revision/runner expectations from retained bundles before candidate upload. Two same-machine Python 3.14.6 builds reproduced identical source/wheel/normalized-sdist bytes; this does not prove a hosted run, supported-version/cross-platform or OCI reproducibility, signature, attestation, registry result, immutable release, or SLSA level. Final publication stays human-gated.
- Added a closed, Approved-SLSA-1.2-pinned provenance implementation plan for source, wheel, sdist, OCI image, and CycloneDX SBOM identities; seven trust boundaries; in-toto Statement v1/SLSA provenance v1; strict verification/failure and evidence-preserving rollback; and 12 testable gates. Both Build and Source tracks remain UNEVALUATED; exact-subject SBOM implementation makes eight gates partial and four planned, with no hosted signature, attestation, trusted-builder, publication, or level claim.
- Added a closed, final-source-pinned NIST SSDF 1.1 mapping for all four groups, 19 practices, and 42 tasks. The first review records 28 partial and 14 planned tasks with owners, evidence/tests where positive, gaps, actions, and 90-day review cadence; draft SSDF 1.2 is excluded and final SP 800-218A remains a separate AI-profile assessment. This is not SSDF conformance, compliance, certification, or operating-process assurance.
- Added a closed, source-pinned OWASP ASVS 5.0.0 mapping across all 17 chapters. It assesses 55 selected requirements as four implemented, 32 partial, 11 planned, and eight not applicable while leaving 290 explicitly unassessed; this is scoped repository evidence and gap governance, not an ASVS level, full assessment, compliance, certification, penetration test, or deployed-security claim.
- Added a closed schema-v1 threat-model index with exact coverage of all nine active runtime modules. Each module now links classified assets, scoped actors, trust boundaries, shared threats, architecture controls, existing tests, ownership, residual risks where exact, assumptions, limitations, and exclusions; all statuses remain evidence-bounded and do not assert deployed security, compliance, certification, or production readiness.
- Added Security Architecture v2 as a closed schema-validated YAML registry with conservative deployment-mode labels, data classes, trust boundaries, evidence-linked control ownership, and normalized residual-risk parity. The readable security architecture, API, and data-handling docs now follow that source; this is repository governance evidence, not proof of deployed control effectiveness, compliance, certification, or production readiness.
- Made current evidence-binder CSV/risk ingress exact and verifiable: CLI/demo reads recognized CSV fields as source text, preserves fractional/malformed scores as visible data-quality cases, and writes schema-v3 indexes with strict policy, selected-source fingerprints, path-independent content digest, and full artifact digest. Direct Python generation retains schema-v2 legacy compatibility; hashes remain local consistency checks rather than signatures or audit opinions.
- Made current client-pack amount redaction exact and verifiable: CLI/demo JSON preserves decimal lexemes before bucketing, fixes double-redaction of valid buckets, omits the local source path, and writes schema-v2 manifests with strict policy, required source/output fingerprints, stable content digest, and full artifact digest. Direct Python generation and unversioned manifests remain legacy compatibility paths; redaction stays best-effort and requires review.
- Made current review and period-comparison ingress exact without breaking direct Python compatibility: CLI/demo paths select strict v2 before pandas inference, malformed/missing amounts stay distinct from valid zero, review workbooks record the policy without moving their first sheet, and current period JSON advances to schema v2 with input-byte provenance plus path-independent decision and complete artifact digests. Historical unversioned JSON remains an explicitly unverified v1 reader; local hashes are not signatures or source-system attestation.
- Made current declarative-rule ingress exact and provenance-bearing: the CLI, demo, explanation, and control-matrix paths select strict v2 through the shared safe YAML lexeme reader; current rule-result JSON advances to schema v2 with pack/input/decision/artifact digests, while direct Python defaults and the unversioned writer/reader remain explicit legacy compatibility paths. These local digests are not signatures or proof of source authenticity.
- Made current YAML configuration ingress strict without breaking historical unquoted decimals: a SafeLoader-derived reader preserves decimal scalar lexemes as text, CLI/benchmark/Studio select strict v2 explicitly, default configs quote the amount tolerance, and direct Python loading retains warning legacy-v1 compatibility.
- Advanced the exact synthetic-generator manifest to schema v2 with strict/legacy financial-input policy in both digests. The CLI selects strict v2, direct Python calls retain warning legacy compatibility, and schema-v1 manifests remain implicit-legacy readers; generator arithmetic and equivalent-input CSV bytes are unchanged.
- Added versioned financial input policies: explicit legacy v1 warns on accepted finite binary floating-point compatibility inputs, while current strict v2 rejects Python/NumPy floating scalars before conversion. Official dataset coercion, reconciliation tolerance reads, and signature-v2 parsing now use strict readers; PyYAML configuration compatibility explicitly remains legacy v1.
- Added versioned reconciliation signatures: current `reconciliation-signature-v2` embeds its policy, rejects binary floating-point/boolean values in financial result columns before null handling, and canonicalizes finite exact amounts. Both engines expose the policy beside the digest; explicit v1 preserves a golden historical digest for compatibility replay.
- Replaced DuckDB work-order partition sizing float division/ceiling with exact integer ceiling division. A reproduced large-count boundary now selects eight buckets instead of seven without changing matching rules. Added a persistent classification ledger for all 75 remaining `float` source lines; after the separate v2 signature slice, P0-005 remains open for legacy financial compatibility readers.
- Made governed local metric `value_text` canonical: count-derived percentages now use exact local-context Decimal arithmetic, count text remains integral, period-readiness averaging is Decimal, and invalid/non-finite stored readiness fails closed. The existing `value REAL` column remains a compatibility projection; SQLite aging aggregates remain bounded approximations.
- Removed Python parsing of SQLite defaults during backup restore. Missing defaulted/nullable columns are now omitted from each parameterized insert so the trusted local schema evaluates its own exact text, timestamp, and expression defaults; missing required non-default columns still fail closed.
- Replaced evidence-binder risk-score float coercion/truncation with versioned exact integer 0–100 validation and explicit missing/invalid status. Malformed scores now produce visible data-quality evidence with `null`, not a manufactured zero. Local/PostgreSQL close readiness now uses exact two-decimal Decimal calculation, and PostgreSQL approval/locking fails closed unless readiness equals `100.00` exactly.
- Replaced synthetic monetary `random.uniform`/fixed-two-decimal generation with versioned `exact-decimal-v1` integer-step Decimal draws, exact CLI rate text, registry-based currency precision, explicit currency columns, LF-stable CSV bytes, and a schema-v1 digested/checksummed manifest. Invalid policy fails before output creation; the eight-CSV Python return contract remains unchanged.
- Preserved server-rendered Studio minimum-amount query text through exact Decimal validation and comparison, added bounded constant HTTP 400 handling, and removed the two-decimal browser-step assumption. Matched stock/GL rows now retain currency. Management-pack sums are ambient-context independent, use the captured registry precision/policy, publish currency-policy provenance, expose unquantified counts, and fail before output creation on cross-currency aggregation without explicit FX.
- Added Alembic revision `0009_postgres_reconciliation_results`, tenant-scoped canonical reconciliation input manifests, exact Decimal source/result amounts, deterministic result and exception IDs, explicit input-use limits, completion coverage checks, manifest/result hashes, immutable completed-run guards, atomic completion audit/outbox evidence, contract tests, and optional live RLS isolation coverage. This persists matcher output but does not claim PostgreSQL matcher execution.
- Added authenticated PostgreSQL server-mode reconciliation read routes for paginated tenant-scoped run metadata, canonical inputs, deterministic results, and typed exceptions, with explicit read permissions and no SQLite fallback. Run submission and matcher execution were initially outside this boundary.
- Added an authenticated `POST /api/v1/reconciliations/runs` boundary that atomically registers a bounded canonical left/right input manifest with idempotency and explicit manage permissions. Production scheduling and large-dataset execution remain separate open capabilities.
- Added Alembic revision `0010_postgres_reconciliation_execution`, durable reconciliation execution claims/leases/progress/cancellation/retry state, authenticated cancellation/requeue routes, and a fresh-connection worker that atomically persists output from the explicit `LocalDeterministicMatcherAdapter`. The adapter uses a schema-only in-memory SQLite dependency and performs no hosted financial writes; production scheduling and large-dataset hosted execution remain open.
- Added explicitly governed hard-key partitioned matching with bounded per-partition graph size, progress/cancellation checks, deterministic partition output, tenant-scoped PostgreSQL-cursor partition input streaming, tenant-scoped atomic partition checkpoints with retry resume, and an in-process multi-worker scheduler that relies on PostgreSQL leases. DuckDB also exposes a bounded CSV relation-batch iterator, and the CLI has a lazy `--streaming` synthetic benchmark mode. Streamed global assignment remains open; 10k/100k evidence passed, both materialized and lazy 1m attempts timed out before writing reports, and no million-row claim is made.
- Added Alembic revision `0008_postgres_evidence_registry`, tenant-scoped PostgreSQL evidence metadata, immutable artifact-reference guards, evidence links/requirements/coverage, provider-checksum verification state, atomic audit/outbox evidence, authenticated server metadata routes, contract tests, and optional live RLS isolation coverage. Artifact bytes, scanning, authorized downloads, and restore remain explicit worker/deployment responsibilities.
- Wired the authenticated PostgreSQL server profile into the bounded Finance Core API: tenant-scoped account reads/writes, exact balanced immutable posted-entry creation/reads, deterministic server identifiers, and explicit `501` responses for unsupported chart/dimension/journal/draft-validation/void capabilities. Local SQLite Finance Core behavior remains unchanged.
- Added exact organization/fiscal-period trial-balance aggregation to the authenticated PostgreSQL Finance Core profile. Posted ledger lines are aggregated with currency-precision checks and deterministic account ordering; legal-entity-scoped trial balance remains explicitly unsupported until an entity dimension exists.
- Routed authenticated server-mode audit listing and verification to the tenant-scoped PostgreSQL hash chain, with deterministic event ordering, metadata preservation, event-hash recomputation, and previous-link verification. The server API no longer reads the legacy SQLite audit ledger when PostgreSQL identity is enabled.
- Added Alembic revision `0006_postgres_close` and a bounded PostgreSQL close-control API for tenant-scoped close periods, deterministic starter tasks, readiness, dependency-aware task completion, approval/locking transitions, reasoned reopen, forced RLS, and atomic audit/outbox evidence. Close-control locks do not lock source-ERP postings.
- Added Alembic revision `0007_postgres_outbox_delivery`, tenant-scoped PostgreSQL outbox lease ownership, deterministic `SKIP LOCKED` claims, publisher acknowledgements, retry/backoff, dead-letter/replay transitions, and a fresh-connection PostgreSQL worker adapter. External transport and managed scheduling remain explicit deployment dependencies.
- Wired the authenticated PostgreSQL server profile into the bounded master-data API: tenant-scoped summary, snapshot, currency, organization, legal-entity, and branch reads/writes, atomic mutation audit/outbox evidence, and deterministic identifiers. Local SQLite master-data behavior remains unchanged.
- Added Alembic revision `0005_postgres_fiscal_periods` and routed server fiscal-period list/create/status operations through the tenant-scoped PostgreSQL master-data boundary with overlap checks, metadata-only status transitions, deterministic identifiers, and atomic audit/outbox evidence. Local SQLite period behavior remains unchanged.
- Added migration 20 and a bounded Accounts Receivable foundation: customer credit profiles, exact minor-unit invoices, approval-time credit-hold/limit controls, permissioned override reasons, posted receipts, accumulating allocations, deterministic credit exposure and aging, RBAC/SoD, authenticated API and CLI surfaces, atomic audit/outbox events, and backup/restore coverage. This does not implement statutory AR posting, tax, credit notes, dunning, payment execution, or ERP writeback.
- Added an optional PostgreSQL server boundary: lazy psycopg loading, TLS and statement-timeout defaults, parameterized transaction-local tenant context, forced RLS foundation schema, unit contracts, and an opt-in live cross-tenant isolation test. Existing domain persistence remains SQLite-first; this does not claim hosted PostgreSQL readiness.
- Added an optional Redis server boundary: lazy redis-py loading, TLS-by-default configuration, tenant-scoped session/revocation metadata, atomic Lua rate-limit counters, expiring compare-and-delete locks, unit contracts, and an opt-in live key-isolation test. The default API remains SQLite/in-process and uses Redis only when configured for login rate limits; the explicit server profile uses PostgreSQL identity, so this does not claim multi-worker server readiness.
- Added an optional S3-compatible object-storage boundary: lazy boto3 loading, TLS-by-default endpoints, tenant-separated keys, SHA-256 verification, optional SSE/object-lock retention, bounded presigned GETs, deletion safeguards, unit contracts, and an opt-in live key-isolation test. Local evidence writers remain filesystem-backed; this does not claim hosted artifact-pipeline readiness.
- Added migration 19 and explicit object-backed evidence registration/verification: tenant-scoped storage references, content-addressed default keys, provider version/checksum metadata, retention metadata, CLI `evidence register --storage-backend s3` configuration, upload-failure isolation, and backup/restore metadata coverage. Local storage remains the default; workers, scanning, authorized downloads, and restore/replication are still open.
- Added a bounded transactional-outbox worker runtime with fresh per-cycle database connections, graceful stop signaling, explicit publisher injection, retry/dead-letter reuse, and lifecycle tests. External transport and managed worker deployment remain out of scope.
- Added a GitHub Actions server-boundary job with PostgreSQL and Redis service containers, a non-privileged PostgreSQL role, server-extra installation, and live tenant/key-isolation tests; hosted readiness is not inferred from workflow configuration alone.
- Added a credential-free Alembic PostgreSQL migration path for the tenant/RLS foundation and a CI `alembic upgrade head` smoke step; existing SQLite domain migrations remain separate and server persistence is not yet claimed.
- Added Alembic revision `0002_postgres_master_data` and a real tenant-scoped PostgreSQL repository for currencies, organizations, legal entities, and branches, with composite foreign keys, forced RLS, deterministic reads, caller-owned transactions, unit contracts, and an opt-in live isolation test. Existing API/domain services remain SQLite-first.
- Added Alembic revision `0003_postgres_ledger` and a bounded PostgreSQL ledger-control repository with exact `NUMERIC(38,18)` amounts, balanced-entry validation, deferred balance triggers, immutable posted records, tenant-scoped audit hash chaining, transactional outbox events, and live non-privileged RLS coverage. The explicit server profile now routes the bounded Finance Core account and posted-entry API operations to this repository; local SQLite routes remain unchanged.
- Added Alembic revision `0004_postgres_identity` and a tenant-scoped PostgreSQL identity repository with PBKDF2 password verification, RBAC role/permission assignments, hashed bearer sessions, throttled activity updates, failed-login lockout, revocation, forced RLS, and live non-privileged tenant-isolation coverage; the explicit server API-auth profile now consumes this boundary.
- Added an explicit PostgreSQL server API-auth profile with required tenant-isolated legacy storage, tenant-header enforcement, middleware bearer validation, RBAC permission snapshots, hashed-session login/logout, revocation, principal propagation into domain authorization/audit calls, and live cross-tenant API tests. Enterprise federation/MFA and full PostgreSQL domain persistence remain open.
- Added migration 15 and a bounded Accounts Payable three-way-match foundation: supplier master, purchase orders, posted goods receipts, supplier invoices, deterministic quantity/price/total variance results, exception-queue routing, idempotency, optimistic row versions, RBAC/SoD checks, transactional audit/outbox events, versioned API routes, and backup/restore coverage. This does not implement statutory AP posting, tax/withholding, payment execution, or ERP writeback.
- Added an explicit trusted-local execution context: API requests disable unbound `local-cli` actor compatibility, while local CLI/service callers retain the documented local-only mode. Added actor-context and API middleware regression tests.
- Added migration 16 for canonical Decimal text/currency columns in account reconciliation, strict invalid-amount handling, atomic workflow/audit/outbox finalization, caller-owned workflow transactions, and Decimal-preserving backup/restore tests.
- Migrated inventory planning/count, FIFO valuation, and valuation reversal mutations to append audit/outbox evidence before transaction commit, with forced audit/outbox failure rollback tests.
- Added migration 17 for canonical Decimal text in journal and intercompany amounts, strict invalid-value import behavior, precision regression tests, and atomic journal/intercompany outbox events.
- Added migration 18 for canonical Decimal text in persisted matching differences, exact tolerance handling, and backup/restore coverage for the new field.
- Removed remaining float arithmetic from stock-to-GL assignment penalties, reconciliation risk magnitude scoring, and declarative control-rule monetary comparisons; invalid rule amount impact is now explicit rather than zero-filled.
- Hardened reporting and review fingerprints/redaction thresholds with Decimal parsing, including invalid-value preservation and Decimal rounding for period comparison identity.
- Migrated variance amount/percentage CLI thresholds and summary JSON/CSV numeric ingress to exact Decimal boundaries; variance report schema v2 retains legacy numeric threshold fields, adds a canonical digested policy plus a v1/v2 compatibility reader, and keeps display rounding separate from exact decisions.
- Replaced float-based per-column anonymizer amount noise with versioned exact global Decimal noise and source-scale rounding. Shareable outputs now omit the reversible original-to-mask table, require a fresh/empty output directory, and include a digested schema-v1 manifest; the unchanged private CSV format is available only through an explicit path outside input/output directories.

- Hardened P0 reconciliation correctness: invalid/missing dates now create data-quality evidence instead of zero-day differences; strict monetary helpers reject null values; stock-to-GL exposes accounting invariants; and platform matching persists unmatched right-side records.
- Added deterministic candidate unioning across reference, exact-field, and amount indexes, optional match idempotency keys, stable result ordering, and explicit right-side unmatched explanations.
- Made audit appends transaction-aware and moved audit-before-commit behavior into matching, journal import, Finance Core lifecycle, and Inventory Core lifecycle mutations. Added a local transactional outbox event and backup/restore coverage for match and journal completion events.
- Added regression coverage for invalid dates and amounts, row/cardinality accounting, right-side orphan visibility, idempotent retries, audit rollback, and existing-transaction audit behavior.
- Promoted the transactional outbox to formal SQLite migration 13, with explicit legacy version-12 upgrade coverage.
- Added opt-in database-per-tenant API routing with strict tenant-ID/path validation, required tenant headers, and cross-tenant session isolation tests; this does not claim PostgreSQL/RLS support.
- Added outbox delivery migration 14 and a local publisher service with leases, deterministic retry/backoff, dead-letter handling, explicit replay, and injected transport boundaries.
- Extended audit-before-commit transaction handling to close-period/task mutations and added rollback regression coverage for close initialization, evidence registration, and other migrated local services.
- Replaced baseline engineering, architecture, roadmap, and risk-register documents with evidence-backed capability boundaries and added ADRs for atomic audit/outbox and reconciliation record accounting.
- Added an experimental read-only React Studio dashboard, native exception queue, evidence binder, and inventory control center backed by four versioned synthetic-only local contracts, with a deterministic executive decision brief, close-readiness signal, guided control story, control-domain/entity health, runtime validation, responsive/RTL/accessibility behavior, deterministic exception IDs, filtering, FIFO valuation/reversal/layer views, and verified screenshots.
- Added `reconforge demo showcase`, `make showcase`, `make showcase-serve`, and a root `DEMO.md` presenter guide for one cohesive generated enterprise package, strict Studio bundle, production web build, and five-minute local walkthrough.
- Added bounded Python generation for the Studio overview/exception/evidence/inventory bundle. Evidence browser metadata excludes source paths and validates SHA-256 values; inventory quantities and values remain exact decimal strings, and inconsistent valuation/reversal/layer records are rejected.
- Added a typed read-only runtime module registry plus `reconforge modules list`, `show`, and `validate` commands. Registry entries distinguish maturity from capability status and declare dependencies, migrations, permissions, interfaces, contracts, data classification, retention/activation notes, and test evidence.
- Added JSON Schemas and ADRs for the modern Studio contracts and runtime module registry.
- Replaced deprecated naive `datetime.utcnow()` generation with shared timezone-aware UTC helpers while preserving existing second-precision `Z` timestamp contracts.
- Added SQLite migration 7 and the experimental `platform.master-data` module for governed organizations, legal entities, branches, currency references, and non-overlapping fiscal periods, with RBAC, audit events, authenticated API routes, CLI commands, safe v6 upgrade coverage, and a versioned path-free snapshot schema.
- Preserved migration-7 master data in local JSON backups and made supported pre-v7 backups restore into their source schema before upgrading to the current schema.
- Added migration 8 and the experimental `finance.core` module: governed charts/accounts, dimensions, journals, exact minor-unit balanced control entries, atomic reference rechecks, SoD validation, immutable Validated/Voided records, trial-balance/snapshot contracts, API/CLI surfaces, and schema-aware backup/restore coverage.
- Added migration 9 and the experimental `inventory.core` module: units, item references, warehouse/location hierarchy, lot/serial traceability, exact-quantity Draft/Posted/Voided movements, atomic stock and serial checks, SoD posting, immutable Posted records, derived on-hand and deterministic exception contracts, API/CLI surfaces, and schema-aware backup/restore coverage.
- Added migration 10 to `inventory.core`: immutable physical-count snapshots, exact count results, reasoned submission/approval/cancellation, known-user SoD, stale-balance rejection, generated Draft variance adjustments, exact reorder thresholds and deterministic advice, repository isolation, strict API/CLI/contracts, backup/export recovery, and read-only synthetic Studio views. This does not implement purchasing, valuation, finance posting, or ERP writeback.
- Added migration 11 to `inventory.core`: entity FIFO policies, exact inbound cost evidence, chronological cost layers and immutable consumptions, Draft/Approved/Cancelled lifecycle, known-user SoD, balanced Finance Core Draft generation, movement-void protection, repository isolation, strict API/CLI/contracts, schema-aware backup/restore/export, and synthetic read-only Studio visibility. This does not implement AVCO, landed or manufacturing cost, automatic Finance Core validation, statutory posting, or ERP writeback.
- Added migration 12 to `inventory.core`: exact whole-valuation reversal through a separately Posted mirror movement, immutable FIFO `Restore`/`Remove` effects, dependency-order protection for consumed inbound layers, debit/credit-swapped Finance Core Drafts with copied dimensions, protected movement/finance evidence, separate RBAC/SoD, repository isolation, strict API/CLI/contracts, schema-aware backup/restore/export, and synthetic read-only Studio visibility. This does not implement partial or reversal-of-reversal orchestration, automatic Finance Core validation, statutory posting, or ERP writeback.

## v0.7.0 — Foundation-Stage Local Platform Readiness

ReconForge ERP v0.7.0 is a foundation-stage release for local-first, export-based pilot evaluation. It expands self-hosted/local-capable foundations for finance controls, reconciliation workflows, synthetic demos, support readiness, and conservative launch materials without claiming hosted production service readiness, direct ERP connectors, certification, audit opinions, legal signatures, customer traction, ROI, or platform replacement.

- Added foundations for the local platform backbone: SQLite DB/audit, local users/RBAC, workflow state machine, REST API, Studio auth-required mode, DB import/export, backup bridge, and supporting security/roadmap documentation.
- Added foundations for DB-backed finance workflows: account reconciliations, close management, approvals/certification metadata, evidence registry, journal controls, intercompany, controls testing, matching, unified exceptions, and metrics.
- Added Studio DB pages and local API route foundations for accounts, close, exceptions, and metrics while keeping the workflow local-first and export-based.
- Added local close checklist workflow commands: `reconforge close init`, `reconforge close list`, `reconforge close set-status`, and `reconforge close report`.
- Added lightweight reconciliation certification metadata fields to local review state and review register exports. These fields are workflow metadata only and are not legal sign-off, audit opinions, compliance certifications, or digital signatures.
- Added local variance analysis command: `reconforge analyze variance`.
- Added rule-pack-derived control matrix export command: `reconforge controls matrix`.
- Added read-only Studio pages for local close checklist, variance analysis, control matrix, and evidence coverage.
- Added an export-based Oracle inventory/GL mapping profile without direct connector claims.
- Added `reconforge mappings profile-template` for local generic CSV profile authoring templates.
- Added JSON schema documentation for close checklist, certification metadata, variance report, control matrix, and profile template outputs.
- Expanded management pack KPIs with unresolved high-risk count, recurring exception count when period comparison output exists, close checklist completion when close state exists, evidence coverage when evidence output exists, accepted risk count, and review completion signals.
- Added strategy documents for global capability benchmarking, platform blueprint, feature gaps, differentiation, and the next release roadmap.
- Added documentation for close workflow, variance analysis, control matrix, and reconciliation certification metadata.
- Added focused tests for new CLI commands, malformed inputs, status validation, output files, certification metadata, and HTML/Markdown escaping behavior.
- Added the synthetic enterprise demo package for generated multi-entity platform-foundation walkthroughs using synthetic data only.
- Added pilot/support/buyer readiness docs: support playbook, pilot onboarding checklist, buyer FAQ, draft implementation packages, release readiness checklist, and security questionnaire updates.
- Added conservative website and launch copy drafts with a claim-boundary guide for local-first/export-based positioning.
- Added release readiness and deployment smoke documentation for local CLI, API, Studio, synthetic demo, DB, and Docker verification commands.
- Updated version metadata to `0.7.0`.

## v0.6.1 — Pilot Readiness Hardening

ReconForge ERP v0.6.1 focuses on post-productization hardening for credible local pilots, consultant demos, and cautious commercial evaluation while staying local-first, file-based, and export-oriented.

- Added client-pack redaction and exclusion controls, including `--redact-names`, `--redact-amounts`, `--exclude-raw-records`, `--summary-only`, `--exclude-evidence`, and optional manifest checksums.
- Added SHA-256 integrity manifests for evidence binders and optional checksum entries for client-pack manifests. These are checksums, not legal digital signatures.
- Added period trend reporting with new, recurring, resolved, high/critical, review-completion, accepted-risk, escalated, and top recurring theme outputs.
- Added export-based ERPNext, Microsoft Dynamics, and NetSuite profiles without direct connector claims.
- Added a Docker build workflow and Docker verification documentation while keeping Docker runtime verification explicitly unclaimed unless build/run commands pass in a live Docker environment.
- Added a compliance disclaimer, redaction controls guide, evidence-integrity guide, and updated security/commercial documentation.
- Added pilot proposal and client onboarding checklist documents for consultant-led evaluations.
- Added synthetic Odoo and SAP pilot case studies with clear no-customer, no-savings, and no-audit-opinion boundaries.
- Added a demo recording checklist, expanded demo video package, and static landing page foundation.
- Updated version metadata to `0.6.1`.

## v0.6.0

ReconForge ERP v0.6.0 focuses on productization and enterprise usability while staying local-first and file-based.

- Added Studio review status update actions that write to `output/review_state.json`.
- Added safe local POST handling, status validation, escaped rendering, and tests for Studio review updates.
- Added `reconforge mappings validate` for ERP mapping profile validation.
- Added `reconforge demo run` for a deterministic first-time-user workflow with reports, evidence, review state, and review register export.
- Added Control Value Summary sections to HTML and Excel reports.
- Added `reconforge report client-pack` for local consultant/client handoff folders.
- Added productization assessment, demo scenarios, client handoff docs, and practical 10-minute demo onboarding.
- Updated version metadata to `0.6.0`.

## v0.5.0

ReconForge ERP v0.5.0 adds the local exception review workflow foundation.

- Added file-based review state in `output/review_state.json` with allowed statuses: New, Under Review, Resolved, Accepted Risk, and Escalated.
- Added `reconforge review list`, `reconforge review set-status`, and `reconforge review export` CLI commands.
- Added optional `output/review_register.xlsx` export for audit-ready review status.
- Integrated review status, reviewer notes, and update metadata into evidence binder index, register, summaries, review forms, and audit trail JSON when review state exists.
- Added Studio exception filters for severity/risk level, exception type, review status, source file, search text, minimum amount, and sorting.
- Added review workflow documentation and tests for state handling, CLI commands, Studio filters, and evidence register integration.

## v0.4.0

ReconForge ERP v0.4.0 focuses on ERP mapping foundations for export-based Odoo and SAP workflows.

- Added rule-pack schema reference covering `pack.yml`, `rules.yml`, `mapping.yml`, `risk_model.yml`, supported operators, severity, confidence, risk impact, evidence fields, examples, and validation commands.
- Expanded the Odoo inventory valuation control pack with practical export mapping guidance, field candidates, join keys, quality checks, stronger rules, expected exceptions, and sample commands.
- Expanded the SAP MB51/FAGLL03 control pack with practical MB51 and FAGLL03/FBL3N export mapping guidance, field candidates, join keys, quality checks, stronger rules, expected exceptions, and sample commands.
- Updated Odoo and SAP export guides with field-level mapping notes and local workflow commands.
- Added strategic planning docs for repository assessment, competitor intelligence, category leadership, product roadmap, adoption engine, commercial strategy, trust/security, and thought-leadership content.
- Added structure tests for v0.4.0 schema docs and ERP mapping profiles.
- Added README positioning for export-based ERP mapping profiles and no-cloud-upload core workflows.

## v0.3.0

ReconForge ERP v0.3.0 expands the project into a stronger pre-1.0 platform release.

- Added advanced rule operators for duplicate, cross-file, variance, date, and aging controls.
- Added rule explanation command.
- Expanded to 15 control packs with risk models and sample commands.
- Added risk intelligence package.
- Added evidence binder review forms, HTML index, and Excel evidence register.
- Added anonymization profiles and amount noise.
- Added dealership and service synthetic data profiles plus currency option.
- Added benchmark HTML output and engine registry.
- Added matching strategies with confidence, similarity, and review-required metadata.
- Added offline exception explanation layer.
- Added plugin/connector foundation.
- Added expanded Studio pages for rule results and risk matrix.
- Added market intelligence, category strategy, risk, anonymization, synthetic data, AI, plugin, security, and privacy documentation.

## v0.2.0

ReconForge ERP v0.2.0 upgrades the project into a broader local-first ERP reconciliation and audit intelligence platform.

- Added YAML rule engine with validation, listing, and execution CLI commands.
- Added control packs for audit basics, Odoo inventory valuation, SAP MB51/FAGLL03, workshop spare parts, fleet maintenance, manufacturing WIP, and dealership service.
- Added audit evidence binder for High and Critical exceptions.
- Added data anonymizer that preserves referential integrity across ERP exports.
- Added synthetic data generator for workshop, manufacturing, and fleet scenarios.
- Added benchmark engine with Pandas support and optional DuckDB handling.
- Added ReconForge Studio local web interface.
- Added market research, product strategy, architecture, benchmark, Studio, plain-English, Arabic, playbook, launch, OpenAI OSS application, commercial strategy, and release documentation.
- Added security workflow, CodeQL workflow, Dependabot, and pre-commit configuration.
- Updated README, Dockerfile, Makefile, CI, security, contributing, and roadmap materials.

## v0.1.0

Initial open-source release of ReconForge ERP.

- Added Typer CLI with `init`, `validate`, `reconcile`, `report`, `dashboard`, and `doctor` commands.
- Added CSV and Excel readers for ERP exports.
- Added schema validation for required columns, data types, duplicate references, amount consistency, and master-data references.
- Added stock movement to GL reconciliation with exact, fuzzy reference, amount/date proximity, and exception matching.
- Added work-order controls for spare-parts issues, direct purchase fitting, old-part returns, invoice gaps, post-closure issues, and cancelled PO linkage.
- Added WIP aging report with configurable buckets and risk scoring.
- Added Excel management pack, CSV/JSON outputs, Markdown summary, and HTML dashboard report.
- Added local FastAPI dashboard server.
- Added synthetic sample data, documentation, Docker, Docker Compose, Makefile, tests, Ruff, Mypy, and GitHub Actions CI.
