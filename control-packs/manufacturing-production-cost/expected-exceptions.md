# Expected synthetic exceptions

- `MFG-002` exposes material-cost and completion-cost variance and scrap above
  the local five-piece control limit.
- `MFG-UNKNOWN` is visible as an unmatched material issue and is never silently
  attached to a production order.
