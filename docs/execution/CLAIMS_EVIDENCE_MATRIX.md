# Claims Evidence Matrix

This matrix governs public wording for the evidence-bounded Phase 0 implementation and non-publishing v0.7.1 candidate. A file, tag, or green check is not sufficient evidence unless the exact subject and corresponding gate are identified and verified.

| Claim | Code evidence | Test evidence | Runtime evidence | Maturity | Allowed wording |
| --- | --- | --- | --- | --- | --- |
| Versioned matching result envelope contract | E-857 publishes a closed Draft 2020-12 schema for strategy identity/version, explanation schema, SHA-256 digests, and JSON result/exception objects | Contract test validates every emitted payload against the schema and rejects version drift; Ruff/Mypy pass | Local schema/runtime evidence only; no independent consumer or hosted deployment certification | Experimental / bounded contract evidence | "Publishes a versioned schema for matching result evidence"; not production interoperability certification |
| Worker-boundary replay verification for matching results | E-856 routes grouped and sequential PostgreSQL adapter output through JSON round-trip, closed-envelope reconstruction, and manifest/identity/digest verification before projection | Matching strategy plus PostgreSQL grouped/sequential suites pass; Ruff/Mypy pass | Persistence-free synthetic worker boundary only; live hosted cross-engine and multi-host recovery remain unproven | Experimental / bounded matching evidence | "Verifies matching evidence before PostgreSQL-worker projection"; not live PostgreSQL production readiness |
| Closed JSON replay envelope for matching results | E-855 adds strict `MatchingStrategyResult.to_payload/from_payload` transport and replay verification independent of persistence backend | Matching strategy contract suite passes, including JSON round-trip, extra-field rejection, and identity tamper rejection; Ruff/Mypy pass | In-process synthetic transport evidence only; hosted cross-engine replay remains open | Experimental / bounded matching evidence | "Round-trips a closed matching result envelope and rejects tampering"; not PostgreSQL runtime or production assurance |
| Explicit matching strategy identity/version evidence | E-854 adds strategy ID/version fields to every `MatchingStrategyResult` and verifies them against the executing manifest alongside the manifest/input/decision digests | `tests/test_matching_strategy_contract.py`, PostgreSQL grouped/sequential worker contracts pass; Ruff/Mypy required | Synthetic adapter and worker evidence only; no cross-engine hosted replay or production readiness claim | Experimental / bounded matching evidence | "Attaches and verifies strategy identity/version with matching result digests"; not cross-engine or production assurance |
| Composite regulated admission evidence | E-853 binds complete regulated runtime findings to a non-local customer-managed key manifest and emits child/composite digests | Admission/key/deployment/inventory tests pass 50/50; Ruff and Mypy pass | Offline prerequisite evidence only; no KMS/HSM, IAM, provider, rotation, or production admission proof | Experimental / bounded deployment evidence | "Rejects regulated admission evidence with unresolved profile findings or a local key provider"; not regulated readiness or compliance |
| Provider-neutral managed-key custody evidence | E-852 verifies non-secret provider/key identity, active customer ownership, purpose, scope, and rotation bounds with a deterministic digest | Key/deployment/inventory tests pass 44/44; Ruff and Mypy pass | Offline metadata only; no KMS/HSM, key bytes, rotation execution, provider availability, or production custody evidence | Experimental / bounded deployment evidence | "Verifies a non-secret managed-key custody manifest"; not KMS/HSM control or regulated readiness |
| Profile-digest-bound runtime evidence | E-851 requires runtime facts to carry and match the immutable profile digest before evidence findings are returned | Runtime/readiness focused tests pass 12/12; Ruff and Mypy pass | Local digest binding only; no external runtime or IAM enforcement evidence | Experimental / bounded deployment evidence | "Rejects runtime evidence when it is not bound to the selected profile contract"; not production enforcement or readiness |
| Deployment readiness evidence CLI | E-850 verifies the closed mode matrix through bounded safe-YAML ingress, path containment, and canonical digest before displaying edition evidence | Readiness/runtime/profile/worker/inventory tests pass 36/36; Ruff and Mypy pass | Local repository evidence only; no external probes, IAM, secrets, databases, or production admission | Experimental / bounded deployment evidence | "Displays verified-scoped, partial, and open deployment evidence with a deterministic matrix digest"; not readiness or production assurance |
| Mode-specific deployment evidence matrix | E-849 consolidates Community/Team/Enterprise/Regulated evidence across eight gates with path-bound references and explicit partial/open boundaries | `tests/test_deployment_readiness_matrix.py` passes 2/2 with JSON Schema closure and evidence-path checks | Repository-visible and disposable local evidence only; regulated keys, independent failure domains, external IAM, and production readiness remain open | Experimental / bounded deployment evidence | "Provides a mode-specific evidence matrix with explicit gaps"; not deployment readiness, compliance, certification, or production assurance |
| Digest-bound deployment runtime evidence manifest | E-848 validates a closed local manifest containing one edition and the profile runtime facts, then returns explicit findings and a canonical digest | `tests/test_deployment_runtime_evidence.py`, deployment-profile/worker tests, and the exact parser inventory gate pass 31/31; Ruff and Mypy pass | Offline operator/CI evidence only; no external storage, identity, queue, object-store, key, backup, or production admission probe | Experimental / bounded deployment evidence | "Binds declared deployment runtime facts to a deterministic digest and profile findings"; not deployment readiness or external-service proof |
| Worker permission manifest verification CLI | E-847 adds `deployment verify-worker-manifest`, which parses one local JSON manifest, invokes the closed verifier, and prints the canonical digest without network or IAM mutation | `tests/test_deployment_profiles.py`, `tests/test_worker_permission_manifest.py`, and the exact parser inventory gate pass 18/18; Ruff and Mypy pass | Local CLI evidence only; no provider/IAM provisioning, hosted admission, or production runtime evidence | Experimental / bounded deployment evidence | "Verifies a local worker permission manifest and prints its deterministic digest"; not IAM provisioning or production enforcement |
| Digest-bound worker permission manifest verifier | E-846 provides a strict offline verifier for worker/principal identity, scope, distinct non-human discovery/execution permissions, complete grants, closed fields, and canonical SHA-256 digest | `tests/test_worker_permission_manifest.py` and deployment-profile tests pass 15/15; Ruff and Mypy pass | Local pure verification only; no provisioning, identity-provider read, hosted rollout, or production IAM evidence | Experimental / bounded deployment evidence | "Verifies a closed worker permission manifest and produces a deterministic digest-bound evidence input"; not service-account provisioning or production IAM assurance |
| Hosted worker discovery/execution separation gate | E-845 extends immutable deployment profiles and runtime facts so Team/Enterprise/Regulated validation fails closed without explicit worker discovery/execution separation evidence; Community remains local-first | `tests/test_deployment_profiles.py` passes 9/9 and covers missing/verified Team evidence, regulated findings, stable profile order/digests, and invalid facts | Descriptive local profile validator only; no service-account provisioning, hosted rollout, live IAM, or production assurance | Experimental / bounded deployment governance | "Fails hosted deployment-profile validation when worker discovery/execution permission separation evidence is absent"; not IAM rollout, enterprise readiness, or production assurance |
| Reconciliation discovery/execution permission separation | E-844 adds optional `discovery_policy_permission`; configured workers authorize queue enumeration with that permission while pre-claim execution checks retain `policy_permission`, with backward-compatible fallback | API/worker/policy contract tests pass 33/33 with one declared live-PostgreSQL skip; Ruff and Mypy pass | Synthetic worker/repository contract only; service-account provisioning, fleet rollout, live PostgreSQL, and production IAM evidence remain open | Alpha / bounded worker governance | "Supports least-privileged separation of reconciliation discovery and execution authorization"; not universal deployment enforcement or production assurance |
| Reconciliation worker amount-policy propagation | E-843 persists the submission exposure as rule metadata and re-applies it to the service-worker tenant and pre-claim scoped policy checks before execution | API/worker/policy contract tests pass 32/32 with one declared live-PostgreSQL skip; Ruff and Mypy pass | Synthetic in-memory repository and authenticated API/worker contracts; discovery-lane policy, live PostgreSQL, worker fleet, provider, posting, and production IAM evidence remain open | Alpha / bounded worker control | "Propagates an immutable reconciliation exposure into worker claim authorization"; not universal scheduler authorization or production assurance |
| Reconciliation run amount-ABAC binding | E-842 computes gross absolute exact Decimal exposure from canonical submission inputs before the server authorization decision; incomplete amount coverage remains None and is never inferred as zero | `tests/test_api_server_reconciliation.py` passes 3/3 and asserts `Decimal("20.00")`; route Ruff/Mypy pass | Synthetic authenticated PostgreSQL adapter contract only; no live database, worker, provider, posting, or production IAM evidence | Alpha / bounded route control | "Binds reconciliation run submission to an exact gross amount-bounded ABAC decision"; not complete reconciliation authorization, enterprise ABAC, or production assurance |
| Ownership-change preparation amount-ABAC binding | E-841 converts canonical ownership-change inputs and passes exact gross Decimal exposure into central server policy before PostgreSQL artifact persistence | `tests/test_api_consolidation_ownership_change.py`, domain and dependency tests pass 19/19 and assert `Decimal("220.00")` | Synthetic authenticated non-posting route only; no universal financial-route, provider, posting, or production IAM evidence | Alpha / bounded route control | "Binds ownership-change preparation to an exact gross amount-bounded ABAC decision"; not complete enterprise ABAC or production assurance |
| PPA preparation amount-ABAC binding | E-840 converts canonical PPA Money and passes consideration plus NCI fair value as gross Decimal exposure into central server policy before PostgreSQL artifact persistence; allocation detail is not double-counted | `tests/test_api_consolidation_ppa.py`, domain and dependency tests pass 16/16 and assert `Decimal("200.00")` | Synthetic authenticated non-posting route only; no universal financial-route, provider, posting, or production IAM evidence | Alpha / bounded route control | "Binds PPA preparation to an exact gross amount-bounded ABAC decision"; not complete enterprise ABAC or production assurance |
| Deferred-tax preparation amount-ABAC binding | E-839 converts canonical deferred-tax items and passes gross absolute fair-value Decimal exposure into central server policy before PostgreSQL artifact persistence; tax basis is not double-counted | `tests/test_api_consolidation_deferred_tax.py`, domain and dependency tests pass 15/15 and assert `Decimal("270.00")` | Synthetic authenticated non-posting route only; no universal financial-route, provider, posting, or production IAM evidence | Alpha / bounded route control | "Binds deferred-tax preparation to an exact amount-bounded ABAC decision"; not complete enterprise ABAC or production assurance |
| Intercompany elimination amount-ABAC binding | E-838 converts canonical Money lines and passes their gross absolute Decimal exposure into central server policy before PostgreSQL proposal persistence | `tests/test_api_consolidation_intercompany.py`, domain and dependency tests pass 15/15 and assert `Decimal("200.00")` | Synthetic authenticated non-posting route contract only; no universal financial-route, provider, posting, or production IAM evidence | Alpha / bounded route control | "Binds intercompany elimination preparation to an exact gross amount-bounded ABAC decision"; not complete enterprise ABAC or production assurance |
| Finance Core entry amount-ABAC binding | E-837 parses both sides of each PostgreSQL Finance Core journal line as exact non-negative decimals and passes the gross debit total into central server policy before adapter access | `tests/test_api_server_finance_core.py` asserts balanced synthetic `Decimal("140.00")` binding; server-core/live/dependency tests, Ruff, and Mypy pass | Synthetic authenticated server adapter contract only; no universal financial-route, live posting, provider, or production IAM evidence | Alpha / bounded route control | "Binds Finance Core entry creation to an exact amount-bounded ABAC decision before adapter access"; not complete enterprise ABAC, posting assurance, or production IAM |
| High-risk impairment route amount-ABAC binding | E-836 passes the exact Decimal sum of typed canonical carrying amounts from the PostgreSQL consolidation impairment prepare route into central server-scope policy evaluation before persistence | `tests/test_api_consolidation_impairment.py`, policy/dependency tests assert typed actor and exact `Decimal("140.00")` binding; Ruff and Mypy pass | Local authenticated route contract with synthetic non-posting data; no universal route coverage, live IAM, posting, provider, or production evidence | Alpha / bounded route control | "Binds the impairment prepare route to an exact amount-bounded ABAC decision before persistence"; not complete enterprise ABAC or production IAM assurance |
| Fail-closed bounded amount ABAC primitive | E-835 makes `CentralPolicyEngine` require a finite exact `Decimal` amount whenever a minimum or maximum bound is supplied; missing values cannot be inferred as zero or accepted implicitly | `tests/test_policy_engine.py` covers lower-only, upper-only, combined missing amounts and existing exact below/above/in-range boundaries; Ruff and Mypy pass | Local policy-engine evidence only; universal route/job/cache adoption, distributed invalidation, and production IAM effectiveness remain unverified | Alpha / bounded policy primitive | "Denies a bounded policy when its exact financial amount is missing"; not complete enterprise ABAC, authorization coverage, compliance, or production IAM assurance |
| Durable write-back recovery observation evidence | E-834 adds `WritebackRecoveryObservationRecord`, SQLite migration 43, PostgreSQL Alembic 0090, append-only triggers, scope binding, deterministic observation/evidence-node identities, API persistence before accepted transition, and reviewer list endpoint | `tests/test_connector_writeback_observations.py`, API recovery/list tests, Alembic/static contracts, and the retained SQLite/PostgreSQL matrix pass; exact PG 16.14/17.10 cells share record digest `768c363a6c196da78fe7c6ca40281f1d4ca051c194c8585f648b69c25574aa65` with direct mutation refusal and non-privileged role flags | Synthetic provider responses; one Docker host/failure domain; no live vendor, accounting, settlement, cross-host HA, or production credential evidence | Experimental / bounded durable evidence | "Persists scoped, immutable, digest-bound provider recovery observations and exposes them for reviewer drill-down"; not live provider interoperability, accounting posting, settlement, distributed exactly-once, HA/DR, or production assurance |
| Bounded provider status outcome observation | E-833 adds the explicit accepted/rejected/pending/not_found/unknown taxonomy and frozen `WritebackRecoveryObservation`; the recovery executor preserves immutable intent state unless the observation is accepted | Focused tests cover all five outcomes, legacy boolean compatibility, key binding, raw-response/response/observation digests, pending refusal, retry/TLS/secret boundaries, and no-state-advance behavior; 55/55 pass | Injected synthetic provider responses only; no live vendor or credential | Experimental / bounded recovery evidence | "Classifies provider status observations without lifecycle mutation and advances only on an accepted, idempotency-bound outcome"; not live provider semantics, accounting, settlement, distributed delivery, or production assurance |
| Fail-closed negative provider outcome handling | E-832 guards the shared write-back POST and idempotency-status response validators so `accepted=false` cannot produce an acknowledgement or compensation transition; the prior immutable intent remains available for governed reconciliation | Focused connector transport/domain tests cover original dispatch, recovery, compensation rejection, response schema, idempotency binding, retries, TLS pinning, secret boundaries, and no-state-advance assertions; 46/46 pass | Local injected synthetic responses only; no provider network or credential use in the new cases | Experimental / bounded transport correctness | "Rejects explicit negative provider outcomes before write-back acknowledgement state advances"; not provider interoperability, accounting, settlement, distributed delivery, or production assurance |
| Bounded PostgreSQL write-back recovery and compensation parity | E-831's additive closed runner stages the six-version append-only lifecycle and uses original versus distinct compensation idempotency keys after synthetic acceptance-before-persistence crash windows; SQLite is the canonical reference and exact PostgreSQL 16.14/17.10 cells use the existing write-back persistence and migration | Closed report/schema/source/role/package/CI tests cover 16 checks per PostgreSQL cell, 13 SQLite checks, recovery replay idempotency, compensation actor separation, direct UPDATE/DELETE refusal, tenant isolation, six negative mutations, cleanup, and canonical-history parity | One Docker Desktop host ran exact PostgreSQL 16.14/17.10 cells in 20.803 seconds; SQLite/PostgreSQL history SHA-256 `d59b3648c99690c016c73a3ca9012ca6e7d806429dccc0cbd109081656c5db9d`; report digest `bdf1d82d0667f244160c43068cdab5c7516096d31fba17848e2ec1c33a3cdbb2` | Experimental / bounded provider-neutral recovery evidence | "Proves bounded recovery and compensation-key parity for synthetic write-back acceptance on the disclosed PostgreSQL 16.14/17.10 and SQLite profiles"; not live-provider conformance, accounting, settlement, cross-host HA, distributed exactly-once, production RPO/RTO, or production assurance |
| Bounded receiver replay across synchronous PostgreSQL failover | E-830's additive closed runner uses the unchanged E-829 receiver, physical streaming `remote_apply`, direct SyncRep observation, exact fencing-before-promotion, controller-paused re-seed, synchronous rejoin, endpoint rediscovery, and SQLite reference parity on policy-owned images | Closed schema/report/source/image/role/package/CI tests cover 23 true checks per cell, response-loss replay, partition uncertainty, one post-rejoin apply, restart no-effect, RPO/RTO bounds, secret absence, six negative mutations, and supply-policy consumer closure | One Docker Desktop host ran exact PostgreSQL 16.14/17.10 two-node cells in 64.067 seconds; acknowledged synthetic-effect RPO 0; local RTO 6.168/6.199 seconds; both final/rejoined/restarted histories equal SQLite SHA-256 `5f5f48a2cf4071f93e064b127f2ecfa67fb5cbf29463a357aa93cfba52419f14`; report digest `5ae22491c01eb93daf38dd7fe6788c4daa7a0d648fb7dfa53ca7edf11d5e07d1` | Experimental / bounded single-host synchronous topology | "In the disclosed one-host PostgreSQL 16.14/17.10 synchronous topology, an acknowledged synthetic receiver effect survived manual fenced promotion with no duplicate, and one partition-uncertain identity applied after redundancy was restored"; not cross-host HA, automatic failover, production RPO/RTO, provider interoperability, accounting, settlement, or production exactly-once assurance |
| Bounded PostgreSQL receiver replay parity | The E-829 optional psycopg store preserves the E-828 digest-only contract, takes a transaction-scoped canonical receiver/key lock, atomically commits immutable receipt/effect rows, and exposes no listener or posting path | Constructor/schema/lazy-dependency tests plus the closed report contracts cover exact images, non-privileged roles, sequential/conflict/eight-process/crash cases, direct mutation and malformed input, native restore, source/policy binding, SQLite parity, package/CI order, and negative evidence | One Docker Desktop host ran exact PostgreSQL 16.14 and 17.10 images sequentially in 14.142 seconds; each passed 17 checks and cleanup, three receipts equal three effects, both restored histories equal SQLite SHA-256 `1c42c656b8e09897710e05411c78a91c709c15b70309d2e68f1e4417f1ac32f8`; report digest `3e53b99e9d33598d5f010d2b0ad405cbc167936840976564221f8b27bb4d33b1` | Experimental / bounded single-host server-backed conformance | "The disclosed PostgreSQL 16.14/17.10 reference receiver converges same-database exact replays to one synthetic effect and matches the SQLite canonical history"; not live-provider interoperability, distributed exactly-once, cross-host failover, accounting, settlement, HA/DR, or production assurance |
| Bounded write-back receiver replay creates one synthetic effect | The E-828 digest-only request, atomic SQLite effect/receipt store, immutable triggers, closed runner/schema, ADR 0542, and CI artifact definition model the receiving side separately from sender retries | Unit/property/report/network/SDK/package tests cover closed input, stable/separating digests, sequential and spawned-process replay, payload conflict, mutation refusal, source/report binding, independent restore, secret/payload absence, negative schema, and workflow order | One Windows host ran eight spawned receiver processes plus crash-after-commit and independent SQLite restore in 2.473 seconds; three receipts equal three effects; refreshed report digest `cfe14335be6d04387d9f2c1be2909a1d843ebefe744e421f288cf096479274fb`; no live vendor, cross-host consensus/failover, accounting posting, settlement, or hosted run | Experimental / bounded same-host receiver conformance | "The disclosed SQLite reference receiver converges exact replays to one synthetic effect and refuses same-key payload retargeting"; not vendor interoperability, distributed exactly-once delivery, accounting, settlement, or production assurance |
| Declared PostgreSQL write-back migration matrix retains decision parity | The E-827 shared observation runner, closed matrix schema, supply-chain policy, CI artifact step, and ADR 0541 execute one refusal/restore implementation against exact PostgreSQL 16.14 and 17.10 digests | Matrix/drill/policy tests validate both complete cells, identical canonical valid/invalid histories, all checks and cleanup, source/policy/report digests, closed negative cases, package inclusion, and workflow ordering | One Docker Desktop host ran both sequential single-node cells in 24.178 seconds; report digest `6c9e41dd55ef0ff9292a52aacd15030766660db85c5ddda78391aeeb4ecb73f3`; no hosted run, provider, posting, rolling upgrade, replication, cross-host HA/DR, or production recovery | Experimental / bounded two-version migration parity | "The disclosed PostgreSQL 16.14 and 17.10 profiles produce identical canonical write-back histories and pass the same 0089 refusal and independent-restore checks"; not an open-ended support, provider, accounting, HA/DR, or production claim |
| PostgreSQL write-back migration refuses drift without evidence mutation | Alembic `0089_pg_writeback_identity`, the E-826 digest-pinned Docker runner, closed report schema, and ADR 0540 preserve the refused database while defining an independent pre-drift restore path | Retained-report schema/canonical-digest/source/runtime-binding tests plus the live drill prove exact audit refusal, unchanged revision/history/trigger, native dump listing, independent restore and upgrade, enhanced direct-INSERT refusal, and cleanup | One Docker Desktop host, digest-pinned PostgreSQL 17.10, two isolated databases, native `pg_dump`/`pg_restore`, synthetic credentials/data, source-bound runner/migration digests, and refreshed report digest `b143c756a27b914ae92a32c3a1595529bd87fc4d64784afee1710a817a44674e`; E-827 separately covers the declared two-version matrix; no provider, posting, cross-host HA/DR, or production recovery | Experimental / bounded migration recovery evidence | "Proves that PostgreSQL 0089 refuses a synthetic drifted write-back history without mutating it and that a pre-drift native backup upgrades independently on the disclosed runtime"; not provider, accounting, HA/DR, or production assurance |
| Write-back lifecycle versions remain bound to one proposal identity | `WritebackIntent.proposal_digest`, shared transition validation, SQLite migration 42, PostgreSQL Alembic `0089_pg_writeback_identity`, and ADR 0539 bind schema/scope, provider mutation, payload digest, idempotency, requester/time, and feature decision while preserving append-only evidence | Connector properties and adversarial repository/direct-SQL/migration/API contracts cover all identity fields, state jumps, drifted-history refusal, stable proposal digest, changing version digests, and migration-chain parity; focused selector and static gates pass | Local SQLite plus disposable digest-pinned PostgreSQL 17.10 runtime under a non-superuser/NOBYPASSRLS application role; both live histories and a three-head-upgrade/two-deep-downgrade Alembic drill pass. No live provider, source authenticity, accounting posting, distributed receiver idempotency, HA/DR, or production assurance | Experimental / bounded write-back governance | "Binds local and server-profile write-back lifecycle evidence to one immutable proposal identity and rejects retargeted versions"; not live provider execution, accounting posting, autonomous approval, or production readiness |
| Global expansion claims remain evidence-gated | `docs/adr/0531-global-expansion-program-framework.md`, `docs/execution/BACKLOG.yaml` (`E-1000`, `E-1001`), and `docs/execution/DECISIONS.md` (`D-910`, `D-912`) require explicit closure gates before any global/enterprise/global-ready wording is allowed | Planning-only slice evidence is recorded in `STATE.md` and `EVIDENCE.md`; no dedicated regression exists for global wording because this is a claim-boundary control | No runtime command executed specifically for this control; closure remains conditional on slice evidence in `E-1001` dependencies and future runtime evidence files | Experimental / bounded governance control | "Global capability claims are only stated after close/consolidation, matching, connectors, durable jobs, identity, and deployment-mode slices reach their recorded evidence gates" |
| Fail-closed deployment edition profile contract | `reconforge/deployment/profiles.py` defines immutable digest-bound Community/Team/Enterprise/Regulated declarations and `reconforge deployment profiles` renders them read-only; ADR 0532 and E-810 define the boundary | `tests/test_deployment_profiles.py` covers stable order/digests, network/write-back denial, human approval, regulated prerequisites, invalid facts, and CLI filtering; 7/7 focused tests, Ruff, and Mypy pass | Local Python/CLI contract only; no service provisioning or verification, hosted deployment, identity federation, KMS/HSM, independent failure-domain, RPO/RTO, or production evidence | Experimental / bounded deployment contract | "Provides a deterministic descriptive deployment-edition contract with fail-closed runtime findings"; not deployment readiness, HA/DR, compliance, certification, or production assurance |
| Arabic/English locale-aware major Studio number rendering | `apps/web/src/App.tsx` and major Studio views (`BankStatementStudio`, `RetailSettlementStudio`, `ManufacturingCostStudio`, `ProfessionalInvoicePaymentStudio`, `IndividualCashflowStudio`, `MappingStudio`, `RuleStudio`, `AdminAudit`, `LiveStudio`) route the active locale into shared locale formatters for summary/count/value rendering while keeping all projection semantics and permissions unchanged | `apps/web/src/locale-format.test.ts`, `apps/web/src/i18n.test.ts`, and `apps/web/src/App.test.tsx` cover locale-format integrity and locale metadata behavior; focused locale selector passes 5/5 and web typecheck passes | Local frontend typecheck/Vitest only; no backend/service behavior changed | Experimental / bounded global accessibility slice | `Renders major Studio metrics, counts, and result summaries with deterministic Arabic/English locale formatting` |
| Policy-bearing artifact constructors fail closed | `VarianceThresholdPolicy`, `ClientPackOptions`, and `RulePackExecution` validate financial-input policy in `__post_init__` so unsupported values fail at construction time before artifact, strategy, or report consumers can observe them | `tests/test_variance_analysis.py`, `tests/test_client_pack_financial_input_policy.py`, and `tests/test_rules_engine.py` prove unsupported constructor values raise `InvalidAmountError`; focused constructor tests, Ruff, and Mypy pass | Local construction and focused contract checks only; no provider, posting, write-back, hosted CI, HA/DR, or publication evidence added by this slice | Experimental / bounded exactness guard | "Rejects unsupported financial-input policy metadata for variance, client pack, and rule-result artifacts at construction time"; not a posting, provider, or production claim |
| Core matching and Stock/GL result policy metadata fails closed | `StockGLReconciliationResult`, `MatchRunResult`, and `DeterministicMatchOutput` validate the canonical financial-input policy before frozen results escape; strict-v2 remains the default and explicit legacy-v1 remains named compatibility | `tests/test_stock_gl_reconciliation.py`, `tests/test_application_matching.py`, `tests/test_reconciliation_input_policy.py`, and `tests/test_reports.py` reject unsupported construction and preserve policy behavior; the focused 33-test suite, current 2,935-test full regression, Ruff, and Mypy pass | Local result construction and report consumers only; no provider, posting, write-back, hosted, HA/DR, or production evidence | Experimental / bounded exactness guard | "Rejects unsupported core matching and Stock/GL financial-policy metadata at result construction"; not a statutory, provider, or production claim |
| Work-order result policy metadata fails closed | `WorkorderReconciliationResult.__post_init__` validates the canonical financial-input policy before a frozen result escapes; strict-v2 remains the default and explicit legacy-v1 remains named compatibility | `tests/test_workorder_reconciliation.py` rejects unsupported construction; the focused Work-order/report/CLI/reconciliation-policy suite, Ruff, and Mypy pass | Local result construction and report consumers only; no provider, posting, write-back, hosted, HA/DR, or production evidence | Experimental / bounded exactness guard | "Rejects unsupported Work-order financial-policy metadata at result construction"; not a statutory, provider, or production claim |
| Current local PostgreSQL industry/API runtime boundary | Alembic migrations through `0088_pg_currency_snapshot`, PostgreSQL persistence adapters, and authenticated server-API selections for manufacturing, retail settlement, professional invoice-payment, and bank-statement controls | E-716 runs the eight industry persistence/API files plus the live metrics parity and Alembic availability contracts; all 26 selected tests pass with the existing deprecation warning only | Fresh disposable PostgreSQL 16 and Redis 7.4 services on one Docker Desktop host, non-superuser PostgreSQL role, synthetic records, and cleanup | Experimental / bounded local runtime | "Current local PostgreSQL industry/API persistence contracts pass through migration head under a non-superuser role"; not hosted CI, native backup, live provider, source-authenticity, posting, write-back, HA/DR, or production assurance |
| Combined Work-order and Stock/GL reports retain one financial-input policy | `reconforge/reconciliation/workorders.py` binds the validated keyword-only policy to every monetary ingress and `WorkorderReconciliationResult`; `reconforge/reports/management_pack.py` rejects unsupported or mixed result policies before writing; the Work-order CLI emits the selected policy in JSON/workbook metadata | `tests/test_workorder_reconciliation.py`, `tests/test_reports.py`, `tests/test_cli_and_studio.py`, and `tests/test_reconciliation_input_policy.py` cover strict result metadata, artifact metadata, and mixed-policy refusal; Ruff and Mypy pass | Local SQLite/sample-data CLI/report path only; no statutory posting, live provider, hosted CI, HA/DR, or production evidence | Experimental / bounded exactness guard | "Provides explicit policy binding, artifact metadata, and mixed-policy refusal for combined local Work-order and Stock/GL reports"; not statutory accounting, provider, or production assurance |
| Workspace-bound currency-registry governance | `reconforge/utils/currency_registry_governance.py`, SQLite migrations 40/41, PostgreSQL Alembic `0087_pg_currency_binding`/`0088_pg_currency_snapshot`, local/PostgreSQL master-data adapters, authenticated bind route, CLI command, snapshot/export/backup integration, and the currency schemas persist a version/digest binding plus canonical historical snapshot per workspace without switching the process-wide registry; `CurrencyRegistryContext` adds an immutable operation boundary | `tests/test_currency_registry_governance.py`, `tests/test_money_currency.py`, `tests/test_master_data.py`, `tests/test_application_master_data.py`, `tests/test_postgres_master_data_application.py`, `tests/test_postgres_operations.py`, `tests/test_db_backup_restore.py`, `tests/test_db_export_persisted_json.py`, `tests/test_db_export_import.py`, and authorization-inventory contracts cover current/drifted/invalid/unbound states, automatic historical-context selection, tamper refusal, sanitization, context mutation isolation, lineage refusal, RLS/schema/migration shape, API/CLI, replay, and backup/export | Local SQLite and PostgreSQL adapter/schema contracts only; no configured live PostgreSQL rerun, live FX, statutory posting, or production assurance | Experimental / bounded governance evidence | "Provides explicit workspace-bound currency binding with deterministic replay of a persisted registry context and installed-registry drift evidence"; not statutory accounting, live-rate, compliance, or production-readiness claim |
| Experimental bank statement to ledger control | `reconforge/domain/bank_statement_control.py`, `reconforge/application/bank_statement_control.py`, CAMT.053 projection, CLI registration, `control-packs/bank-statement-reconciliation`, and module registry `bank.cash-reconciliation` implement the exact non-posting export boundary; the reader also verifies the nested decision digest and canonical projection | `tests/test_bank_statement_control.py` covers exact Money/tolerance, reference normalization, date window, account/amount exceptions, ambiguity, duplicate IDs, unmatched records, permutation digest, outer and nested report tamper refusal, CLI, and schema; full local suite/static/package gates are recorded by E-437/E-438/E-599 | Synthetic local CAMT.053 XML, JSON/CSV fixtures, and the CLI produce a nested- and outer-digest-bound report; no bank/ERP provider, network, posting, write-back, persistence, or production runtime evidence | Experimental / bounded local artifact | "Provides an experimental local CAMT.053-to-ledger control for exported records with canonical decision-integrity checks and explicit matched, exception, ambiguous, and unmatched outcomes"; not a live bank connector, source authenticity, payment initiation, statutory posting, write-back, or production claim |
| Local and server-profile bank-statement evidence persistence | Migration 39, `reconforge/infrastructure/sqlite_bank_statement_control.py`, Alembic 0084, `PostgresBankStatementRepository`, authenticated `/api/v1/bank/statement-controls` routes, backup integration, and module registry expose immutable workspace-scoped persistence of replay-verified bank-control artifacts; server mode selects PostgreSQL without SQLite fallback | `tests/test_sqlite_bank_statement.py`, `tests/test_api_bank_statement.py`, `tests/test_postgres_bank_statement.py`, and `tests/test_api_server_bank_statement.py` cover exact replay verification, idempotency, tamper/conflict refusal, workspace/tenant isolation, authentication, migration gating, and backup/restore; current authorization inventory passes with 265 routes and digest `66292d07fd4513cbdacc22a95fb4f0ed3f1e16497fdc9044ae02d99233f9efdf` | Local SQLite plus a disposable PostgreSQL 16 non-superuser runtime with synthetic CAMT.053/ledger exports; both profiles report `network_dispatch=disabled`; no live bank authentication/source authenticity, worker completion, payment initiation, posting, write-back, HA/DR, hosted CI, or production evidence | Experimental / bounded local and server-profile persistence | "Provides authenticated local and server-profile persistence for replay-verified bank-statement control evidence"; not a live bank connector, source authenticity, payment initiation, posting, write-back, HA/DR, or production claim |
| Read-only bank reconciliation Studio projection | `apps/web/src/components/BankStatementStudio.tsx`, strict `studio-bank-statement.json` contract validation, `/bank-statement` route/navigation, module registry `modern-studio` interface, and explicit synthetic/no-provider/no-posting boundary | `apps/web/src/components/BankStatementStudio.test.tsx`, `apps/web/e2e/accessibility.spec.ts`, web typecheck, production build, and the full 61-test component plus 7-test accessibility suites pass | Checked-in synthetic browser projection only; no authenticated live API session, bank/provider authenticity, payment initiation, posting, ERP write-back, HA/DR, or production banking runtime evidence | Experimental / bounded modern Studio | "Provides a read-only English/Arabic Studio view for synthetic, replay-digested bank statement evidence"; not a live bank connector, source authenticity, posting, write-back, HA/DR, or production readiness claim |
| Experimental manufacturing production-cost control with local and server-profile evidence | `reconforge/domain/manufacturing_cost_control.py`, `reconforge/application/manufacturing_cost_control.py`, CLI registration, SQLite/PostgreSQL repositories, authenticated API routes, migrations 38/0083, backup integration, `control-packs/manufacturing-production-cost`, and module registry `manufacturing.cost-control` implement exact non-posting cost/quantity controls and immutable persistence; readers verify nested and outer digests | `tests/test_manufacturing_cost_control.py`, `tests/test_sqlite_manufacturing_cost_control.py`, `tests/test_api_manufacturing_cost_control.py`, `tests/test_postgres_manufacturing_cost_control.py`, and `tests/test_api_server_manufacturing_cost_control.py` cover exact Money/Quantity, variance/lineage outcomes, idempotent replay, workspace/tenant isolation, tamper refusal, migration gating, backup/restore replay, API permissions, RLS schema, server selection, live HTTP list/read, authorization inventory, Ruff, and Mypy; disposable PostgreSQL 16 runtime passes with a non-superuser role | Synthetic local exports and a disposable PostgreSQL runtime produce replay-verified evidence; local and server-profile routes expose create/list/read with network dispatch disabled; hosted CI, ERP/MRP provider, statutory valuation, posting, write-back, HA/DR, and production runtime evidence remain open | Experimental / bounded local and server-profile artifact | "Provides an experimental manufacturing production-cost control over exported production records with replay-verified local SQLite and server-profile PostgreSQL evidence"; not statutory valuation, live ERP/MRP integration, inventory/WIP/GL posting, write-back, HA/DR, or production claim |
| Experimental manufacturing production-cost control | `reconforge/domain/manufacturing_cost_control.py`, `reconforge/application/manufacturing_cost_control.py`, CLI registration, SQLite/PostgreSQL persistence boundaries, authenticated API routes, and module registry `manufacturing.cost-control` implement exact non-posting cost/quantity controls | `tests/test_manufacturing_cost_control.py`, `tests/test_sqlite_manufacturing_cost_control.py`, `tests/test_api_manufacturing_cost_control.py`, `tests/test_postgres_manufacturing_cost_control.py`, and `tests/test_api_server_manufacturing_cost_control.py` cover the deterministic report plus idempotency, workspace/tenant isolation, tamper refusal, backup/restore replay, RLS schema, API permissions, live HTTP list/read, authorization inventory, Ruff, Mypy, and disposable PostgreSQL runtime | Synthetic exports and disposable local PostgreSQL evidence only; hosted CI, ERP/MRP provider, statutory valuation, posting, write-back, HA/DR, and production runtime remain unverified | Experimental / bounded local and server-profile artifact | "Provides an experimental manufacturing production-cost control over exported records with replay-verified local and server-profile evidence"; not statutory valuation, live ERP/MRP integration, inventory/WIP/GL posting, write-back, HA/DR, or production claim |
| Read-only manufacturing cost Studio projection | `apps/web/src/components/ManufacturingCostStudio.tsx`, strict `studio-manufacturing-cost.json` contract validation, `/manufacturing-cost` route/navigation, module registry `modern-studio` interface, and explicit synthetic/no-MRP/no-posting boundary | `apps/web/src/components/ManufacturingCostStudio.test.tsx`, `apps/web/e2e/accessibility.spec.ts`, web typecheck, production build, and the full component plus accessibility suites pass | Checked-in synthetic browser projection only; no authenticated live API session, ERP/MRP authenticity, inventory or GL posting, ERP write-back, HA/DR, or production manufacturing runtime evidence | Experimental / bounded modern Studio | "Provides a read-only English/Arabic Studio view for synthetic, replay-digested manufacturing cost evidence"; not a live MRP/ERP connector, statutory valuation, posting, write-back, HA/DR, or production readiness claim |
| Experimental professional invoice and payment control with dual-mode persistence | `reconforge/domain/professional_invoice_payment_control.py`, `reconforge/application/professional_invoice_payment_control.py`, `reconforge/infrastructure/sqlite_professional_invoice_payment.py`, `reconforge/infrastructure/postgres_professional_invoice_payment.py`, `reconforge/api/routes/professional_invoice_payment.py`, `reconforge/api/server_professional_invoice_payment.py`, and module registry `professional.invoice-payment` implement exact non-posting invoice/payment controls plus bounded local/server-profile persistence | `tests/test_professional_invoice_payment_control.py`, `tests/test_sqlite_professional_invoice_payment.py`, `tests/test_api_professional_invoice_payment.py`, `tests/test_api_server_professional_invoice_payment.py`, and `tests/test_postgres_professional_invoice_payment.py` cover exact Money, normalized references, client/amount/date exceptions, duplicate IDs, ambiguity, unmatched invoice/payment, idempotent persistence, workspace isolation, tamper refusal, restore, manifest inclusion, and API boundaries; E-643 adds real TestClient list/read HTTP/RLS coverage | Synthetic local JSON/CSV fixtures plus a disposable PostgreSQL 16 non-superuser HTTP run produce replay-verified evidence and workspace/sibling-tenant denial; no billing/provider call, network, receivables allocation, posting, write-back, production runtime evidence | Experimental / bounded local and server-profile artifact | "Provides a bounded non-posting professional invoice-to-payment control over exported records with dual-mode replayable persistence (SQLite local, PostgreSQL server-profile) and explicit scope checks"; not revenue recognition, receivables posting, live billing connectivity, ERP write-back, full parity across all deployments, HA/DR, or production claim |
| Experimental retail POS settlement control | `reconforge/domain/retail_settlement.py`, `reconforge/application/retail_settlement.py`, CLI registration, `control-packs/retail-pos-settlement`, and module registry `retail.settlement` implement the exact non-posting export boundary; the reader additionally verifies the nested decision digest, canonical ordering, and status counts | `tests/test_retail_settlement.py` covers exact Money/tolerance, fees/refunds/chargebacks, variance, unmatched/ambiguous/duplicate identity, permutation digest, outer and nested report tamper refusal, CLI, and pack execution; focused 10-test run passes | Synthetic local JSON/CSV fixtures produce a nested- and outer-digest-bound report; no provider, ERP, network, posting, persistence, or production runtime evidence | Experimental / bounded local artifact | "Provides an experimental local POS-to-processor settlement control for exported records with canonical replay-integrity checks and visible fees, refunds, chargebacks, unmatched and ambiguous outcomes"; not a live processor connector, source authenticity, settlement-finality, fraud, statutory posting, write-back, or complete retail claim |
| Current local live S3-compatible object-storage drill | `docs/execution/S3_OBJECT_STORAGE_LIVE_DOCKER_DRILL_2026-08-05.json` records the real boto3-backed adapter against digest-pinned MinIO with normal and Object Lock buckets | `tests/test_current_s3_object_storage_live_report.py` validates the closed schema, canonical report digest `5f4ef103abfe4b198bc64e348f554dc52e56d3ebb5d7f425faf1761ce6225c6a`, five observed invariants, and cleanup | One disposable single-node MinIO process with synthetic credentials/bytes; no replication, KMS, cross-site durability, provider interoperability, object-store HA/DR, malware scanning, authorized downloads, or production SLO evidence | Experimental / bounded provider runtime | "Current local evidence verifies the S3-compatible adapter's scoped isolation, immutable conflict, checksum, retention-delete, and cleanup contract"; not cloud durability, object-store HA/DR, or production availability |
| Latest local live S3-compatible object-storage drill | `docs/execution/S3_OBJECT_STORAGE_LIVE_DOCKER_DRILL_2026-08-10.json` records the real boto3-backed adapter against the current MinIO image digest with normal and Object Lock buckets | E-662 and the current-report test validate the latest report alongside the two prior artifacts; all five observed invariants and cleanup are true, with report digest `22597553e1833cf7ec8735ee1091f8309d0117997133b56bd9ee7119c27e1d49` | One disposable single-node MinIO process on one host with synthetic credentials/bytes; no replication, KMS, cross-site durability, provider interoperability, object-store HA/DR, malware scanning, authorized downloads, or production SLO evidence | Experimental / bounded provider runtime | "Latest local evidence verifies the S3-compatible adapter's scoped isolation, immutable conflict, checksum, retention-delete, and cleanup contract"; not cloud durability, object-store HA/DR, or production availability |
| Current local live Redis session/policy drill | `docs/execution/REDIS_LIVE_DOCKER_DRILL_2026-08-05.json` records the real tenant-scoped Redis stores against digest-pinned Redis 7.4 Alpine | `tests/test_redis_live_report.py` validates the closed schema, canonical report digest `99fbd6b7aba969e0a41f4faa9e26d034e5e0f3b5a36e0e4e22bb2928ed9b44ca`, four observed invariants, and CI report upload wiring | One disposable single-node Redis process with synthetic keys, token digests, and session metadata; no replication, Sentinel/Cluster failover, cross-site durability, Redis HA, or production SLO evidence | Experimental / bounded dependency runtime | "Current local evidence verifies tenant-scoped Redis session/policy primitives and cleanup"; not distributed cache HA, failover, or production IAM availability |
| Bounded CAMT.053 offline statement ingestion | `reconforge/connectors/camt053.py` parses one `BkToCstmrStmt/Stmt` with `defusedxml`, 8 MiB/100K-entry/8 KiB text bounds, exact finite Decimal strings, stable entry identity, date ordering, balances, deterministic source digest, and a bounded projection into internal payment-statement pages; `reconforge connectors parse-camt053` emits the closed JSON contract | `tests/test_connector_camt053.py` covers replay digest, JSON Schema, CLI/file output, packaging, duplicate/missing identity, non-finite amount, date ordering, XXE, multiple statements, payload/page bounds, and signed-lineage page projection; connector SDK/network focused tests pass | Local synthetic golden XML and CLI only; no bank credentials, provider transport, live vendor acknowledgement, payment initiation, ERP mapping, write-back, or production deployment | Experimental / bounded evidence | "Provides a bounded offline CAMT.053 statement-ingestion contract with deterministic, read-only evidence and an internal payment-statement projection"; not a live bank connector, payment rail, ERP integration, write-back, or production-readiness claim |
| Governed provider read-manifest portfolio | `reconforge/connectors/conformance.py::verify_manifest_portfolio`, `verify_network_connector`, and `verify_network_retry_failure_injection` enforce read-only capability, synthetic/idempotent declarations, exact HTTPS egress, secret-reference authentication, bounded retry/cursor declarations, process-local circuit breaking, and replay identity across the CAMT.053 HTTPS and ERPNext GL Entry/Payment Entry manifests | `tests/test_connector_sdk.py::test_reference_manifest_portfolio_is_read_only_and_governed` covers the complete reference manifest set; `test_provider_network_manifest_portfolio_replays_through_common_conformance` runs two identical synthetic reads plus 503/429 transient recovery for all three provider-specific registrations; `tests/test_connector_network.py` proves fail-fast circuit opening/recovery, endpoint/credential isolation, and fail-closed circuit bounds; E-591/E-600/E-601 record focused and full local gates | In-process transport and synthetic secret resolver only; no live bank/ERP tenant, provider dialect/version conformance, source authenticity, certificate lifecycle, distributed quota, settlement, posting, write-back, or production availability | Experimental / SDK contract evidence | "Provides a governed, replay-deterministic SDK conformance portfolio with bounded transient recovery and scope-isolated process-local circuit protection for provider read sources"; not live provider interoperability, distributed quota coordination, or production readiness |
| Provider adapter HTTPS sandbox | `tests/test_connector_provider_tls_sandbox.py` uses per-test localhost certificates, an injected public resolver, and the real `PinnedHttpsGetTransport` to exercise CAMT.053 and ERPNext GL Entry/Payment Entry response boundaries | Three focused tests inject a first 503 and require retry, closed response parsing, scope/query/cursor checks, and bearer/token isolation; E-593 records the runtime boundary and package/ADR evidence | Disposable local TLS server and synthetic payloads only; no provider tenant, dialect/version, source authenticity, certificate/credential lifecycle, settlement, posting, write-back, or production availability | Experimental / bounded transport runtime | "Exercises the bounded provider adapters through a local pinned-HTTPS sandbox with retry and schema checks"; not live provider interoperability or production readiness |
| PostgreSQL durable-job 100K-effect tier | `reconforge/benchmark/postgres_durable_job_scale.py::hundred_k_profile` drives 16 workers over 2,500 jobs and forty partitions per job across four forced-RLS tenant lanes | Local artifact plus hosted CI `30970795278` / `server-boundaries` `92194440053` prove 100,000/100,000 effects, 2,500/2,500 completed jobs, zero duplicates/residue, and 625 completions per lane | Digest-pinned PostgreSQL 16 single-node CI service and a local Windows 11/Python 3.14.6 run; no soak, capacity, backpressure, queue HA, failover, host loss, cross-host fairness, RPO/RTO, or production sizing | Experimental / hosted bounded PostgreSQL concurrency evidence | "Publishes a reproducible PostgreSQL 100K-effect durable-job correctness tier"; not a throughput, capacity, HA/DR, SLO, or production-readiness claim |
| PostgreSQL durable-job 1M local-scale tier | `reconforge/benchmark/postgres_durable_job_scale.py` and `tests/test_postgres_durable_job_scale.py` drive and verify `postgres-durable-job-load/1m-effects-v1` with the closed manifest inclusion contract | Structural checks cover profile metadata (`2,500` jobs, `400` partitions per job, `4` tenant lanes, `1,000,000` declared effects), synthetic workload behavior, manifest inclusion, and no-escape invariants for the published profile; `tests/test_postgres_durable_job_scale.py` includes manifest include-file assertions for the 1M artifact | `docs/execution/benchmarks/postgres-durable-job-1m-effects-v1.md` and `docs/execution/benchmarks/postgres-durable-job-1m-effects-v1.json` report local PostgreSQL 16 execution with 1,000,000/1,000,000 effects, 0 duplicates, drained queue/running depth, and manifest/effect digests `252eb01a1b679deb977141d9e1d931e89469d161877e8fdd1a4e88c6ae642103` / `95ba466b31a08f5b3a4506d0b8fc5b44742f01627c79aea772e46d7bd073ee5c`; observed runtime is 1932.3678s / 1.2937 jobs/sec | One-host local Windows 11/Python 3.14.6 PostgreSQL 16 synthetic execution; runtime observations are bounded and do not establish throughput, capacity, queue HA, queue-backpressure coupling, failover/host-loss, soak, RPO/RTO, or production SLO | Experimental / bounded local PostgreSQL scale evidence | "Publishes a bounded local PostgreSQL 1M-effect durable-job correctness tier and its digest-bound execution report"; not an HA/DR, distributed capacity, queue fairness, production capacity, or SLO claim |
| Acquisition deferred-tax calculation bridge | `reconforge/domain/consolidation_deferred_tax.py` calculates signed temporary differences and exact rounded DTA/DTL effects from source-bound acquisition items; policy/source digests, independent maker-checker actors, canonical ordering, closed schema/CLI, and `posted: false` are enforced | `tests/test_consolidation_deferred_tax.py` passes 5/5, including arithmetic/totals, permutation-stable digest, maker-checker/rate rejection, tamper verification, schema validation, and CLI contract; Ruff/Mypy/diff-check pass for changed files | Local synthetic Money/Decimal inputs only; no tax-law provider/rates, recognition or valuation-allowance decision, statutory/legal-book posting, filing, HA/DR, or production close runtime | Experimental / bounded non-posting financial calculation | "Provides a deterministic, replay-verifiable acquisition deferred-tax calculation bridge"; not statutory tax accounting, tax advice, recognition, journal posting, live rates, or production readiness |
| Full local compatibility after deferred-tax slice | Current tree including `acquisition-deferred-tax-bridge-v1` is exercised by the complete repository suite and package/static gates | `uv run pytest -q --tb=short` exits 0 in 325.9s; Ruff, Mypy (448 files), package build, and `git diff --check` pass; declared skips and existing warnings are retained | One Windows workstation with local dependencies; no hosted Python matrix, live vendor provider, independent HA/DR, distributed IAM, scale/soak, or production release evidence | Local compatibility evidence | "Current tree passes the complete local compatibility and packaging gate"; not a hosted release approval or global objective completion |
| HA/DR quorum safety invariant | `HaDrTopology` requires an independent witness domain; `HaDrCluster` treats witness acknowledgement separately from healthy-voter quorum during failover/repromotion and rejects one-voter-plus-witness elections | `tests/test_ha_dr_quorum_simulation.py` passes 5/5 with schema/digest and tamper checks plus explicit co-location/quorum regressions; Ruff/Mypy/diff-check pass for changed files | Orchestration-neutral in-memory simulation only; no live PostgreSQL/network, external fencing, automatic failover, host independence, wall-clock RPO/RTO, or production SLO | Experimental / bounded simulation safety | "Enforces a fail-closed voter-quorum and independent-witness invariant in the HA/DR safety model"; not live HA/DR, automatic failover, RPO/RTO, or production readiness |
| Current PostgreSQL HA/DR repeated drill | `verify_postgres_ha_dr_repeated.py` runs three disposable PostgreSQL 17.10 primary/standby cycles with synchronous replication, partition write refusal, primary fencing, manual promotion/failback, encrypted backup/isolated restore, sentinel replay, and labelled-resource cleanup | `docs/execution/POSTGRES_HA_DR_REPEATED_VERIFICATION_2026-08-06.json` is schema-valid; all three runs report zero acknowledged transaction loss, cleanup passed, failover RTO 11.055–11.137s, failback RTO 0.959–0.968s, and `all_runs_passed: true`; latest artifact test and CI selection tests pass | Docker Engine 29.6.2, two containers in one host/failure domain, synthetic data/key, manual controller, no quorum/witness or host-loss independence | Experimental / bounded live drill | "Provides a reproducible single-host PostgreSQL synchronous-standby failover/failback and encrypted restore drill with zero observed loss in three runs"; not automatic HA, cross-host/zone DR, production SLO, or enterprise readiness |
| Final local gate after HA/DR safety hardening | Current tree including deferred-tax bridge and HA/DR quorum fix passes the complete repository/static/security/package checks | `uv run pytest -q --tb=short` exits 0 in 326.3s; Ruff, Mypy (448 files), Bandit, pip-audit, package build, and `git diff --check` pass | One Windows workstation with local dependencies; no hosted matrix, live provider/write-back, statutory close, independent HA/DR, distributed IAM, scale/soak, or production evidence | Local regression/package evidence | "Current tree passes the complete local regression and package gate"; not hosted release approval or global objective completion |
| Server-scoped PostgreSQL evidence reads | Evidence list/coverage/record/non-sensitive drill-down re-evaluate central `evidence.read` OR `evidence.manage` with request tenant/workspace/organization/legal-entity before the adapter; sensitive drill-down retains `evidence.manage` | `tests/test_api_server_evidence.py` and scope-boundary contracts capture exact four-part hierarchy for read/manage/verify calls; Ruff/Mypy/diff-check pass; ADR 0415 is packaged | Local server-profile route contract with a tenant-bound fake adapter; no worker/export/UI-wide adoption, federation, distributed invalidation, live providers, HA/DR, or production IAM evidence | Experimental / bounded IAM route control | "Re-evaluates central hierarchy policy before PostgreSQL evidence reads and writes"; not universal enterprise IAM, federation, live providers, HA/DR, or production readiness |
| Final local gate after evidence-read IAM adoption | Current tree including deferred-tax, HA/DR quorum, and evidence-read policy slices passes the complete repository/static/security/package checks | `uv run pytest -q --tb=short` exits 0 in 322.8s; Ruff, Mypy (448 files), Bandit, pip-audit, package build, and `git diff --check` pass | One Windows workstation with local dependencies; no hosted matrix, live provider/write-back, statutory close, independent HA/DR, distributed IAM, scale/soak, breadth, or production evidence | Local regression/package evidence | "Current tree passes the complete local regression and package gate"; not hosted release approval or global objective completion |
| Server-scoped legacy PostgreSQL audit views | `/audit/events` and `/audit/verify` re-evaluate tenant-wide `audit.read`/`audit.verify` before the tenant-scoped ledger adapter, with no synthetic workspace | `tests/test_api_server_audit_policy.py` plus server-identity/audit-administration tests pass 4/4 with one declared skip; Ruff/Mypy/diff-check pass | Local server-profile fake adapter and existing synthetic identity boundary; no worker/export/UI-wide adoption, federation, distributed invalidation, live providers, HA/DR, or production IAM evidence | Experimental / bounded IAM route control | "Re-evaluates central tenant policy before legacy PostgreSQL audit views"; not universal enterprise IAM, federation, providers, HA/DR, or production readiness |
| Final local gate after legacy-audit IAM adoption | Current tree including deferred-tax, HA/DR quorum, evidence-read, and legacy-audit policy slices passes complete repository/static/security/package checks | `uv run pytest -q --tb=short` exits 0 in 325.3s; Ruff, Mypy (448 files), Bandit, pip-audit, package build, and `git diff --check` pass | One Windows workstation with local dependencies; no hosted matrix, live provider/write-back, statutory close, independent HA/DR, distributed IAM, scale/soak, breadth, or production evidence | Local regression/package evidence | "Current tree passes the complete local regression and package gate"; not hosted release approval or global objective completion |
| Server-scoped legacy PostgreSQL Finance reads | Legacy tenant-scoped ledger summary/account/trial-balance/entry reads re-evaluate `finance_core.read` before adapter access; writes retain workspace-bound `finance_core.manage` | `tests/test_api_server_legacy_finance_policy.py` plus Finance Core/server-identity tests pass 6/6 with one declared skip; Ruff/Mypy/diff-check pass | Local server-profile fake adapter and synthetic identity boundary; no statutory posting, worker/export/UI-wide adoption, federation, distributed invalidation, live providers, HA/DR, or production IAM evidence | Experimental / bounded IAM route control | "Re-evaluates central tenant policy before legacy PostgreSQL Finance reads"; not statutory posting, universal enterprise IAM, providers, HA/DR, or production readiness |
| Final local gate after legacy Finance-read IAM adoption | Current tree including deferred-tax, HA/DR quorum, evidence-read, legacy-audit, and legacy-Finance policy slices passes complete repository/static/security/package checks | `uv run pytest -q --tb=short` exits 0 in 322.7s; Ruff, Mypy (448 files), Bandit, pip-audit, package build, and `git diff --check` pass | One Windows workstation with local dependencies; no hosted matrix, live provider/write-back, statutory close, independent HA/DR, distributed IAM, scale/soak, breadth, or production evidence | Local regression/package evidence | "Current tree passes the complete local regression and package gate"; not hosted release approval or global objective completion |
| PostgreSQL durable-job backpressure runtime gate | `reconforge/benchmark/postgres_durable_job_backpressure.py` drives four producer lanes and eight PostgreSQL workers through atomic `submit_bounded`, lease/checkpoint, and partition-effect contracts with a four-job queued/retrying cap per lane | Local disposable PostgreSQL 16 run and `tests/test_postgres_durable_job_backpressure.py` prove 64/64 completed jobs, 256/256 effects, maximum queue depth four, 12 rejected retries, zero duplicates/residue, and 16 completions per lane; hosted evidence is pending deferred publication | Windows 11/Python 3.14.6, one PostgreSQL 16 host, synthetic tenants and independent connections; no global quota fairness, throughput, capacity, soak, queue HA/failover, host loss, cross-host fairness, RPO/RTO, or production sizing | Experimental / bounded local runtime evidence | "Provides a bounded PostgreSQL durable-job backpressure and queue-drain correctness gate"; not a throughput, distributed fairness, HA/DR, SLO, or production-capacity claim |
| PostgreSQL grouped-matching 10K partition tier | `PostgresConnectionPool` bounds connection reuse; `reconforge/benchmark/postgres_grouped_matching_scale.py::ten_k_profile` drives 16 worker tasks over 1,000 runs, ten partitions per run, and five explicit grouped modes | Local artifact plus `tests/test_postgres_grouped_matching_scale.py` prove 10,000/10,000 checkpoints, 24,000/24,000 expected result rows, zero duplicate identities, zero failed/active runs, and 200 completed runs per mode; hosted evidence is pending deferred publication | Windows 11/Python 3.14.6, one PostgreSQL 16 host, bounded 16-connection pool, synthetic USD/FX/fee inputs; no throughput, capacity, soak, cross-host scheduling, queue HA/failover, provider/statutory posting, write-back, HA/DR, or production sizing | Experimental / bounded local PostgreSQL concurrency evidence | "Provides a reproducible PostgreSQL 10K-partition grouped-matching correctness tier with bounded connection reuse"; not a throughput, capacity, HA/DR, provider, posting, write-back, or production-sizing claim |
| Exact intercompany elimination proposal bridge | `intercompany-elimination-v1`, `IntercompanyEliminationApplicationService`, and `reconforge consolidation intercompany-eliminations` require explicit signed reporting-currency source lines, group-account mappings, reciprocal entity coverage, exact zero-sum balance, and source/result digests; unresolved groups are retained | `tests/test_intercompany_elimination.py` covers exact proposal balance, permutation stability, replay/tamper refusal, unresolved imbalance/one-way groups, currency refusal, closed JSON Schema, and CLI output; the full local suite and hosted Python 3.11/3.12 jobs pass | Local plus hosted synthetic CI evidence at commit `39ee3560`; no statutory/legal-book posting, live FX, provider interoperability, persistence parity, write-back, HA/DR, or production runtime | Experimental / bounded evidence | "Provides a deterministic non-posting bridge from explicitly mapped intercompany source lines to replayable elimination proposals; it is not statutory accounting, live provider integration, or production close readiness" |
| Signed connector package trust-plus-conformance admission | `load_verified_package_for_admission`, `admit_verified_package`, `VerifiedConnectorPackage`, and `reconforge connectors verify-package` bind Ed25519 publisher trust, manifest conformance, and canonical trust/manifest/signature digests without executing package code | `tests/test_connector_package.py` and `tests/test_connector_sdk.py` pass package authentication, tamper rejection, conformance rejection, trust rotation, read-only portfolio, and CLI checks | Local synthetic package/manifest boundary only; no live ERP/bank provider, executable loader, write-back, or marketplace deployment | Experimental / bounded evidence | "Provides data-only signed read-only connector package admission bound to an operator trust snapshot and conformance gate, with a reproducible CLI; it is not live provider interoperability or executable package marketplace readiness" |
| Governed write-back compensation request boundary | `connectors.writeback.compensate`, local migration 32, actor/scope/version-bound `POST /api/v1/connectors/writeback/intents/{intent_id}/compensate`, append-only intent metadata, and legacy digest compatibility reader | Focused write-back/domain/API/repository gates pass; authorization inventory is 232 routes with digest `d703c87206397de65a3dc4006e12c41f3818dfab518195c0670c1c3a3a5dc30e`; exact replay, wrong actor/reason, stale version, and invalid-state regressions are covered; hosted server-boundaries `92124197210` passes the live synthetic PostgreSQL identity/API fixture | Hosted synthetic PostgreSQL CI only; provider-neutral request boundary and explicit tenant permission provisioning, not a live ERP/bank provider | Experimental / bounded evidence | "Provides an actor-bound, scope-bound, optimistic-versioned compensation-request record without provider I/O; live ERP/bank reversal semantics, automatic compensation, signed packages, HA/DR, and production readiness are not established" |
| Governed write-back compensation dispatch boundary | Server-profile-only `/api/v1/connectors/writeback/intents/{intent_id}/compensate/dispatch`, privileged dispatch permission, explicit in-memory payload resolver, SHA-256 payload binding, provider acknowledgement gate, and idempotent compensated replay | Local API/transport tests cover missing resolver no-effect, payload digest binding, accepted acknowledgement persistence, and replay without a second provider call; route inventory is 233 contracts with digest `5b59764852e9aee179afe726beb2f911489c22fd6fbbcf55180e479f85be3666`; hosted server-boundaries `92134375855` passes the synthetic live PostgreSQL identity/API fixture | Synthetic injected transport and one disposable PostgreSQL CI server only; no live ERP/bank provider or accounting posting | Experimental / bounded evidence | "Provides a server-profile-only, digest-bound compensation dispatch contract with acknowledgement-gated persistence; it is not live ERP/bank reversal interoperability or production write-back" |
| Canonicalized actor identity in write-back maker-checker | `reconforge/connectors/writeback.py` uses `same_actor` in `approve_writeback`, `WritebackIntent.validate_state`, and `request_compensation`; write-back approval route requires tenant/workspace scope and distinct canonicalized actors for separation | `tests/test_connector_writeback.py` adds Hypothesis cases for canonicalized actor self-approval rejection plus focused regression around validated statuses; API/repository authorization and lifecycle slices remain covered in E-308 | Local API, repository, and domain invariant test runtime only; no provider interoperability, production writer/settlement, or live PostgreSQL claim | Experimental / bounded policy gate | "Rejects canonicalized actor self-approval for maker-checker write-back state transitions"; not live provider write-back, HA/DR, or production writing evidence |
| Open-source financial reconciliation and controls toolkit | MIT metadata; CLI, reconciliation, rules, reports, evidence code | Full locked Python 3.11 local suite: 986 collected, 976 passed, 10 skipped | `doctor`, `validate`, and local demo pass | Alpha | "Alpha-stage open-source reconciliation and financial-controls toolkit" |
| Works beside source systems; not a complete ERP | Read-only file ingestion and export-profile plugins; no general source posting engine | CLI/demo tests and compatibility tests | Sample export workflow runs locally | Alpha | "Works beside ERP/accounting/operational systems; not a replacement ERP" |
| Local file-first workflow | Pandas/SQLite/files are default paths; no mandatory provider call in demo | Full local suite passes; live-service tests remain skipped | Offline-style local demo completed in 5.541s | Alpha | "Core sample workflows run locally from files" |
| Current no-network sovereign deployment drill | Locked dependency mirror, digest-pinned Linux runtime, no-network/read-only installation, local identity recovery, encrypted backup/wrong-key refusal, and exact tagged application rollback are exercised by the real Docker drill | E-430 report is schema-valid: 68 entries, 100,386,256 bytes, doctor success, two users restored, zero old sessions, zero network upgrade inputs, and exact rollback; focused schema test and package membership pass | Connected bundle assembly, one Python 3.14.1 Linux runtime and one run; no signature trust, physical custody, OCI offline verification, hardware-backed keys, cross-platform repetition, HA/DR, or production readiness | Experimental / bounded sovereign runtime evidence | "Provides a reproducible bounded no-network Python-wheel install/recovery/rollback drill"; not physical air-gap custody, signed release trust, HA/DR, or production readiness |
| Deterministic reconciliation | Stable keys canonicalize Decimal scale; canonical multiset occurrence IDs distinguish identical copies; signature v3 enforces exact financial cells, includes instance identity, excludes source location, and E-665 canonicalizes stock/GL result-frame ordering by stable identity; optional unresolved-equal-cost-v1 fails closed on equal optima or bounded-search exhaustion; DuckDB partition sizing uses integer ceiling arithmetic and partition summaries retain domain order; hosted hard-key partitions use immutable idempotent checkpoints and expiring worker leases; supported-engine-parity-v1 configures a digested no-skip four-cell CI contract; bounded grouped one-to-many/many-to-many/FX/portfolio-fee decisions retain strategy and result digests; E-712 adds digest-bound reviewed per-run lower budgets | V1/v2 replay, v3 rejection/identity, E-665 permutation output-order regression, grouped strategy/property/mutation/replay contracts, E-712 budget ceiling/floor and non-silent rejection tests, generated plus explicit CSV parity cases, schema/digest-verified golden registry, simulated unhandled termination, and matrix/schema/workflow alignment tests cover current local strategies and configuration boundaries | Local strategy/worker contracts pass; CI server-boundaries runs `30817197467`, `30818624136`, and the subsequent E-337 gate exercise PostgreSQL RLS worker lineage for same-currency groups, fixed synthetic FX, and fee-aware partial portfolio matching. Live process/PostgreSQL crash and broader backend evidence remain bounded | Alpha / bounded evidence | "Versioned deterministic signatures, duplicate multiset identity, canonical stock/GL artifact ordering, bounded grouped matching with fee/FX/partial residual lineage, reviewed per-run lower budgets, generated/golden local checks, and explicit lease/checkpoint replay evidence are documented; scale, live-market rates, statutory posting, distributed recovery, and broader backend evidence remain pending" |
| Dense grouped-matching adversarial boundary | ADR 0381 adds deterministic cases combining equal-cost fee/netting alternatives, fee-aware FX conversion, permutation replay, search-budget exhaustion, and mixed partition/currency rejection; ADR 0383 adds fault injection after every non-terminal replay checkpoint; no case changes the existing domain/application contract | `tests/test_grouped_matching_adversarial.py` and `tests/test_grouped_matching_replay.py` pass 10/10; equal optima expose candidate sets, FX/fee totals remain exact under reversed input, budget refusal has empty selected IDs and stable digest, invalid mixed candidates remain unmatched, and fault points 1/2/3 reproduce the baseline effect digest with zero duplicate effects; Ruff/Mypy and manifest membership pass | Bounded synthetic algorithm/replay cases only; no fuzzing/mutation score, PostgreSQL parity, live-rate source validation, distributed queue failure, performance, posting, write-back, or production evidence | Experimental / bounded algorithm correctness and replay | "Keeps dense fee/FX grouped matching explicit, deterministic, fail-closed, and replayable at the declared SQLite checkpoints"; not complete matching assurance, distributed reliability, or production readiness |
| Grouped-matching property/fuzz campaign | `tests/test_grouped_matching_fuzz.py` generates finite Decimal amounts/fees, currencies, partitions, and dates at the public domain boundary; ADR 0440 records deterministic Hypothesis settings and rollback | Three generated tests pass with the existing grouped correctness, adversarial, replay, and mutation contracts (20 focused tests); permutation digests, closed selection, budget refusal, portfolio non-overlap, and replay digest stability are asserted | Synthetic in-process bounded property/fuzz evidence only; source mutation is covered separately by E-595; PostgreSQL parity, distributed queue fault coverage, live-rate validation, and production sizing remain open | Experimental / bounded property evidence | "Publishes bounded generated invariants for grouped matching"; not complete fuzzing, mutation assurance, distributed reliability, or production readiness |
| Grouped-matching targeted source mutation | `reconforge/benchmark/grouped_matching_source_mutation.py` copies only the target module into a disposable child package and applies three named source replacements; ADR 0441 defines the closed contract and rollback | `tests/test_grouped_matching_source_mutation.py` proves the unmutated contract passes and kills all 3/3 declared mutants with zero survivors; focused grouped suite reaches 27 tests | Targeted synthetic source-mutation evidence only; no domain-wide mutation score, PostgreSQL parity, distributed fault coverage, live-rate validation, or production sizing | Experimental / targeted mutation evidence | "Kills the three declared grouped-matching source mutants"; not a complete mutation score or production-readiness claim |
| Atomic durable-job lane backpressure | Additive `submit_bounded` application/repository contract; SQLite `BEGIN IMMEDIATE`; PostgreSQL lane advisory lock with forced-RLS scoped `queued`/`retrying` count; idempotent replay before cap | Focused SQLite contract proves rejection without mutation, replay while full, sibling-lane isolation, and capacity release after claim/cancel; CI run `30828746821` executes the same live PostgreSQL assertions under the non-privileged role | Synthetic single-node PostgreSQL CI runtime only; no throughput, distributed quota, fairness SLO, soak, HA/DR, or production-capacity evidence | Experimental / bounded runtime primitive | "Provides an atomic bounded durable-job submission primitive per tenant/workspace/entity lane with replay-safe backpressure; it is not a throughput, distributed fairness, HA/DR, or production-capacity claim" |
| Deterministic fair durable-job lane scheduling | `DurableJobLane`, exact workspace/entity claim filters, and process-scoped `RoundRobinDurableJobScheduler` preserve tenant/RLS, `SKIP LOCKED`, and lease-fencing boundaries | SQLite six-claim alternating-lane/no-cross-lane contract passes; CI run `30831774662` passes the matching live PostgreSQL server-boundary contract under the non-privileged role; Security `30831776530`, Docker `30831774950`, and CodeQL `30831774663` pass | Synthetic PostgreSQL single-node runtime only; local workstation has no live DSN; no distributed fairness, throughput, soak, HA/DR, SLO, or production-capacity evidence | Experimental / bounded runtime primitive | "Provides deterministic round-robin selection across explicitly configured durable-job lanes in one scheduler loop; it is not a distributed fairness, throughput, HA/DR, or production-capacity claim" |
| Bounded PostgreSQL multi-worker durable-job profile | `reconforge/benchmark/postgres_durable_job_scale.py` drives the existing PostgreSQL lease/checkpoint/effect repository with eight independent connections and four forced-RLS tenant lanes; `claim_next` rechecks active leases after locking the job row | Local shape/live profile passes; hosted server-boundaries `30845549418` passes the 64-job/256-effect contract with zero duplicates and drained queue/running depth; Security `30845549419`, Docker `30845549425`, and CodeQL `30845549444` pass | Synthetic one-node PostgreSQL 16 Alpine CI service only; timing is observational | Experimental / bounded runtime primitive | "Provides a bounded PostgreSQL multi-connection durable-job correctness profile with active-lease fencing"; do not call it throughput, capacity, distributed fairness, HA/DR, RPO/RTO, or production sizing |
| Bounded PostgreSQL outbox multi-worker delivery | `reconforge/benchmark/postgres_outbox_scale.py` drives the existing tenant-bound PostgreSQL outbox worker with four independent connections and a sink keyed by event ID | Local live profile plus existing outbox contract pass; hosted server-boundaries `30847668458` passes 64 unique published IDs with zero pending/claimed/dead rows; Security `30847668487`, Docker `30847668631`, and CodeQL `30847668940` pass | Synthetic one-node PostgreSQL 16 runtime only; injected in-process sink, no broker or external delivery | Experimental / bounded runtime primitive | "Bounded PostgreSQL transactional-outbox claim/publish/acknowledge profile"; do not call it broker semantics, crash-safe delivery, queue HA, throughput, or production delivery |
| Bounded PostgreSQL grouped-matching multi-worker profile | `reconforge/benchmark/postgres_grouped_matching_scale.py` declares four workers, 32 runs, 64 hard-key partitions, five grouped modes, mode-derived result cardinality, unique result identities, and canonical Decimal serialization | `tests/test_postgres_grouped_matching_scale.py` passes structural guards and a live non-superuser PostgreSQL 16 five-run/ten-partition variant with complete checkpoints, zero duplicate identities, zero failed/active runs, and effect/manifest digests; hosted CI `30854414579` server-boundaries job `91821932337`, Security `30854414587`, Docker `30854414580`, and CodeQL `30854414574` pass on code head `3ae39b68` | Synthetic one-tenant single-node PostgreSQL 16 runtime only; fixed USD/FX/fee inputs; observed runtime is not a throughput or capacity measure | Experimental / bounded runtime primitive | "Bounded PostgreSQL grouped-matching multi-worker correctness profile with checkpoint and result-identity invariants"; do not call it throughput, soak, production sizing, provider/statutory posting, queue HA, failover, or HA/DR |
| Exact Money/Currency foundation | `Money`, `MinorMoney`, versioned 165-policy offline registry, named legacy-v1/current strict-v2 input policies, 52 strict policy defaults across 23 runtime files, strict Money scalar operators, PostgreSQL reconciliation writer normalization and refusal, explicit-only historical legacy replay, AST guards, shared safe YAML plus exception-CSV/JSON lexeme preservation, explicit rounding/policy digests, strict stock/GL candidate amounts, and exact variance/report boundaries | Strict Python/NumPy rejection, repository-wide no-legacy-default and no-production-legacy-helper inventories, PostgreSQL writer default/rejection/no-mutation contracts, explicit legacy warning compatibility, exact/unquoted/safe-tag configuration and rule tests, current/default plus historical artifact schema/digest/tamper/source rechecks, exact review workbook policy/order, official coercion quality, currency/reconciliation/financial-CLI, variance/anonymizer/generator/Studio/report policy tests, a 190-test locked focus, and the full locked suite pass | Current and direct-default artifacts disclose strict policy plus bounded provenance; explicit historical readers remain. External-caller migration, config signing, supported-version/backend parity, source authentication, redaction assurance, and broader artifact coverage remain pending | Alpha foundation | "Financial-input defaults and current documented paths reject binary float before conversion; exact inputs and explicit versioned historical replay remain supported, without a universal backend or source-authenticity claim" |
| Invalid data stays visible | Validators, reconciliation data-quality exception paths, strict period fingerprints, and schema-2 evidence risk-score status | Validation/reconciliation, malformed period amount-versus-valid-zero, and malformed evidence-score regression tests | Sample validation reports 10 warnings and 0 hidden errors; strict period comparison records malformed amounts as invalid rather than zero; malformed explicit evidence scores render unavailable rather than zero | Alpha | "The sample workflow reports data-quality warnings explicitly; malformed values remain distinct from valid zero in the documented period/evidence boundaries" |
| Evidence artifacts with integrity metadata | Evidence binder/index, Evidence Graph v1, schema-v2 legacy plus strict-v3 exact risk-score/input policy and selected-source fingerprints, client-pack schema-v2 source/output fingerprints, and SHA-256 code | Evidence v2/v3 plus client-pack v1/v2 and Evidence Graph v1 schema/digest/tamper/tests and full local suite pass | Demo generated 14 evidence cases before this slice; strict boundary fixture preserves fractional score as explicit data-quality evidence and current CLI emits a verified v3 index | Experimental | "Generates local evidence/client-pack artifacts and checksummed evidence graph exports for review; hashes are not signatures, source authentication, stable evidence identity, audit opinions, redaction assurance, or disclosure approval" |
| Exact control-decision scores | Pure Decimal readiness/count-percentage calculation, exact PostgreSQL full-readiness check, evidence integer-score policy, and canonical metric `value_text` | Hostile-context readiness/metric, near-100 lock rejection, invalid/fractional/scientific evidence-score, and invalid stored-readiness tests | Local focused control/evidence/metric suites pass; live PostgreSQL remains unverified locally | Alpha / bounded evidence | "Exact score handling is verified for evidence-binder selection, count-derived close readiness, and canonical governed metric text; this is not a certification or statutory-close claim" |
| Local SQLite backup/restore | Version/checksum validation, allowlisted parameterized restore, lifecycle replay, FK verification, database-owned omitted-column defaults, and optional operator-keyed AES-256-GCM envelope | Backup/restore, encrypted round-trip/tamper/wrong-key, DB bridge, legacy additive-column, exact default delegation, and enterprise DB tests pass | Local test databases restore without overwriting an existing target by default; temporary-file secure deletion, key lifecycle, and host-loss DR are unverified | Alpha / local only | "Provides tested local SQLite backup/restore with optional authenticated encryption and schema-version controls; this is not an enterprise DR guarantee" |
| React Studio | `apps/web` React/Vite client over clearly separated synthetic artifacts, bounded browser-local Mapping/Rule foundations, a same-origin `metrics.read` live contract, and an axe-core cross-route accessibility gate | 42 unit/component tests and 10 Chromium E2E tests pass; WCAG 2 A/AA and WCAG 2.1 A/AA automated scans cover all seven native routes in English and Arabic/RTL plus both mobile directions, focus, preferences, masking, and 403/no-fallback behavior | Production build passes; `/live` preserves exact metric text and distinguishes loading/empty/stale/error/ready without synthetic fallback; no surface publishes | Experimental | "Experimental read-only React Studio with bounded local Mapping and Rule previews, an authorized fail-closed live metrics view, and automated bilingual accessibility regressions; this is not independent WCAG conformance, manual assistive-technology validation, deployed identity, durable persistence, publication, ingestion, or live-service assurance" |
| Optional DuckDB execution | DuckDB engine and partition path exist; partition aggregation preserves the default summary order | Fixed and generated full-scan/forced-partition parity, duplicate/invalid, and row-permutation tests pass | Local proof uses DuckDB 1.5.5; 1K benchmark completed on stated hardware | Experimental | "Optional experimental DuckDB engine with bounded parity evidence on the disclosed version; not a scale or support-matrix claim" |
| Optional PostgreSQL/Redis server profile | 52 Alembic revisions, tenant-bound adapters, durable principal scope grants, workspace/entity-scoped business APIs and workers, authorized RLS control-plane exports, composed direct and parent-derived business RLS, hierarchical execution and object-artifact scope, federation, SCIM, service-account, privileged-session, emergency-authority, WebAuthn public-key storage, durable scheduling, optional notification routes/transports, a human/MFA-governed count-only security overview, authoritative PostgreSQL user/session/role/policy lifecycle, and native integration/retention governance plus CI service definitions cover the current single-node Team boundary | Phase 1 parity inventory, isolated migration, backup/restore, application repositories, hierarchy/job/export/object-store/business/API-scope/identity/federation/SCIM/service/step-up/emergency/WebAuthn/scheduler/notification/security/identity/access/governance and forced-RLS contracts pass at their documented scopes; E-193 verifies runtime disable across four native resources and monotonic append-only retention metadata | E-193 migrated fresh to 0052 and passed live non-superuser NOBYPASSRLS HTTP evidence for human step-up, signed pagination and tamper denial, sibling exclusion, disclosure controls, four runtime disable paths, stale state/version denial, idempotent non-shortening retention, retirement denial, direct-SQL floor enforcement, and audit-failure rollback. External providers remain synthetic; object-lock propagation, consolidated audit browsing, accessible UI, HA/DR, compliance, and external assurance remain absent. Prior source-built MinIO localhost object isolation is recorded | Experimental / single-node evidence | "Optional single-node PostgreSQL Team foundations, documented multi-entity isolation, durable hosted polling, default-off redacted notifications, a disclosure-bounded human/MFA security overview, authoritative identity/access lifecycle, and native integration/retention metadata governance have live synthetic evidence; accessible UI, audit browsing, real providers, object-lock propagation, Redis delivery, HA, managed deployment, hosted scale, production readiness, compliance, and external assurance remain unverified" |
| Authenticated scoped control-plane export over HTTP | The PostgreSQL `/api/v1/exports/scoped` route resolves the authenticated hierarchy, re-evaluates `reports.read`, snapshots through forced-RLS, and returns a deterministic digest-bound envelope; E-692 adds a real service-account request with explicit workspace/organization/entity grants | `tests/test_api_server_scoped_exports.py::test_live_server_scoped_export_http_is_service_scoped_and_tenant_isolated` passes against disposable PostgreSQL 16.14 with sibling-workspace 403, sibling-tenant 401, digest parity, and guarded cleanup; the phase-4 workflow contract and standalone hosted selector are locked | One-host synthetic local HTTP/PostgreSQL/RLS evidence only; hosted execution, provider object storage/KMS, replication, live ERP/bank behavior, write-back, independent HA/DR, and production IAM remain unverified | Experimental / bounded HTTP IAM evidence | "Provides a deterministic authenticated PostgreSQL scoped-export route with tested hierarchy denial; this is not universal enterprise authorization, provider durability, live integration, HA/DR, or production readiness" |
| Evidence-bounded OIDC/SAML assertion federation | Static public-JWKS OIDC via joserfc; fixed-certificate signed SAML via python3-saml/xmlsec; bounded operator JSON; server-issued hashed one-time challenge; forced-RLS replay/link/audit; prelinked local-role subset; existing hash-only session/logout | Real RSA OIDC and real XML-signature SAML traverse HTTP; issuer/audience/signature/time/correlation/failure/replay/air-gap/private-material/role-widening/tenant contracts pass | Disposable PostgreSQL 17.10 non-superuser HTTP lifecycle proves challenge, OIDC login, `/me`, logout/revocation, replay denial, sanitized audit and tenant isolation | Experimental / evidence-bounded | "Provides optional bounded direct OIDC/SAML assertion verification tied to pre-provisioned local authority; authorization-code exchange, discovery, IdP-initiated SAML, SCIM, MFA, provider single logout, hosted interoperability, independent review, and Enterprise readiness are not claimed" |
| Bounded SCIM lifecycle persistence | Closed RFC 7643 User/Group subset plus migration 0035 forced-RLS tenant/domain storage, stable IDs/versions, concurrent external-ID serialization, role-free local identities, same-domain membership, sanitized audit, and atomic deactivation/session revocation | JSON/hostile/application/schema/ETag tests and a non-superuser concurrency, replay, deactivation, no-role, session, and RLS lifecycle pass | Disposable PostgreSQL 17.10 migration and lifecycle evidence exists; authenticated SCIM HTTP, filters, PATCH/ETag enforcement, discovery, provider interoperability, MFA, and independent review remain absent | Experimental / persistence-only | "Provides bounded tenant/domain SCIM lifecycle persistence with role-free groups and atomic deactivation/session revocation; no SCIM HTTP interoperability, enterprise identity, or production-readiness claim is made" |
| Docker packaging | Digest-pinned base image and Docker workflows exist | GitHub workflow definition exists | Local daemon unavailable; no local image run | Unverified locally | "Docker assets are provided; verify them in a running Docker environment" |
| 1K benchmark behavior | Benchmark harness and versioned `benchmarks/small_1k` files | Engine tests include parity cases | Pandas 0.9930s; DuckDB 1.1568s on stated hardware | Measured only | "Measured on the documented 1K synthetic dataset and hardware" |
| Millions of transactions / enterprise-ready / bank-grade | No sufficient end-to-end evidence | No qualifying release, scale, DR, or independent validation gate | None | Planned/not allowed | Do not use these claims |
| Compliance or certification | Documentation explicitly disclaims assurance | No independent assessment | None | Not claimed | "Provides evidence/control tooling; does not confer compliance or certification" |
| Evidence-bounded security architecture | Schema-v2 registry defines Community/Team/Regulated boundaries, six data classes, seven trust boundaries, ten bounded/partial controls, role ownership, evidence paths, and normalized residual-risk links | Closed-schema, reference, evidence-path, edition-label, unsupported-claim, and risk owner/rating parity contracts pass | Repository evidence only; control operating effectiveness, public-internet hardening, independent assessment, and regulated deployment are not verified | Alpha / internal governance | "Maintains a versioned evidence-linked security architecture; implemented labels are bounded and do not imply a secure deployment, compliance, certification, or production readiness" |
| Evidence-bounded module threat models | Schema-v1 index matches all nine active runtime modules and links scoped assets, actors, boundaries, threats, controls, tests, owners, assumptions, residual risks, limitations, and out-of-scope capabilities | Closed schema, exact registry parity, unique/closed reference, existing evidence-path, residual-boundary, and readable-view contracts pass | Repository evidence only; threat mitigation in a deployment, penetration testing, continuous control operation, and independent validation are not verified | Alpha / internal governance | "Maintains a versioned threat-model index for every active module; case statuses are bounded/partial/deployment-dependent/planned and do not assert a secure deployment" |
| Scoped stable OWASP ASVS mapping | The normative mapping pins official OWASP ASVS 5.0.0 source identity and SHA-256, lists all 17 chapters, and assesses 55 selected requirements as 4 implemented, 33 partial, 10 planned, and 8 not applicable; the other 290 requirements are explicitly unassessed | Closed-schema, source-pin, chapter/count, selected ID/level, status-total, evidence-path, claim-boundary, and readable-view contracts pass | Repository evidence only; no deployment assessment, penetration test, independent validation, ASVS level, compliance, or certification is established | Alpha / internal governance | "Maintains a scoped, version-pinned evidence mapping for 55 selected OWASP ASVS 5.0.0 requirements; 290 requirements remain unassessed" |
| All-task final NIST SSDF mapping | The normative mapping pins official final SP 800-218/SSDF 1.1 PDF/table identities, excludes draft SSDF 1.2, records SP 800-218A as a separate profile assessment, and covers all 4 groups, 19 practices, and 42 tasks as 28 partial and 14 planned with owners, evidence/tests where positive, gaps, actions, and 90-day review cadence | Closed-schema, exact source/status/count/group/practice/task, status-total, owner/evidence, review, claim-boundary, and readable-view contracts pass | Repository evidence only; no organizational interviews, operating-process assessment, independent review, release signing, deployment evidence, SSDF conformance, compliance, or certification is established | Alpha / internal governance | "Maintains a version-pinned, all-task repository evidence/gap mapping for final NIST SSDF 1.1; every task remains partial or planned" |
| SLSA provenance implementation plan | The normative plan pins official Approved SLSA 1.2 tag/commit and defines exact source/wheel/sdist/image/SBOM identities, seven trust boundaries, in-toto Statement v1/SLSA provenance v1, independent verification/failure codes, evidence-preserving rollback, and 12 owned gates; candidate/SBOM code and tests make eight partial while four remain planned | Closed-schema, exact spec/track/artifact/boundary/attestation/failure/gate/status/owner/evidence/review/claim/readable-view contracts pass | No release-bound signed attestation, trusted-builder assessment, retained hosted verifier execution, source-control assessment, revocation drill, level, or verified property is established | Alpha / implementation governance | "Maintains a version-pinned SLSA 1.2 provenance implementation plan; Build and Source tracks are UNEVALUATED" |
| Signed release candidate execution | Tag-only workflow, six hash-locked build tools, commit-epoch sdist normalization, exact source/wheel/sdist/image manifest, keyless attestations, strict bundle verification, least-privilege permissions, and human-gated publication | Manifest/normalizer/workflow contracts pass; exact-main CI/Security/Docker/CodeQL/Scorecard pass; run `30243819239` passes every build/scan/attest/verify step on GitHub-verified signed tag `v0.7.1` | Retained artifact `8644255664` checksums and file/image provenance independently verify against repository/workflow/tag/revision/GitHub-hosted-runner expectations; no GitHub Release, PyPI publication, OCI reproducibility, SLSA level, compliance, certification, or production-readiness evidence | Alpha / executed non-publishing delivery control | "Produces a verified non-publishing v0.7.1 release candidate with exact subject provenance; immutable release publication and broader assurance remain unverified" |
| Per-artifact CycloneDX SBOM execution | Release-integrated builder binds CycloneDX 1.7 source/wheel/sdist/image documents to exact release-manifest subjects, enforces Python metadata parity, inventories matching npm-lock versions in source, and now uses pinned Syft 1.51.0; retained v0.7.1 evidence used Syft 1.49.0 | Determinism/schema/tamper/workflow tests pass; hosted run `30243819239` generated and attested four exact-subject SBOMs under the retained historical generator | Downloaded source (239 components), wheel (30), sdist (30), and exact image (3972) SBOMs, five checksum records, and four separate predicate bundles independently verify for v0.7.1; current 1.51.0 hosted generation is unexecuted, completeness remains `unknown`, and no immutable publication, compliance, certification, or production-readiness assurance follows | Alpha / executed historical plus updated definition | "Provides verified subject-bound CycloneDX 1.7 SBOMs for the v0.7.1 candidate and defines a pinned 1.51.0 generator for future candidates; completeness and dependency safety are not claimed" |
| Fail-closed exact-image container security gate | Closed policy pins Syft 1.51.0 and Grype 0.117.0 identities, native schema/configuration, database schema/age, exact image config/manifest/platform binding, severity/exception rules, fixed-only hash-bound OpenVEX with a 30-day review ceiling, 90% license-inventory coverage, recurring execution, pre-registry ordering, post-push config binding, and digest-pinned disposable service images | Dedicated schema/pass/block/critical/exception/stale/mismatch/governed-VEX/ungoverned-suppression/operational/license tests plus supply-chain/workflow drift tests pass | `CONTAINER_SECURITY_LOCAL_2026-08-22.json` v2 records Alpine 3.24.1, 68 packages, 94.11% license inventory, database v6.1.9, three exact source-proven CPython `fixed` dispositions, and two unexcepted OpenSSL High blockers; status is `blocked`, so no registry publication or release-readiness claim is allowed | Alpha / locally executed blocking control | "The current local image is subject-bound; three CPython matches are recorded as source-proven fixed and two OpenSSL matches still block release. License coverage is inventory only, and no hosted, legal, or production assurance is claimed" |
| Locked dependency and secret-gate definition | Universal `uv.lock` closes 128 Python runtime/server/observability/backup/federation/MFA/build/tool packages under exact uv/cutoff/archive identity; Docker and normal/server/security/candidate CI consume it with `--locked`; closed policy/exception registries bind weekly four-ecosystem updates, hash-exported Python and npm audits, and checksum-pinned redacted history/tree Gitleaks before registry authentication | Closed schemas, lock/source/hash/npm-gap, workflow-order/pin, exception lifetime/approval, critical-ban, and drift contracts pass; actionlint passes | Hosted head `a4a35f8` passes CI `30856023045` (Python 3.11/3.12, server-boundaries, parity, Docker), Security `30856022993`, Docker `30856022975`, and CodeQL `30856022972`. The prior 49.0.0 cryptography advisory is remediated by 50.0.0/pyOpenSSL 26.4.0 with no exception; branch/release operation, npm SRI completeness, provenance, and broader package assurance remain time-bounded | Alpha / hosted dependency remediation | "Defines a 128-package locked Python/server/observability/backup/federation/MFA resolution and fail-closed gates; the cryptography advisory is remediated and the committed CI/security matrix is green, while broader package assurance and release operation remain unverified" |
| Current exact secret-scan closure | `.gitleaks.toml` retains default Gitleaks rules and bounded generated-path exclusions; `.gitleaksignore` contains only exact historical and checked-tree fingerprints for synthetic test literals | E-592 records checksum-verified Gitleaks 8.30.1 history scan over 602 commits with zero findings and a zero-finding 25.12 MB clean-archive checkout scan; the generated local workspace timeout is explicitly excluded from evidence | Local checkout/archive only; hosted security attestation, branch protection, credential-vault operation, and external secret safety remain unverified | Experimental / bounded security evidence | "The current local history and clean archived tree pass the configured Gitleaks scan with exact synthetic-fixture fingerprints"; not a hosted security or credential-safety claim |
| Bounded tabular file ingress | `tabular-file-ingress-v1` is invoked before canonical pandas and direct DuckDB parsing and before FI-012 CSV fingerprint/output work; schema-v1 inventory records 13 surfaces and exactly allowlists all direct pandas/stdlib delimited-parser locations, including both client-pack `DictReader` passes | 26 dedicated ingress/CSV-redaction cases plus five inventory contracts cover size/row/column/cell/field budgets, symlinks, type mismatch, archive paths/duplicates/encryption/compression/decompression, active content, DTD/entities, external relationships, formulas, sparse columns, aggregate sheets, mapping/plugin/DuckDB routing, duplicate CSV headers, exact quoted/multiline values, streaming order, safe errors, pre-output rejection, per-file rollback, schema/repository/runtime-policy parity, and AST drift; full 986-test suite passes | Locked Python 3.11.15 full suite and 102-case combined boundary pass; the 80-case focused compatibility target also passes on Python 3.11.15 and ambient Python 3.14.6. No HTTP upload, malware scanner/quarantine, hosted run, or hostile legacy XLS-internal test exists | Alpha / bounded partial control | "Canonical local CSV/XLS/XLSX reconciliation, mapping, DuckDB, built-in CSV adapter, and FI-012 CSV-redaction paths use bounded preflight; FI-012 redistribution also uses the separately documented bounded staging policy, while legacy XLS internals, remaining report/restore/Studio readers, malware scanning, upload, connector, and authenticity controls remain incomplete" |
| Bounded structured-document ingress | `structured-document-ingress-v1` bounds JSON/YAML bytes and object/event resources; FI-010 configuration/mapping/control/Reconciliation-as-Code, FI-011 close-workflow, two FI-012 manifest readers, and exact-lexeme FI-012 JSON redaction route through ambiguity/unsafe/over-budget rejection; JSON redaction preflights before fingerprint hashing and destination preparation; the closed inventory exactly allowlists the two central direct PyYAML parser locations | 54 dedicated hostile/compatibility cases plus five inventory contracts cover file/type/encoding/link, size/node/depth/collection/scalar/alias budgets, JSON/YAML duplicate keys, huge numeric conversion, exact fractional/exponent lexemes, non-finite values, merge/cycle/multiple documents, unsafe/binary tags, safe generic close/manifest/redaction errors, output non-mutation, runtime-policy parity, AST drift, valid packs/templates, current/legacy manifest verification, and strict/legacy redaction buckets; the full 986-test suite passes | Locked Python 3.11.15 full suite passes in 373.85s as part of E-061; the 102-test combined boundary also covers adjacent tabular/copy controls. No remaining report/restore/Studio parser, malware, source-authenticity, disclosure-approval, redaction-completeness, semantic-correctness, or hosted claim is made | Alpha / bounded partial control | "Selected local configuration, mapping, control-pack, Reconciliation-as-Code, close-workflow, client-pack/evidence manifests, and client-pack JSON-redaction inputs use a bounded safe parser; other file surfaces remain partial, and these controls do not prove malware absence, authenticity, approval, redaction completeness, or safe redistribution" |
| Bounded staged client-pack redistribution | FI-012 freezes bounded candidate/selected sets; rejects symlink/special, excessive, invalid-UTF-8, overlong-line, oversized and aggregate-over-budget inputs; streams redacted text and non-redacted bytes; bounds output rechecks; and builds/rechecks the complete pack in a sibling staging directory. Existing-output publication writes a path-minimized schema-v1 marker binding exact sibling names and bounded old/staged tree digests; explicit recovery accepts four closed states | 30 dedicated hostile/resource/race/publication/recovery/compatibility/AST contracts plus existing JSON/CSV/manifest/CLI tests cover pre-hash rejection, frozen late-file exclusion, handled rollback, direct and generator-unwinding simulated interruptions, four recovery states, marker/tree tamper, unknown/multiple siblings, reparse refusal, stale-file removal, path rebasing, supported nested-demo output, and output-verifier ceilings | Current E-062 focused and full locked evidence is recorded in EVIDENCE.md. Fresh output uses one rename; existing output uses two renames and remains non-observer-atomic/non-crash-atomic. The unkeyed marker is not authenticated and real host/filesystem loss is not tested | Alpha / bounded partial control | "Client-pack selected copies use fixed resource budgets, complete-pack staging, handled-failure rollback, and explicit integrity-checked local interruption recovery; this does not prove authentication, crash-atomic replacement, real host/filesystem-loss durability, malware absence, source authenticity, disclosure approval, redaction completeness, or safe redistribution" |
| Bounded database backup and restore | FI-009 restore routes both adjacent SQLite JSON files through the bounded structured profile; optional AES-256-GCM authenticates the pair before mutation. PostgreSQL uses fixed native-tool argv, streaming AES-256-GCM, exact permission gates, a new restore database, post-restore schema checks, and failure cleanup | SQLite hostile/schema/TOCTOU/non-mutation/current/version-six/encryption cases and seven PostgreSQL adapter authorization/integrity/rollback cases pass; a PostgreSQL 17 native drill restored Alembic 0015 and 41 ReconForge tables and removed a failed partial restore | E-063/E-103/E-104 local evidence only. PostgreSQL service-file custody, actual adapter execution across platforms, KMS/key rotation, HA, host loss, cross-version upgrades, and measured RPO/RTO remain unverified | Alpha / bounded partial control | "SQLite and one PostgreSQL 17 profile have bounded encrypted backup/restore controls and local rollback evidence; enterprise HA, regulated, and DR readiness remain planned" |
| Hosted PostgreSQL native-tool bootstrap contract | `server-boundaries` installs the PostgreSQL 16 client package matching its pinned PostgreSQL 16 service before locked dependencies, fails closed on `pg_config`/`pg_dump` major drift, and checks `pg_dump`, `pg_restore`, `createdb`, `dropdb`, and `psql` under `pg_config --bindir`; ADRs 0447, 0464, and 0494 plus the workflow contract test preserve this ordering and exact path boundary | `tests/test_phase4_execution_contract.py::test_server_boundaries_bootstraps_versioned_postgres_native_tools_before_live_tests` passes with package/version assertions; Ruff and diff-check pass | Local workflow and compatibility evidence only; no fresh hosted encrypted backup/restore run, cross-platform native-tool evidence, HA/DR, RPO/RTO, or production release evidence | Local release-operation contract | "The hosted server-boundaries workflow explicitly selects and checks PostgreSQL 16 native tools before its live gate"; not proof that a hosted run succeeded or that backup/DR is production-ready |
| PostgreSQL native-tool suffix-aware verifier hardening | `verify_postgres_native_tools.py` now resolves required binaries from `pg_config --bindir` using suffix candidates and validates that PATH resolves to the same versioned binaries (including Windows `.exe` naming) before allowing live gating | `tests/test_postgres_native_tools.py` adds dedicated coverage for suffixed `pg_config`/`pg_dump` discovery and keeps existing major-version and PATH-drift rejection checks | Local proof only; no hosted runner matrix is provided and no runtime backup/restore recovery evidence is attached to this specific gate | Experimental / bounded release-gate hardening | "The PostgreSQL native-tool verifier accepts Windows executable variants while keeping version-pin and fail-closed PATH-bindir checks; this does not prove hosted bootstrap repair, cross-platform parity, or backup/DR readiness" |
| PostgreSQL native dump wrapper resilience | `PostgresNativeBackupAdapter` requires a non-empty dump after `pg_dump` and performs one bounded equals-form `--file=<path>` retry only after a successful invocation produced no usable file; a second absence fails closed | `tests/test_postgres_backup.py` passes normal encrypted backup, one portable retry, and double-absence refusal (11 passed, one declared native-tool skip) | Local fake-runner injection only; no hosted toolchain, platform wrapper, independent HA/DR, restore RPO/RTO, or production backup evidence | Experimental / bounded resilience | "Hardens the PostgreSQL native dump argument boundary with one bounded retry"; not a hosted-CI repair, HA/DR, RPO/RTO, or production backup claim |
| Project risk governance | Schema-v1 normalized R-001..R-018 source with role owners, scoring method, mitigations/evidence, residual actions, and review cadence | Schema, sequential-ID/rating, evidence-path, date-cadence, and Markdown-parity tests pass | Governance files verified in sdist only | Alpha / internal governance | "Maintains a schema-validated project risk register; this is not an independent assessment, GRC product, or certification" |
| Evidence-bounded maturity labels | Schema-v1 product/module ceiling policy linked to this matrix; all nine runtime modules are Experimental | Exact module coverage/rank, synthetic promotion rejection, claim-reference, and seven-surface wording tests pass | Policy/schema verified in sdist only | Alpha / internal governance | "Public product wording is capped at Alpha/foundation stage and current module metadata at Experimental; this is not release-readiness evidence" |
| Bounded legacy database JSON imports | FI-009 account/control imports use `database-legacy-import-json-ingress-v1` before database inspection: stable regular non-reparse reads, strict duplicate/non-finite JSON, 16 MiB/file, 500,000 nodes, depth 32, 50,000 collection items, 1 MiB scalars, unambiguous direct-list/single-envelope/keyed-map shapes, and parsed-byte digest/size/profile evidence | 25 dedicated hostile/resource/ambiguity/TOCTOU/schema/compatibility/AST cases plus four existing DB export/import cases pass; published account/control schemas accept each documented compatibility shape and reject multiple aliases | Current E-064 focused/full locked evidence is recorded in EVIDENCE.md after final gates. Individual legacy record fields remain permissive; the unkeyed digest is not authentication, authorization, malware scanning, encryption, or DR evidence | Alpha / bounded compatibility control | "Local account/control DB import JSON uses bounded duplicate-safe validation and visible shape errors before mutation; this does not prove source authenticity, semantic completeness, centralized authorization, malware absence, encryption, or recovery readiness" |
| Bounded business-record ingress | FI-008 CSV/JSON entrypoints route through `business-record-ingress-v1`: stable regular non-reparse fingerprints, 64 MiB/file, 250,000 records, 512 fields/record, 10,000,000 cells, 128,000-character fields/scalars, JSON 2,000,000-node/depth-32 ceilings, duplicate/non-finite/ambiguity rejection, exact fractional lexemes, and parsed-byte digest/size/profile evidence | 21 dedicated hostile/exactness/resource/TOCTOU/compatibility/AST cases plus 35 existing account/control/matching/inventory/receivables/atomic service cases pass in the current focused run | Final E-065 locked/full/package evidence is recorded only after the exit gates below. Domain schemas remain service-specific, and unkeyed hashes do not authenticate, authorize, classify, or malware-scan inputs | Alpha / bounded parser control | "Local platform business-record CSV/JSON uses bounded duplicate-safe parsing and exact fractional text with parsed-byte provenance; this does not prove business semantics, source authenticity, centralized authorization, malware absence, or supported throughput" |
| Bounded Studio generated-artifact ingress | FI-007 routes all four direct CSV reads and its JSON helper through `generated-artifact-ingress-v1`: stable regular non-reparse fingerprints, 64 MiB/file, 250,000 rows, 512 columns, 10,000,000 cells, strict headers, JSON 1,000,000-node/depth-64 ceilings, duplicate/non-finite rejection, exact fractional lexemes, safe empty views, and optional companion shape/count checks | 24 dedicated hostile/resource/TOCTOU/companion/display/route/AST cases plus 36 existing Studio compatibility/auth/page cases and five exact inventory contracts pass in the current focused run | Final E-066 locked/full/package evidence is recorded only after exit gates. CSV-only output remains readable; companions do not digest CSV bytes, and unkeyed fingerprints do not authenticate, authorize disclosure, classify, or malware-scan content | Alpha / bounded parser control | "Current local Studio CSV/JSON views use bounded stable parsing and optional companion consistency while retaining historical display typing; this does not prove cryptographic binding, source authenticity, disclosure approval, malware absence, or supported throughput" |
| Bounded generated report ingress | FI-006 routes variance CSV/JSON and management-report JSON through `generated-artifact-ingress-v1`; CSV mode is explicit and variance selects exact text before Decimal validation, while shared budgets, ambiguity rejection, reparse refusal, and stable fingerprints apply | Dedicated exact-text/display/invalid-mode, hostile CSV/JSON, resource-profile, management fail-closed, and AST cases plus variance/report/Studio/inventory compatibility tests pass | Final E-067 locked/full/package evidence is recorded only after exit gates. Unkeyed fingerprints establish local byte consistency, not producer/schema authenticity, disclosure approval, classification, malware absence, or supported throughput | Alpha / bounded parser control | "Selected local variance and management report inputs use bounded stable parsing with exact variance numeric lexemes; this does not prove producer authenticity, schema authorization, malware absence, or supported throughput" |
| Bounded evidence/review CSV ingress | FI-005 evidence and review readers use explicit exact-text/display modes under shared generated-artifact limits; the binder parses each selected CSV once and evidence-index v3 fingerprints use and finally verify the cached parsed-byte checksum/size | Dedicated hostile/resource/mode/cache/provenance/change/CLI/AST cases plus evidence, review, period, financial-policy, and inventory compatibility tests pass | Final E-068 locked/full/package evidence is recorded only after exit gates. Review-state JSON and authenticated producer/schema/disclosure/malware controls remain outside this CSV slice | Alpha / bounded parser and evidence-integrity control | "Selected local evidence/review CSV inputs use bounded stable parsing and current evidence v3 fingerprints name the parsed CSV bytes; this does not prove producer authenticity, disclosure approval, malware absence, or supported throughput" |
| Bounded review-state JSON compatibility ingress | FI-014 uses `review-state-json-ingress-v1` with regular-file/stable-byte checks, strict UTF-8, duplicate/non-finite rejection, bounded graph resources, and tolerant normalization only after a valid object is parsed | Dedicated profile/compatibility/hostile/resource/change/CLI/Studio/DB/evidence/AST cases plus existing review, period, and DB compatibility tests pass | Final E-069 locked/full/package evidence is recorded only after exit gates. Hashes remain unkeyed and producer/schema identity, actor/workspace authorization, disclosure, malware, and supported throughput remain outside the slice | Alpha / bounded parser control | "Local review-state JSON is bounded and duplicate-safe while valid legacy entry coercion remains readable; this does not authenticate the producer or actor, authorize disclosure, prove malware absence, or establish supported scale" |
| Closed direct filesystem JSON parser inventory | The schema-v1 inventory and exact AST test classify every direct stdlib JSON call; all six former filesystem calls use bounded stable readers with recorded display/exact-text modes, while persisted fields and packaged registry text are explicit exclusions | Dedicated root-shape/exactness/change/compatibility/duplicate/resource/CLI/AST cases plus period, rule, dashboard, enterprise-demo, Studio-demo, review, and inventory contracts pass | Final E-070 locked/full/package evidence is recorded only after exit gates. The allowlist proves repository-visible call coverage, not semantic schema completeness, producer/actor authenticity, malware absence, or future connector safety | Alpha / closed parser-call inventory and bounded current file readers | "Current repository direct JSON parser calls are closed by an exact inventory, and current filesystem JSON readers are bounded and representation-explicit; this is not source authentication, authorization, malware assurance, or a future-upload claim" |
| Bounded AP/AR financial idempotency JSON | `financial-idempotency-json-v1` binds both AP/AR writers and readers to canonical object text, unique keys, integer-only number tokens, and fixed byte/graph limits; producer rejection remains inside the rollback boundary | Dedicated schema/canonical/hostile/resource/float/cycle/replay-before-effect/producer-rollback/AST tests plus AP, AR, backup, export, and migration contracts pass | Other direct FI-013 decoders were subsequently migrated, but producer/source authenticity and broader persisted-state semantics remain separate | Alpha / bounded persisted pair | "AP/AR idempotency responses use a bounded integer-token-only JSON contract and rejected producer/replay values do not create a new financial effect; this is not complete persisted-state coverage or producer authentication" |
| Bounded PostgreSQL reconciliation JSONB | Four named rule/attributes/lineage/evidence profiles share `postgres-reconciliation-object-v1`; producers and repository/worker consumers enforce finite unique-key object and graph budgets while JSONB rule text is re-canonicalized before established fingerprints | Dedicated schema/canonical/hostile/resource/pre-SQL/pre-hash/worker-before-matcher/AST tests plus PostgreSQL reconciliation and API compatibility targets pass | Local simulated records only until live PostgreSQL gates run; producer authentication and business-semantic completeness remain open | Alpha / bounded persisted PostgreSQL pair | "PostgreSQL reconciliation JSONB uses bounded field-specific producer/consumer profiles and corrupt rows fail before matcher or public use; this is not live-service, provenance, scale, or complete persisted-state evidence" |
| Bounded SQLite matching-rule JSON | `sqlite-matching-rule-json-v1` binds the one producer text to both matching tables and validates idempotent replay/public readers under finite unique-key object and graph budgets while preserving spaced historical bytes and policy defaults | Dedicated schema/canonical/hostile/resource/pre-transaction/replay-before-effect/public-reader/compatibility/AST tests plus matching-policy targets pass | Structural local SQLite evidence only; rule semantics and stored-row authenticity remain separate | Alpha / bounded persisted SQLite pair | "SQLite matching rules use a bounded producer/consumer JSON profile and corrupt rows fail before replay or public return; this is not provenance, scale, or complete persisted-state evidence" |
| Bounded tenant-scoped Redis session JSON | `redis-session-json-v1` binds the producer/consumer to a closed four-string object with a token digest, explicit UTC expiry, and narrow byte/graph limits while retaining compact text, tenant key hashing, TTL, missing-key, and identifier-match behavior | Dedicated schema/canonical/hostile/resource/pre-client/corrupt-state/tenant/TTL/AST tests plus the existing optional live Redis contract pass locally except the explicitly skipped service case | The optional primitive is not integrated into the main login lifecycle; live Redis and stored-value authenticity remain open | Alpha / bounded optional persisted pair | "Tenant-scoped Redis session metadata has a bounded closed producer/consumer contract; this is not evidence of live-service operation or production login integration" |
| Bounded and recoverable public SQLite export | Exact field registry binds all five exported `_json` values; every payload validates before mutation; nine unchanged format-v1 files stage behind an additive exact SHA-256/byte manifest; existing-tree replacement uses bounded rollback state and explicit verified recovery | Fifteen field-contract cases plus eight manifest/schema/handled-failure/crash/tamper/stale/sibling/CLI publication cases pass; locked 39-test focus, full 1,274-test collection, static/security gates, runtime smoke, and package membership pass | Local structural/change-detection evidence only; SHA-256 is unauthenticated, actor/disclosure authorization and malware controls are absent, observer/pre-marker/host-loss and audit/filesystem atomicity limits remain, and throughput is unmeasured | Alpha / bounded local publication control | "Public SQLite exports are structurally bounded, staged, manifest-bound, and recoverable for tested local failure states without changing format-v1 payload shapes; this is not provenance, authorization, malware, remote-filesystem, host-loss, or scale evidence" |
| Bounded authorization and SoD policy | Central policy requires an explicit permission and exact supplied tenant/workspace/entity/period grants; ownership refusal defaults on; a shared actor comparator covers an exact inventory of nine platform approve/review service methods plus trusted-local workflow history | Hypothesis scope-conjunction, creator-action, and actor-normalization properties plus generic override, AP/AR, account, certification, inventory, workflow, RBAC, transaction, and exact AST inventory contracts pass | The property primitive is not yet universal route/repository ABAC; amount/region/data-class rules, assignments/delegation, privileged emergency access, external identity governance, and deployed effectiveness remain outside the claim | Alpha / bounded P0 policy foundation | "Current inventoried local approval/review services prohibit creator self-approval, and the central policy primitive denies unspecified permission or unauthorized supplied tenant/entity/period scope; this is not complete enterprise ABAC or privileged-access governance" |
| Read-only enterprise policy conflict analysis | `auth.policy_analysis` canonically analyzes approved tenant-bound grants for overlapping SoD permission pairs, service-account human permissions, unscoped privileged grants, and duplicate active grants; findings are severity/digest bound and the artifact never mutates authorization | `tests/test_policy_analysis.py` covers conflict classes, revoked/disjoint scopes, permutation stability, maker-checker, tamper/schema checks, and CLI; module/threat/file-ingress gates are bound | Analysis is a local snapshot only; it does not enforce policy, replace CentralPolicyEngine, prove PostgreSQL/RLS or federation, invalidate distributed caches, or cover every route/job/export/UI surface | Experimental / bounded IAM analysis | "Provides deterministic read-only policy conflict analysis for an approved local snapshot"; never complete enterprise authorization, compliance, certification, or production effectiveness |
| PostgreSQL/RLS policy snapshot conflict analysis | `PolicyAnalysisApplicationService` loads active PostgreSQL RBAC, persisted role-permission scopes including exact optional amount bounds, and enabled service-account grants through `PostgresPolicyAnalysisRepository` under the request tenant transaction, then delegates to the same deterministic analyzer; the API route is read-only and requires `security.policy.manage` plus an independent approval timestamp | `tests/test_postgres_policy_analysis.py`, `tests/test_postgres_policy_scopes.py`, `tests/test_postgres_policy_analysis_runtime.py`, API authorization inventory, repository/parity inventories, and CI server-boundaries run `30808822315` | The live claim is limited to a synthetic single-node PostgreSQL snapshot under the non-privileged role; amount bounds are currency-agnostic analysis evidence, and provider federation, distributed invalidation, and universal route/job/export/UI enforcement remain open | Experimental / bounded PostgreSQL IAM analysis | "Provides a tenant-RLS PostgreSQL policy-snapshot conflict analysis endpoint with maker-checker input and bounded persisted resource/amount scope projection"; not complete enterprise policy administration or production effectiveness |
| Governed evidence drill-down | SQLite `EvidenceRegistryService`, tenant-scoped `PostgresEvidenceRepository`, and the authorized API route share direction/depth/page ceilings, deterministic ordering, default provenance masking, manage-only sensitive access, and read-audit behavior | Local API integration covers traversal, masking, permission denial/allow, invalid bounds, pages and audit count; PostgreSQL repository contract covers redacted paging and bounded audit encoding; authorization inventory is closed at 157 routes | Full local 1,450-test collection passes; no live PostgreSQL graph, graph UI, authenticated manifest serving, or throughput drill exists | Alpha / bounded P2-002 contract | "Provides bounded, role-aware, audited evidence metadata drill-down in current SQLite and PostgreSQL code contracts; this is not live-service, signed-manifest, graph-UX, or scale evidence" |
| Reconciliation-as-Code v1 | Packaged Draft 2020-12 schema and strict Pydantic contract use bounded YAML/JSON ingress, exact decimal strings, executable-hook rejection, canonical SHA-256 manifests, typed human workflow/evidence/golden-case fields, compatibility reading, and atomic validated file rollback | Schema/round-trip/digest/unsafe-input/golden/diff/rollback tests and CLI validation pass; built wheel/sdist include the schema | Full local suite/static/package gates pass. SHA-256 is not a signature; validation/normalization runtime adapters, signed pack installation, publication approval, and Rule Studio remain unimplemented | Alpha / bounded P2-003 contract | "Supports a versioned declarative Reconciliation-as-Code v1 contract with deterministic identity, embedded synthetic cases, and local atomic rollback; this is not signed pack provenance or full rule lifecycle readiness" |
| Reconciliation-as-Code operator tooling | `rules recon-as-code` exposes validate/lint/test/diff/simulate/explain/rollback; embedded cases run only through registered versioned matching adapters and simulation declares no side effects | CLI integration and phase-2 tests prove deterministic counts/digests, structural change paths, mismatch behavior, no arbitrary execution, and rollback revalidation | Execution scope is `matching-adapter-only-v1`; declarative validation/normalization steps are not run and outputs cannot approve or publish financial decisions | Alpha / bounded P2-004 tooling | "Can lint, test, diff, explain, simulate, and roll back the current matching-adapter Reconciliation-as-Code contract without arbitrary code execution; this is not autonomous approval or a complete control-pack runtime" |
| Connector SDK and read-only network runtime | Strict manifest/registration contracts, static built-in allowlist, data-only Ed25519 envelope, versioned active/revoked trust registry, exact HTTPS egress with fixed-query support, public-address pinned TLS, public no-auth or secret-reference execution, bounded cursor/idempotency/rate/retry/response, and fenced durable page artifacts | Local/export conformance, package tamper/revocation, SSRF/redirect/secret/error, public no-auth header isolation, deterministic replay, uncertain object write, and SQLite close/reclaim/resume parity tests pass | Synthetic transport, secret, SQLite, and local object store only; no vendor system, production vault, distributed quota, executable external package, write-back, external assurance, or Enterprise readiness | Alpha / evidence-bounded SDK runtime | "Provides a manifest-driven read-only connector SDK and synthetic HTTPS/durable-job conformance runtime; SAP/Odoo remain export profiles and no live vendor connector is claimed" |
| World Bank public REST reference connector | `reconforge/connectors/world_bank_public.py` pins DS01556/RS00963 and three exact JSON page offsets, uses the shared public no-auth executor, validates a closed finite-Decimal schema, and computes request/canonical response digests | `tests/test_connector_world_bank_public.py` covers exact egress, no Authorization header, schema drift/non-finite rejection, manifest substitution, permutation stability, and the opt-in live first-page fetch; E-460 records 6 focused passes plus the live HTTP 200 page; current direct fetch returned 1,000 rows from source count 2,508 in one attempt with response digest `9f0e40df0fe72f2310e8e5747285f4657389ffe4f55b844e5ca405da6f88dc11` | One public endpoint and bounded synthetic/public records; no vendor credentials, ERP/bank integration, freshness/SLA, write-back, or production operation | Experimental / community reference | "Provides a bounded World Bank public REST reference connector for deterministic interoperability testing"; not a live ERP/bank integration, freshness guarantee, write-back, or production-readiness claim |
| Concrete PostgreSQL named-query source | `reference-postgres-readonly` uses a dedicated `database_source` manifest, exact credential-free endpoint allowlist, runtime DSN host/port/database matching, two fixed parameterized views, bounded `READ ONLY` transactions, statement timeout, tenant-local context, cursor/replay digests, and canonical finite Decimal output | `tests/test_postgres_database_reference.py` structural contracts pass; the local PostgreSQL 16 non-superuser/non-`BYPASSRLS` gate proves synthetic view reads, RLS tenant isolation, endpoint binding, cursor replay, and `0E-18` normalization; hosted CI `30865985067` server-boundaries job `91857821428`, Security `30865984998`, Docker `30865985056`, and CodeQL `30865985004` pass | Deployment-provided views and single-node synthetic PostgreSQL only; no ERP/bank vendor schema, TLS/vault operation, provider failure injection, write-back, throughput/soak, HA/DR, or production readiness | Experimental / bounded runtime primitive | "Provides an experimental fixed-profile PostgreSQL read-only source contract over deployment-provided views; it is not live ERP/bank interoperability or write-back" |
| Live ERP connectors | Export-profile plugin files and a vendor-neutral synthetic network runtime exist | File/profile and synthetic SDK conformance tests only | No vendor system connection verified | Planned | "Export mapping profiles" and "connector SDK runtime", never "live SAP/Odoo connector" |
| ERP reference HTTPS sandbox | `reference-erp-readonly` validates a closed entity-scoped ledger-line page and optionally rejects pages outside an expected entity; the real pinned HTTPS executor is exercised against a disposable TLS sandbox with bounded retry/cursor/idempotency and secret isolation | `tests/test_connector_erp_reference.py` passes injected-schema, entity-guard, and loopback TLS/retry/digest contracts; ADR 0402 and package membership are aligned | Loopback synthetic transport only; no ERP vendor, vault, provider-version, accounting posting, write-back, HA/DR, or production interoperability evidence | Experimental / bounded connector transport evidence | "Exercises a provider-neutral ERP-shaped read-only connector through a disposable HTTPS sandbox"; not a live ERP connector or write-back |
| Payment-statement HTTPS sandbox | `reference-payment-statement-readonly` validates a closed account-scoped statement page and optionally rejects pages outside an expected account; the real pinned HTTPS executor is exercised against a disposable TLS sandbox with bounded retry/cursor/idempotency and secret isolation | `tests/test_connector_payment_statement.py` passes injected-schema, account-guard, and loopback TLS/retry/digest contracts; ADR 0403 and package membership are aligned | Loopback synthetic transport only; no bank vendor, licensed dialect, vault, settlement, payment initiation, write-back, HA/DR, or production interoperability evidence | Experimental / bounded connector transport evidence | "Exercises a provider-neutral payment-statement connector through a disposable HTTPS sandbox"; not a live bank connector or payment/write-back proof |
| Local synthetic signed data-only pack lifecycle | Closed JSON manifest/envelope, Ed25519 trust snapshot, declarative conformance, SQLite maker-checker lifecycle and immutable actor/digest events | Synthetic tamper, false-golden, self-approval, dependency, upgrade, disable, rollback, module, ingress, maturity and threat-index tests | Local synthetic keys and SQLite only; no external publisher, marketplace, customer data, network, or production trust service | Experimental / evidence-bounded | "Local synthetic signed data-only pack lifecycle"; never marketplace, certified pack, verified publisher, compliant, or enterprise-ready |
| Multi-resource upgrade and rollback automation | Closed v1 plan/matrix, distinct operator/deployment versions, release binding, all-resource preflight, approval/journal/receipts, reverse compensation, uncertain isolation, and concrete wheel/SQLite/PostgreSQL/object/configuration/pack adapters | Focused orchestration, hostile integrity, real all-resource local composition, tagged v0.7.0-to-v0.7.1 wheel, and disposable PostgreSQL 17.10 encrypted restore/0052-to-0053/rollback drills pass | Local and disposable synthetic evidence only; production secrets/key custody, host loss, distributed cutover, downtime/capacity, HA/DR, and independent assurance remain unverified | Alpha / evidence-bounded operations | "Supported-version upgrade and bounded rollback automation has local/disposable synthetic evidence"; never zero-downtime, HA/DR, compliant, certified, or Enterprise-ready |
| Bounded authenticated SCIM 2.0 | Strict User/Group application models, forced-RLS PostgreSQL lifecycle, hash-only client credentials, and 15 authenticated `/scim/v2` operations | Local hostile/protocol tests plus 21/21 live PostgreSQL SCIM/Alembic cases; full repository/static/security gates pass | Synthetic PostgreSQL 17.10 non-superuser runtime only; no hosted IdP interoperability, independent assessment, MFA, OAuth server, or production approval | Alpha / evidence-bounded RFC 7643/7644 subset | "Provides a tenant-scoped authenticated SCIM 2.0 User/Group subset with revocable credentials and no implicit RBAC; hosted interoperability and Enterprise readiness are not verified" |
| Role-free service-account lifecycle and bounded HTTP machine principal | Forced-RLS account/direct-permission/hash-only-credential/append-only-event aggregate, reserved `rfa_` authentication, typed principal propagation, central human-action denial, operator CLI, and migration 0037 | 14/14 live lifecycle plus final 35/35 HTTP/policy/service PostgreSQL cases; broader 41/41 audit-logger regression target; full repository/static/security/package gates | Synthetic PostgreSQL 17.10 non-superuser evidence only; workload identity federation, hosted operation, and independent assurance remain open | Alpha / evidence-bounded machine identity | "Provides tenant-scoped role-free service accounts and bounded HTTP machine authentication with explicit least privilege and human-action denial; this is not complete privileged-access management or Enterprise readiness" |
| Session-bound privileged password reauthentication | Migration 0038 forced-RLS append-only assertion, ten-minute application TTL, fifteen-minute DB ceiling, current-session middleware snapshot, closed privileged permission registry, `/auth/step-up`, and structured denial | Local hostile/API/policy/Community compatibility tests plus clean 48/48 PostgreSQL identity/service/policy/migration target and full repository/static/security gates | Synthetic PostgreSQL 17.10 non-superuser runtime proves denial before step-up, password reauthentication, privileged success, session revocation invalidation, tenant isolation and append-only evidence; true MFA, federated ACR/AMR and independent assurance remain open | Alpha / evidence-bounded reauthentication | "Selected PostgreSQL privileged actions require recent session-bound password reauthentication in addition to RBAC; this is not MFA or Enterprise readiness" |
| Governed PostgreSQL access-policy lifecycle | Backend-neutral service, migration 0051, forced-RLS role/policy/assignment metadata, optimistic exact-set mutations, role retirement, last-manager guard, affected-session invalidation, domain audit, signed pagination, and server SQLite-role fail-close | Unit and HTTP hostile tests plus live PostgreSQL 17.10 non-superuser NOBYPASSRLS lifecycle, sibling denial, atomic audit-failure rollback, direct/long recovery, and governed-downgrade refusal | Synthetic single-node evidence only; permission registration remains operator-controlled, and full ABAC, policy approval workflow, integration/retention administration, accessible UI, HA, production IAM assurance, and independent review remain absent | Alpha / evidence-bounded access administration | "Provides tenant-scoped optimistic role, role-policy, and user-role lifecycle administration with session invalidation and final-manager protection in the PostgreSQL profile; this is not complete ABAC, production IAM assurance, or Enterprise readiness" |
| Governed native integration disable and evidence-retention metadata | Backend-neutral service, migration 0052, native federation/SCIM/service-account/notification projections, optimistic runtime disable, forced-RLS versioned policies, append-only assignments, monotonic evidence floor/version, domain audit, and signed pagination | Unit/HTTP hostile tests plus live PostgreSQL 17.10 non-superuser NOBYPASSRLS runtime denial for all four kinds, sibling isolation, cursor tamper/mismatch denial, stale conflicts, disclosure checks, idempotent/non-shortening retention, direct-SQL floor guard, and audit-failure rollback | Synthetic single-node evidence only; no generic connector creation/enable/secret write, external provider interoperability, legal approval, object-store/backup/replica/export lock propagation, accessible UI, HA/DR, or independent assurance | Alpha / evidence-bounded governance | "Provides tenant-scoped administration of four native integration kinds and monotonic versioned evidence-retention metadata in the PostgreSQL profile; this is not legal-retention assurance, object-lock proof, provider interoperability, compliance, or Enterprise readiness" |
| PostgreSQL governed write-back intent persistence and server API boundary | Alembic `0061_pg_writeback_intents`, backend-neutral `WritebackIntentApplicationService`, `PostgresWritebackIntentRepository`, and `server_writeback` request boundary persist the existing canonical write-back intent JSON, digest, lifecycle version, tenant/workspace scope, and timestamp; forced RLS, replay identity, transaction advisory locking, optimistic transitions, append-only trigger, authenticated actor binding, and request-scope equality prevent cross-scope reads and mutation | Static schema/package contracts plus focused application/repository/API contracts; CI `30839751728` server-boundaries proves proposed/approved/dispatched/acknowledged lifecycle, idempotent replay, sibling-tenant exclusion, stale-version refusal, and direct-SQL mutation refusal under a non-superuser role; local API contract proves server-boundary selection and explicit dispatch disablement; Security `30839744852`, Docker `30839746700`, and CodeQL `30839744249` pass; E-371 adds the server-only dispatch hand-off and route inventory inclusion; live server-identity/API fixture passes 4/4 with real RLS persistence and one synthetic provider acknowledgement; exact head `392b7907` is green on CI `30926588702` (server-boundaries `92050378455`, PostgreSQL/HA-DR `92050378311`, Docker parity `92052163349`), Security `30926583902`, Docker `30926589234`, and CodeQL `30926584585` | Single-node PostgreSQL persistence and injected synthetic provider only; no live ERP/bank provider, credential vault, accounting posting, compensation execution, throughput, HA/DR, or production write-back evidence exists | Experimental / bounded runtime primitive | "Provides tenant/workspace-isolated PostgreSQL persistence and an opt-in server-profile API hand-off for digest-bound governed write-back intents; this is not live ERP/bank interoperability or production write-back" |
| Provider-neutral digest-bound write-back transport | `WritebackNetworkRegistration`, `PinnedHttpsPostTransport`, `PinnedHttpsRecoveryTransport`, and `WritebackNetworkExecutor` enforce exact HTTPS egress, feature/approval/operation gates, short-lived payload SHA-256 equality, secret-reference resolution, bounded retries with the same idempotency key, canonical provider response digest, bounded request/response content, and an explicit recovery endpoint whose pinned GET never sends a second mutation; E-371 exposes dispatch and E-483 exposes the scope-bound server recovery route behind separate dispatch/reconcile permissions | Focused transport/conformance tests cover success, transient HTTP/transport retry, permanent/misbound/content-type/schema failures, secret isolation, payload/response tamper, size bounds, pinned POST/GET shape, and recovery after a lost acknowledgement with zero POST calls; E-487's disposable local TLS sandbox exercises three POST retries followed by one real GET recovery; `tests/test_api_connectors.py` and `tests/test_api_authorization_inventory.py` prove local default disablement, synthetic API acknowledgement/recovery, replay without duplicate provider call, and 239-route inventory closure; package export and focused static gates pass | Loopback/injected transport only; no vendor endpoint semantics, customer credential, hosted vault, provider status-API interoperability, compensation delivery, accounting posting, distributed quota, HA/DR, or production write-back evidence | Experimental / bounded transport/API primitive | "Provides an opt-in provider-neutral, digest-bound HTTPS write-back transport contract with bounded retries and an explicit no-second-mutation recovery transport; this is not live ERP/bank interoperability or production write-back" |
| Governed temporary emergency authority | Migration 0039 forced-RLS request/grant/event lifecycle, closed five-permission registry, maker-checker approval, session-bound activation, 5–60 minute TTL, per-use evidence, and mandatory independent review | Local policy/schema tests plus 13/13 live PostgreSQL repository/HTTP/migration cases, direct downgrade/re-upgrade, and full repository/static/security/package gates | Synthetic PostgreSQL 17.10 non-superuser runtime proves self-only request, independent approval/review, session binding, use evidence, termination, tenant isolation and append-only controls; true MFA, hosted operation, production PAM and independent assurance remain open | Alpha / evidence-bounded emergency control | "Provides bounded temporary emergency authority with independent approval and post-use review in the PostgreSQL profile; this is not production PAM, MFA, or Enterprise readiness" |
| User-verified WebAuthn MFA for privileged actions | Pinned optional standards library, closed exact-origin RP configuration, migration 0040 forced-RLS one-time challenges/public credentials/evidence, User Verification, signature counters, session-bound assurance, and configured privileged-policy enforcement | Real P-256 registration/authentication verifier tests plus live end-to-end HTTP/RLS/replay/counter/policy/migration target passing 22/22 and full repository/static/security/package gates | Synthetic PostgreSQL 17.10 and synthetic authenticator evidence only; browser/authenticator matrix, recovery, attestation policy, hosted deployment, and independent assurance remain open | Alpha / evidence-bounded MFA | "Supports optional user-verified WebAuthn MFA and requires it for selected privileged actions when configured; this is not universal authenticator interoperability or Enterprise readiness" |
| Governed real public-financial-data verification | Exact manifest for eleven U.S. Treasury, World Bank, and UK government responses; central bounded YAML/JSON ingress; one exact FI-023 mixed-encoding CSV surface; closed schemas; exact Decimal equations; deterministic matcher; explicit allowlisted downloader; redacted report; manual attested workflow | Hostile tamper, missing/extra input, duplicate-key, truncated-row, unsafe-URL/redirect, dirty-tree, schema, parser-inventory, permutation, workflow-permission/action-pin, and retained-report tests | Fresh clean-commit Windows/Python 3.11.15 run E-444 matched 967/967 with zero unmatched/exceptions and reproducibility SHA-256 `890a4aa8f7b362981bfdd1f0f333d3bad55a886d842c0ff0a82ff165c48e26c5`; accepted external operators: zero; independent security review: absent | Alpha / real public-data technical evidence | "A bounded maintainer run reproducibly verifies 967 records from three named official public-data sources"; do not call it a customer pilot, production validation, audit opinion, independent review, compliance, certification, or Enterprise readiness |
| Deterministic multi-entity translation artifact | `domain.consolidation` enforces balanced single-functional-currency entity inputs, stable source identities and trial-balance digests, explicit group mapping/rate types/buckets, exact Decimal/Money currency policy, source-bound rates, canonical digests, visible rounding, and an unposted CTA; the application port and existing immutable object store retain tenant/workspace-scoped artifacts | Focused examples plus Hypothesis permutations cover exact translation, distinct same-currency historical buckets, identity rates, currency-specific rounding, binary-float/precision/timezone rejection, unbalanced/mixed/duplicate/missing/unused/period/policy failures, schema validation, rehashed tamper rejection, immutable retry, and scope isolation | Local in-process calculation and local immutable object-store replay only; no database run workflow, consolidation journal, approval, ownership/elimination/NCI, statutory statement, live rate provider, ERP write-back, independent assurance, or production scale evidence | Experimental / bounded finance foundation | "Deterministic multi-entity translation artifact foundation with explicit rate and rounding lineage and an unposted translation-adjustment proposal"; never full consolidation, statutory reporting, compliant, certified, Enterprise-ready, or best globally |
| Effective-ownership, NCI-presentation, and elimination worksheet | `domain.consolidation_lifecycle` embeds a verified translation result; validates non-overlapping approved ownership history, rooted controlling paths, exact direct/effective/NCI percentages, currency-rounded plus unrounded NCI presentation, source-bound zero-sum elimination proposals, canonical accounts/digests, and `posting_effect=none`; the existing consolidation application boundary exposes the pure preparation use case | Examples plus 30 Hypothesis permutations cover historical selection, indirect ownership products, missing/overlap/root/control failures, actor/date/float rejection, elimination balance/entity/currency/account conflicts, closed schema validation, and rehashed financial-output tamper rejection; translation/module/threat/boundary regressions pass | Local in-process non-posting worksheet only; NCI is a bounded presentation allocation, not acquisition accounting. No persisted ownership/run, journal approval/post/reversal, lock/reopen, SQLite/PostgreSQL parity, statutory statement, live rate, ERP write-back, independent assurance, or production-scale evidence | Experimental / bounded finance foundation | "Prepares a deterministic non-posting group worksheet with effective controlling ownership, exact NCI presentation, and balanced traceable elimination proposals"; never full statutory consolidation, posted books, compliant, certified, Enterprise-ready, or best globally |
| Governed local consolidation close lifecycle | `application.consolidation_close` defines a backend-neutral service/protocol and `infrastructure.sqlite_consolidation_close` persists verified worksheet payloads into migration-25 SQLite periods, runs, immutable derived journal lines, posting/reversal effects, and period events; schema triggers enforce lifecycle order, row-version advancement, SoD metadata, immutability, balanced committed effects, and local-only control-journal boundaries | `tests/test_sqlite_consolidation_close.py` covers migration availability, pre-migration fail-closed behavior, full prepare/approve/post/reversal/lock/reopen lifecycle, SoD denial, locked-period denial, audit-failure rollback, direct-SQL bypass guards, tamper detection, backup/restore trigger replay, bounded JSON rejection, and package manifest membership; related boundary, registry, threat-model, backup, and consolidation regression targets pass locally | Local SQLite/library boundary only. It persists verified worksheet runs and exact local control-journal effects, but it is not statutory consolidation, legal-book posting, source-ERP/bank mutation, PostgreSQL parity, complete API/CLI/UI exposure, live rates, independent assurance, production scale evidence, or a release-readiness claim. API drill-down and read-only CLI evidence are bounded and additive | Experimental / bounded finance foundation | "Persists verified worksheets into a local SQLite consolidation control-journal lifecycle with maker-checker approval, exact posting/reversal effects, period lock/reopen events, and tested backup/restore replay"; never statutory consolidation, posted legal books, compliant, certified, Enterprise-ready, or best globally |
| Acquisition fair-value/goodwill bridge | `domain.consolidation_acquisition` computes exact consideration plus NCI fair value less identifiable net assets as goodwill or an explicitly policy-allowed bargain purchase; output lines are source/policy digest-bound, maker-checker attributed, balanced, replay-verifiable, and marked `posted: false` | `tests/test_consolidation_acquisition.py` covers exact goodwill, bargain-purchase fail-closed policy, decimal-scale replay, currency/self-approval rejection, tamper verification, and closed JSON Schema; module registry and full static gates pass | Pure local calculation artifact only; no purchase-price allocation detail, tax/deferred tax, impairment, step acquisition/disposal, statutory classification, journal posting, live rates, source write-back, independent assurance, or production readiness | Experimental / bounded finance foundation | "Provides a deterministic non-posting acquisition fair-value/goodwill bridge under an explicit policy"; never statutory acquisition accounting, legal opinion, compliant, certified, Enterprise-ready, or best globally |
| Acquisition PPA detail | `domain.consolidation_ppa` canonically orders source-bound asset/liability valuation items, reconciles book net assets to fair-value net assets and exact adjustments, and embeds the verified goodwill/bargain bridge; output is maker-checker attributed, digest-bound, balanced, and `posted: false`. E-332 adds canonical JSONB PostgreSQL persistence with recomputation, forced tenant RLS, append-only guards, idempotent identity, and audit event. E-333 adds authenticated server-profile POST/GET routes with strict canonical Money and authenticated actor binding; E-334 exercises that API path through the live server-identity boundary | `tests/test_consolidation_ppa.py` covers the deterministic domain/CLI; `tests/test_postgres_consolidation_ppa.py` covers schema/migration contracts; `tests/test_postgres_consolidation_ppa_runtime.py` passes as the dedicated unskipped CI gate in run `30811914832`; `tests/test_api_consolidation_ppa.py` covers local API contract/auth/typed reconstruction; `tests/test_api_server_identity.py` covers the live authenticated POST/GET path in server-boundaries run `30815726556`; module/threat/file-ingress/authorization gates are included | Live API/evidence storage is verified only on PostgreSQL 16 Alpine CI with synthetic single-node data and one API process; no tax/deferred-tax effects, impairment, statutory judgment, equity method, ledger posting, source write-back, independent valuation assurance, restore, HA/DR, or production readiness | Experimental / bounded finance foundation | "Provides a deterministic, tenant-isolated non-posting acquisition PPA evidence artifact under declared source and valuation inputs, with an authenticated PostgreSQL server-profile evidence API"; never statutory acquisition accounting, legal opinion, compliant, certified, Enterprise-ready, or best globally |
| Server-scoped audit and security views | PostgreSQL audit browsing/verification and the security-center overview invoke central policy with exact audit/security permissions, bind the validated tenant, and use no synthetic workspace before the tenant-wide repository boundary | `tests/test_api_audit_administration.py`, `tests/test_api_security_center.py`, and `tests/test_postgres_security_center.py` pass 4/4 with one declared skip; focused assertions capture tenant binding and null workspace semantics | Synthetic PostgreSQL server-profile route/runtime evidence only; redaction and operational snapshots remain bounded, with no federation, distributed invalidation, worker/export/UI adoption, independent HA/DR, compliance, or production IAM assurance | Experimental / bounded IAM route control | "Re-evaluates central tenant policy before PostgreSQL audit and security operational views"; not complete enterprise IAM, compliance, HA/DR, or production readiness |
| Server-scoped metrics reads | PostgreSQL metrics dashboard and lineage routes invoke central `metrics.read` policy with the validated tenant and no synthetic workspace before the adapter | `tests/test_api_metrics.py` passes 1/1 for both routes with explicit tenant/null-workspace assertions; Ruff/Mypy pass | Local synthetic server-profile route contract only; no SLO, compliance, federation, distributed invalidation, worker/export/UI, independent HA/DR, or production evidence | Experimental / bounded IAM route control | "Re-evaluates central tenant policy before PostgreSQL metrics reads"; not a security assurance, SLO, compliance, HA/DR, or production-readiness claim |
| PostgreSQL idempotent outbox-consumer business effect | `0062_pg_outbox_consumer`, forced-RLS immutable `outbox_consumer_receipts`, advisory serialization, source-event foreign key, canonical persisted-payload digest recomputation, and `PostgresOutboxConsumer.apply` commit a database-local effect and receipt in one transaction; same-digest replay returns `duplicate` and changed/missing events fail closed | `tests/test_postgres_outbox_consumer.py` covers schema/input guards and a live lost-acknowledgement failure injection; effect and receipt cardinalities remain one after lease reclaim, the outbox is published, and migration head through `0062_pg_outbox_consumer` applies on a disposable PostgreSQL database; hosted CI `30851329394` passes server-boundaries, Python 3.11/3.12, parity, and Docker with Security `30851329385`, Docker `30851329398`, and CodeQL `30851329332` | Single-node PostgreSQL 16 non-superuser runtime only; no external broker/provider exactly-once, cross-host failover, queue HA, throughput, soak, compensation, or production evidence | Experimental / bounded runtime primitive | "Provides a tenant-scoped PostgreSQL database-local idempotent consumer receipt boundary for same-transaction business effects"; never external exactly-once delivery, HA/DR, or production readiness |
| Deterministic HA/DR quorum and fencing safety model | `reconforge.reliability.ha_dr` provides a topology-validated three-voter/three-domain plus witness state machine with quorum checks, fence-before-elect failover, stale-leader rejection, exact catch-up, and approval-gated re-promotion; the verifier emits a schema-closed canonical report and digest | `tests/test_ha_dr_quorum_simulation.py` plus existing HA/DR drill contracts pass; the generated report `docs/execution/HA_DR_QUORUM_SIMULATION_2026-08-04.json` validates against its closed schema and records two logical failovers, four ordered commits, `rpo_commits=0`, stale-write rejection, and `split_brain_observed=false` | Deterministic in-process simulation only; existing PostgreSQL/Docker drill remains single-host/manual and partial. No independent hosts, PostgreSQL/network failover, external fencing, automatic promotion, wall-clock RPO/RTO, throughput/soak, or production SLO evidence | Experimental / bounded reliability primitive | "Provides a deterministic orchestration-neutral quorum/fencing safety model with a verifiable simulation artifact"; this is not HA/DR deployment evidence, automatic failover, RPO/RTO, or production availability |
| Repeated PostgreSQL HA/DR runtime drill | `.github/scripts/verify_postgres_ha_dr.py` and `verify_postgres_ha_dr_repeated.py` execute the real Docker primary/synchronous-standby topology, encrypted native backup/restore, partition refusal, fencing, promotion, rejoin, and failback; the CI job uploads the report | Local artifacts `docs/execution/POSTGRES_HA_DR_REPEATED_VERIFICATION_2026-08-04.json` and `docs/execution/POSTGRES_HA_DR_REPEATED_VERIFICATION_2026-08-05.json` are schema-valid; the fresh report records 3/3 runs, zero acknowledged transaction loss, final sequence 4, cleanup, failover RTO 11.084–11.175s, and failback RTO 0.981–1.023s; hosted CI run `30884962171`, job `91914021265`, reports 3/3 passes, zero loss, failover RTO 10.468–11.301s, failback RTO 0.424–0.668s, and uploaded the report | Two containers on one Docker host with a manual controller and synthetic key/data; no independent failure domain, quorum/witness, automatic failover, site loss, managed-key custody, or production SLO | Experimental / single-host runtime evidence | "Provides repeated single-host PostgreSQL synchronous-standby backup/failover/failback evidence"; not independent HA, automatic failover, site-loss DR, or production availability |
| Live S3-compatible object-storage provider gate | `.github/scripts/verify_s3_object_storage_live.py` exercises the real boto3-backed `S3ObjectStore` against digest-pinned MinIO, including normal and object-lock buckets; the report is schema-closed and credential-free | `tests/test_s3_object_storage_live.py` validates report schema/digest and CI wiring; hosted `object-storage` job runs both live provider cases and uploads the report artifact | One disposable MinIO process on one CI host with synthetic credentials/bytes; no replication, KMS, cross-site durability, provider interoperability, object-store HA/DR, malware scanning, authorized downloads, or production SLO evidence | Experimental / bounded provider runtime | "Provides a live S3-compatible object-storage contract for hierarchical isolation, immutable creation, checksum tamper refusal, and object-lock delete refusal"; this is not cloud durability, object-store HA/DR, or production availability |
| Redis-shared policy-cache generation | `RedisPolicyCacheVersionStore` supplies a monotonic generation to explicitly enabled local `PolicyDecisionCache` instances; non-safe API requests bump the generation and clear local entries, while Redis stores no decisions or credentials | Unit cache tests prove independent instances stop reusing an allowed decision after a bump and bypass during a version-store outage; live Redis test with two independent clients passes atomic generation observation and cleanup | Synthetic Redis 7 single-node runtime only; coarse global invalidation, Redis HA/failover, outage recovery, complete route/job/export/UI adoption, federation, and production IAM assurance remain unverified | Experimental / bounded server primitive | "Provides an opt-in Redis-shared generation for cross-process policy-cache invalidation with fail-safe cache bypass on generation-read failure"; this is not complete distributed IAM, Redis HA, or Enterprise readiness |
| Domain-diverse grouped-matching 10K profile | `reconforge/benchmark/grouped_matching_domain_scale.py` runs six bounded grouped modes through the public strategy adapter and application service, with exact Decimal/FX/fee/partial policies and closed digests | Focused profile tests plus schema-validated artifact prove 2,500 partitions/10,000 records, expected 416 deliberate ambiguities, zero unmatched partitions, zero adapter mismatches, and zero permutation mismatches; observed run is 6.8093s/1.5709 MiB on Windows 11/Python 3.14.6 | One-host, one-process synthetic algorithm run only; PostgreSQL parity, soak, distributed capacity, provider I/O, posting, and production sizing remain unverified | Experimental / bounded algorithm evidence | "Publishes a reproducible domain-diverse 10K grouped-matching correctness profile with explicit ambiguity and permutation/adapter parity"; this is not a throughput, capacity, PostgreSQL, or production-sizing claim |
| PostgreSQL durable-job 10K-effect tier | `reconforge/benchmark/postgres_durable_job_scale.py` runs 16 independent workers over 2,500 jobs and four forced-RLS tenant lanes using the real PostgreSQL repository | Live local PostgreSQL 16 run and schema-valid artifact prove 10,000/10,000 effects, 2,500/2,500 completed jobs, zero duplicates/residue, and 625 completions per lane; observed 30.4805s/82.0197 jobs per second on Windows 11/Python 3.14.6 | One-host synthetic correctness/concurrency run only; soak, backpressure, queue HA, failover, host loss, cross-host fairness, RPO/RTO, and production sizing remain unverified | Experimental / bounded PostgreSQL concurrency evidence | "Publishes a reproducible PostgreSQL 10K-effect durable-job correctness tier"; not a throughput, capacity, HA/DR, SLO, or production-readiness claim |
| Hosted PostgreSQL durable-job 10K-effect gate | `tests/test_postgres_durable_jobs.py::test_live_postgres_durable_job_10k_multi_worker_scale_profile` runs the existing 16-worker profile through the real PostgreSQL repository/application path in `server-boundaries` | Hosted CI run `30887647946` / job `91922298719` passed the test: 2,500/2,500 completed jobs, 10,000/10,000 effects, zero duplicates/residue, and 625 completions per lane | Digest-pinned PostgreSQL 16 single-node CI service with synthetic tenant lanes and a non-privileged role; no soak, capacity, backpressure, queue HA, host loss, cross-host fairness, RPO/RTO, or production sizing | Experimental / hosted bounded concurrency evidence | "Runs a hosted PostgreSQL 10K-effect correctness/concurrency gate"; not a capacity, throughput, HA/DR, SLO, or production-readiness claim |
| Hosted PostgreSQL grouped-matching 500-partition gate | `tests/test_postgres_grouped_matching_scale.py::test_live_postgres_grouped_matching_500_partition_scale_profile` reuses the public grouped adapter and PostgreSQL worker over five modes | Hosted CI run `30894602923` / `server-boundaries` job `91944412213` passed the gate: 500 completed partitions, exact result-row cardinality, zero duplicate result identities, zero failed/active runs, and 50 runs per mode; the initial 2,000-partition attempt failed and is not promoted | Digest-pinned PostgreSQL 16 single-node CI service with synthetic tenant/RLS scope; no throughput, soak, backpressure, cross-host fairness, provider interoperability, posting, write-back, HA/DR, or production sizing | Experimental / hosted bounded matching evidence | "Runs a hosted PostgreSQL grouped-matching 500-partition correctness gate"; not a capacity, throughput, provider, HA/DR, or production-readiness claim |
| Current local PostgreSQL HA/DR repeated drill (2026-08-10) | `.github/scripts/verify_postgres_ha_dr.py` and `verify_postgres_ha_dr_repeated.py` run three disposable PostgreSQL 17.10 primary/synchronous-standby cycles with encrypted backup/isolated restore, fencing, manual promotion/failback, sentinel replay, and labelled-resource cleanup | `docs/execution/POSTGRES_HA_DR_REPEATED_VERIFICATION_2026-08-10.json` is schema-valid and SHA-256 bound; 3/3 runs report zero acknowledged loss, final sequence 4, cleanup success, failover RTO 11.117–11.321s, failback RTO 0.931–1.082s, and `all_runs_passed: true` | Docker Engine 29.6.2, two containers on one host/failure domain, synthetic data/key, manual controller; no quorum/witness, automatic failover, host/site loss, cross-site recovery, or production SLO | Experimental / bounded local live drill | "Provides current reproducible single-host PostgreSQL synchronous-standby failover/failback and encrypted restore evidence"; not independent HA, automatic failover, site-loss DR, hosted E-461 closure, or production availability |
| Strict management-pack financial ingress | `reconforge/reports/management_pack.py` selects `STRICT_FINANCIAL_INPUT_POLICY` for amount, risk, WIP, and close-completion parsing; optional malformed values remain unquantified | `tests/test_reports.py`, `tests/test_generated_report_ingress.py`, and `tests/test_reconciliation_input_policy.py` pass; explicit regression rejects binary-float `0.1`; Ruff passes | Local synthetic/report calculations only; other named legacy callers, statutory policy, hosted matrices, live providers, and production evidence remain open | Experimental / bounded financial-control evidence | "Management-pack financial aggregation rejects binary-float ingress and preserves visible invalid-value boundaries"; not statutory accounting, compliance, or production readiness |
| Replay-verifiable consolidation close evidence bundle | `reconforge/domain/consolidation_close_bundle.py` derives `consolidation-close-bundle-v1` only from replay-verified SQLite/PostgreSQL run details and binds worksheet, translation, management-statement, journal, and effect digests | Focused SQLite bundle/lifecycle tests pass 15/15; PostgreSQL close contract plus bundle tests pass 7/7 with one declared no-DSN skip; a live local PostgreSQL run passes all 8 close tests; tampered statement binding and bundle digest are refused | Additive read-time local-control-journal and management evidence; no statutory statement, external posting, provider/write-back, HA/DR, or production claim | Experimental / bounded evidence-plane contract | "Binds replay-verified close artifacts into one tamper-evident run bundle"; not a statutory, posting, provider, HA/DR, or production-assurance claim |
| Hosted PostgreSQL queue-policy and lane-fairness gate | `.github/workflows/ci.yml` explicitly invokes `tests/test_postgres_durable_jobs.py::test_live_postgres_job_application_contract_and_rls` and `::test_live_postgres_round_robin_scheduler_is_lane_scoped_and_deterministic` under `server-boundaries` | Hosted CI run `30898382499` / `server-boundaries` job `91956632669` passed the invocation; the tests cover queue-cap rejection/replay, retry/cancellation cleanup, tenant isolation, exact lane filters, and deterministic round-robin selection | Digest-pinned PostgreSQL 16 single-node service with synthetic tenants and non-privileged role; bounded queue/retry/fairness correctness only, no capacity, distributed fairness, soak, queue HA, failover, or production SLO | Experimental / hosted bounded concurrency evidence | "Runs hosted PostgreSQL queue-cap and scheduler-fairness correctness gates"; not a throughput, capacity, distributed-fairness, HA/DR, or production claim |
| Scope-bound server write-back authorization | `enforce_server_scoped_permission` re-evaluates the four server write-back permissions with authenticated tenant/workspace/entity grants and step-up state before PostgreSQL repository access; the new dispatch route requires the dedicated permission and the connector router is included in the startup inventory; local SQLite compatibility remains unchanged | Focused execution-scope and write-back API tests pass 8/8, connector dispatch/API/inventory tests pass 7/7; exact code head `e963de17` is green on CI `30924311132` (server-boundaries `92042596978`, PostgreSQL/HA-DR `92042596980`, Docker parity `92044277714`), Security `30924311089`, Docker `30924311183`, and CodeQL `30924311129`; Ruff and Mypy pass | Local/synthetic server-profile request contracts only; complete route/job/export/UI adoption, federation, cache invalidation, live providers, network dispatch to a vendor, compensation, HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "The server write-back route family is centrally bound to the authenticated hierarchy, and dispatch is fail-closed behind a dedicated permission"; not universal enterprise IAM, live provider write-back, or production readiness |
| Scope-bound PostgreSQL consolidation-close API | `server_consolidation_close` passes the authenticated execution hierarchy through `PostgresTenantBoundary` and now preserves it through close/certification repository scope resets; authenticated period creation and the full replay-verified run/approval/post/reversal/lock/reopen lifecycle query by workspace and fail closed on sibling-workspace records; the router is in the startup authorization inventory | Live server-identity fixture plus `tests/test_api_consolidation_close.py`, `tests/test_api_server_scope_boundary.py`, and `tests/test_api_authorization_inventory.py` pass as part of the 14-test API/identity/close/inventory gate; strict worksheet replay, maker-checker transitions, journal effect creation, optimistic versions, and sibling-write refusal pass; E-568 scope-capture contracts and E-569 full regression pass; E-644 adds real TestClient PostgreSQL HTTP list/read/RLS coverage and caught the internal Money-bearing replay object before serialization; E-655 adds the Alembic-head authenticated full lifecycle on a non-superuser/non-BYPASSRLS role, including period/run replay, committed posting/reversal effects, lock/reopen state, and sibling workspace/tenant denial; migration head is `0086_pg_close_reopened` for current evidence | Synthetic single-node PostgreSQL API route evidence; close-table hierarchy persistence, statutory consolidation, legal-book posting, external ERP/bank provider, write-back, independent HA/DR, restore, or production IAM evidence is not | Experimental / bounded financial API control | "Provides a hierarchy-bound PostgreSQL consolidation-close API with replay-verified run lifecycle and maker-checker control-journal transitions"; not statutory consolidation, legal-book posting, external provider/write-back, HA/DR, or production readiness |
| Scope-bound consolidation ownership API | `reconforge.api.routes.consolidation_ownership` exposes strict save/effective-resolution operations over the backend-neutral ownership service; server mode uses the authenticated execution hierarchy and PostgreSQL RLS, while local mode retains SQLite compatibility; save-time approver lookup requires an enabled distinct identity with a governed finance permission | Live server-identity fixture plus `tests/test_api_consolidation_ownership.py`, `tests/test_api_server_scope_boundary.py`, `tests/test_api_authorization_inventory.py`, and `tests/test_postgres_consolidation_ownership.py` pass 29/29 on local PostgreSQL; unknown and unauthorized approver rejection passes; authorization inventory is 219 routes with digest `46a0eac80865dbf7219a2b8230c8dc576d41cd503bdae224c9e01e442828e8f8`; Ruff/Mypy/build pass; hosted head `6eb71f2a` passes CI `30913043618` including server-boundaries `92004218583`, postgres-ha-dr `92004218707`, docker-parity `92005941216`, Security `30913043237`, Docker `30913043606`, and CodeQL `30913043202` | Synthetic single-node PostgreSQL and one API process; the live fixture proves identity lookup, RLS, and adapter integration but not a separate approver session/MFA, statutory consolidation, provider/write-back, HA/DR, or production IAM | Experimental / bounded financial API control | "Provides a strict hierarchy-bound API for immutable effective-dated ownership master data with save-time approver identity and permission verification"; not statutory consolidation, independent approval-session proof, live provider/write-back, HA/DR, or production readiness |
| Server-scoped close and ownership mutations | PostgreSQL close period/run/reversal/lock/certification mutations and ownership save invoke `enforce_server_scoped_permission` with the authenticated tenant/workspace before repository access; GET/read routes and SQLite compatibility are unchanged | Focused close/ownership/execution-scope tests pass 15/15; final full pytest, Ruff, Mypy, build, and diff-check pass; exact head `8521b15d` is green on CI `30929398907` (server-boundaries `92059947838`, postgres-ha-dr `92059947810`, Docker parity `92061607466`), Security `30929399364`, Docker `30929398698`, and CodeQL `30929399241` | Synthetic single-node PostgreSQL server profile and local route contracts only; complete route/job/export/UI adoption, federation, distributed invalidation, live providers, independent HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "Re-evaluates central tenant/workspace policy before PostgreSQL close and ownership mutations"; not universal enterprise IAM, federation, live write-back, HA/DR, or production readiness |
| Server-scoped close-management mutations | PostgreSQL close period initialization, task status, lock, and reopen call `enforce_server_scoped_permission` for `close.manage` before repository access; read routes and SQLite compatibility are unchanged | Focused server identity/scope tests pass 7/7; final full pytest, Ruff, Mypy, and diff-check pass; exact head `6d2a926a` is green on CI `30931676837` (server-boundaries `92067646971`, postgres-ha-dr `92067646818`, Docker parity `92069231895`), Security `30931676624`, Docker `30931675916`, and CodeQL `30931676434` | Synthetic single-node PostgreSQL server profile and local route contracts only; complete route/job/export/UI adoption, federation, distributed invalidation, live providers, independent HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "Re-evaluates central tenant/workspace policy before PostgreSQL close-management mutations"; not universal enterprise IAM, federation, live write-back, HA/DR, or production readiness |
| Server-scoped finance-ledger mutations | PostgreSQL account upsert and atomic ledger-entry creation call `enforce_server_scoped_permission` for `finance_core.manage` before repository access; reads and SQLite compatibility are unchanged | Focused server identity/scope tests pass 7/7; final full pytest, Ruff, Mypy, and diff-check pass; exact head `35c09f63` is green on CI `30932859161` (server-boundaries `92071583948`, postgres-ha-dr `92071583884`, Docker parity `92073207856`), Security `30932857268`, Docker `30932856740`, and CodeQL `30932859207` | Synthetic single-node PostgreSQL server profile and local route contracts only; complete Finance Core parity, statutory posting, complete route/job/export/UI adoption, federation, distributed invalidation, live providers, independent HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "Re-evaluates central tenant/workspace policy before PostgreSQL finance-ledger mutations"; not statutory posting, universal enterprise IAM, federation, live write-back, HA/DR, or production readiness |
| Server-scoped master-data mutations | PostgreSQL currency, organization, legal-entity, branch, fiscal-period, and period-status mutations call `enforce_server_scoped_permission` for `master_data.manage` before repository access; reads, explicit workspace limits, and SQLite compatibility are unchanged | Focused server identity/scope plus execution-scope tests pass 7/7; master-data/server-scope selection passes 16/16; exact head `d8bc4dd8` passes hosted CI `30937323538` (server-boundaries `92086636635`, postgres-ha-dr `92086636683`, Docker parity `92088189807`, both Python suites, engine-parity, object-storage), Security `30937323640`, Docker `30937323622`, and CodeQL `30937323585` | Synthetic single-node PostgreSQL server profile and local route contracts only; full master-data/Finance Core parity, federation, complete route/job/export/UI adoption, distributed invalidation, live providers, independent HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "Re-evaluates central tenant/workspace policy before PostgreSQL master-data mutations"; not universal enterprise IAM, federation, live write-back, HA/DR, or production readiness |
| Server-scoped reconciliation run mutations | PostgreSQL reconciliation submit, cooperative cancel, and requeue call a central any-permission scope evaluator for `reconciliation.manage` OR `match.run` before repository access; local compatibility and read routes remain unchanged | `tests/test_api_server_reconciliation.py` plus `tests/test_api_execution_scope.py` pass 9/9; full pytest, Ruff, Mypy, build, and diff-check pass locally; exact head `d8bc4dd8` passes hosted CI `30937323538` (server-boundaries `92086636635`, postgres-ha-dr `92086636683`, Docker parity `92088189807`, both Python suites, engine-parity, object-storage), Security `30937323640`, Docker `30937323622`, and CodeQL `30937323585` | Synthetic single-node PostgreSQL server profile and local route contracts only; distributed worker authorization, universal route/job/export/UI adoption, federation, live providers, independent HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "Re-evaluates central tenant/workspace policy before PostgreSQL reconciliation run mutations while preserving OR permission compatibility"; not universal enterprise IAM, distributed authorization, live write-back, HA/DR, or production readiness |
| Server-scoped evidence mutations | PostgreSQL evidence registration, linking, requirement creation, sensitive drill-down, and checksum verification call central scope enforcement for `evidence.manage` or `evidence.verify` before repository access; local compatibility and ordinary reads remain unchanged | `tests/test_api_server_evidence.py`, `tests/test_api_execution_scope.py`, and `tests/test_api_authorization_inventory.py` pass 12/12; full pytest, Ruff, Mypy, build, and diff-check pass; exact head `3778811` passes hosted CI `30940202330` (server-boundaries `92096349955`, postgres-ha-dr `92096349776`, Docker parity `92097921442`), Security `30940202609`, Docker `30940202853`, and CodeQL `30940202230` | Synthetic single-node PostgreSQL server profile and local route contracts only; workspace-level evidence persistence, universal route/job/export/UI adoption, federation, live providers, independent HA/DR, and production IAM assurance remain unverified | Experimental / bounded IAM route control | "Re-evaluates central tenant/workspace policy before PostgreSQL evidence mutations"; not complete evidence-plane isolation, universal enterprise IAM, federation, live write-back, HA/DR, or production readiness |
| Tenant-wide administration policy boundary | `enforce_server_tenant_permission` binds the requested tenant to `X-ReconForge-Tenant` and re-evaluates central policy without a synthetic workspace before access-role, identity/session, scope-grant, and security-governance repository work | `tests/test_api_execution_scope.py` plus access-administration, identity-administration, and security-governance regressions pass 15/15; Mypy/Ruff/diff-check pass | Local request contracts; the PostgreSQL live suites remain the source of provider/runtime evidence when configured, while worker/export/UI adoption, federation, distributed invalidation, live providers, independent HA/DR, and production IAM assurance remain open | Experimental / bounded IAM route control | "Re-evaluates central tenant policy before PostgreSQL tenant-wide administration"; not universal enterprise IAM, federation, distributed invalidation, HA/DR, or production readiness |
| Live PostgreSQL tenant-wide administration gate | E-397 tenant-wide policy boundary is active in access, identity, scope-grant and security-governance route families | Fresh PostgreSQL 16 database at Alembic `0064` with non-superuser `reconforge_app` passes access administration, security governance and identity administration live contracts 3/3; tenant isolation and guarded rollback assertions hold | Current local single-node PostgreSQL runtime only; the superuser/dirty-database attempt is excluded; no federation, distributed invalidation, worker/export/UI adoption, live providers, independent HA/DR or production IAM assurance | Experimental / bounded live PostgreSQL IAM evidence | "Tenant-wide administration is exercised under a non-superuser PostgreSQL RLS runtime gate"; not universal enterprise IAM, HA/DR or production readiness |
| Request hierarchy binding for server policy | `enforce_server_scoped_permissions` rejects an adapter tenant mismatch and normalizes organization/legal-entity headers before central policy evaluation even when `workspace_id=None`; explicit/header mismatches and entity-without-parent fail closed | `tests/test_api_execution_scope.py` plus tenant-scoped PPA/impairment/deferred-tax contracts pass 23 tests; Mypy/Ruff/diff-check pass; ADR 0417 is packaged | Local request-policy contracts; tenant-only evidence schemas still do not prove multi-entity row isolation or production IAM | Experimental / bounded IAM invariant | "Server policy checks bind the authenticated tenant and optional organization/entity hierarchy"; not complete enterprise IAM, statutory multi-entity isolation, federation, HA/DR or production readiness |
| Server-scoped consolidation PPA policy | PostgreSQL consolidation PPA POST/GET routes re-evaluate `finance_core.manage` or `finance_core.read` OR `finance_core.manage` against the validated request tenant with an explicit null workspace before the tenant-scoped non-posting adapter | `tests/test_api_consolidation_ppa.py` passes 3/3 and `tests/test_api_server_identity.py` passes 3/3 with one declared skip; the focused regression captures tenant binding and no synthetic workspace | Local route contracts plus existing synthetic PostgreSQL server-identity lifecycle; no statutory acquisition accounting, provider, write-back, independent HA/DR, distributed IAM, or production evidence | Experimental / bounded IAM route control | "Re-evaluates central tenant policy before PostgreSQL consolidation PPA evidence access"; not statutory acquisition accounting, universal enterprise IAM, live provider integration, write-back, HA/DR, or production readiness |
| Governed write-back compensation transport | `WritebackNetworkRegistration.allowed_compensation_operations` is empty by default; `WritebackNetworkExecutor.dispatch_compensation` uses a separate operation marker/key, bounded caller payload digest, and `complete_compensation` acknowledgement binding; rejected acknowledgements cannot claim `compensated` | `tests/test_connector_writeback_network.py` passes 24/24 with allowlist/tamper/lifecycle/retry/conformance and negative-acknowledgement cases; Ruff and Mypy pass | Synthetic injected transport only; no live ERP/bank provider, reversal semantics, vault, signed executable package, production egress, HA/DR, or production IAM evidence | Experimental / bounded transport contract | "Provides an opt-in provider-neutral compensation transport with separate idempotency, positive acknowledgement binding, and fail-closed rejected acknowledgements"; not a live provider integration, accounting reversal decision, or production write-back claim |
| PostgreSQL intercompany elimination evidence | Migration `0063_pg_ic_elimination`, forced-RLS immutable `intercompany_elimination_artifacts`, authenticated server API, exact source/result JSONB and digest columns, replay verification, and prepare/read ABAC bound to tenant/workspace/organization/legal-entity | Focused schema/API tests pass 4/4 plus the hierarchy route contract; hosted CI `30961377710` / `server-boundaries` `92165832615` passes the live non-superuser migration, RLS, idempotent replay, sibling-tenant isolation, and immutability gate; ADR 0416 is packaged | Digest-pinned single-node PostgreSQL CI with synthetic non-posting proposals only; no statutory consolidation, journal posting, ERP/bank provider, write-back, throughput, HA/DR, or production evidence | Experimental / bounded live PostgreSQL evidence | "Persists hierarchy-bound, replay-verifiable intercompany elimination proposals in PostgreSQL"; not statutory consolidation, posting, live provider integration, HA/DR, or production readiness |
| Server Finance Core API adapter | `server_finance_core.py` carries authenticated tenant/workspace hierarchy through forced-RLS `PostgresFinanceCoreRepository`; rich chart/account/dimension/journal/snapshot and explicit entry lifecycle routes re-evaluate `finance_core.read/manage/validate`, while legacy minimal ledger payloads remain compatible | `tests/test_api_server_finance_core.py` passes route/scope/no-local-fallback contracts under hosted CI `30965184880`; optional `tests/test_api_server_finance_core_live.py` runs chart/account/journal/draft-validation/isolation against a non-privileged PostgreSQL role when DSN is configured | The live-DSN route gate is conditional and was not promoted as an independent hosted API claim; no statutory consolidation/legal-book posting, live ERP/bank connector, write-back, throughput, HA/DR, or production evidence | Experimental / bounded API and IAM evidence | "Provides a tenant/workspace-scoped PostgreSQL Finance Core API adapter with explicit lifecycle controls"; not statutory posting, live provider integration, write-back, HA/DR, or production readiness |
| PostgreSQL close/intercompany evidence binding | Alembic `0064_pg_close_ic_links` creates forced-RLS immutable links from prepared close runs to replay-verified `ice-*` artifacts; the close adapter checks exact proposal payloads, workspace/period/currency, matched IDs, unresolved counts, and link digests before approval and includes artifact digests in the additive bundle | Local full suite/static/package gates pass; hosted CI `30968619652`, server-boundaries `92187914873`, postgres-ha-dr `92187914777`, Docker parity `92188812253`, Security `30968619726`, Docker `30968619657`, and CodeQL `30968619666` pass at head `01f4de85` | Synthetic bounded PostgreSQL control-journal evidence only; no statutory/legal-book posting, live ERP/bank provider, write-back, throughput, HA/DR, compliance, certification, or production evidence | Experimental / bounded evidence provenance | "Binds replay-verified intercompany evidence to prepared PostgreSQL close runs and blocks approval when coverage is incomplete"; not statutory consolidation, legal-book posting, live provider/write-back, HA/DR, or production readiness |
| Governed durable-worker claim boundary | Opt-in `GovernedDurableJobWorkerService` evaluates the central policy before a durable-job claim, requires a service-account principal whose identity equals `worker_id`, and rejects tenant/workspace/entity mismatches before repository access | `tests/test_governed_worker_policy.py` passes 3/3: missing permission, human principal, and sibling scope are no-effect; scoped `match.run` service claim succeeds; Ruff/Mypy pass | Local SQLite worker contract only; no universal worker/export/UI adoption, revocation re-evaluation, distributed invalidation, federation, live providers, independent HA/DR, or production IAM assurance | Experimental / bounded IAM worker control | "Provides an opt-in central-policy gate before a durable worker claims a scoped job"; not universal enterprise IAM, distributed authorization, HA/DR, or production readiness |
| PostgreSQL reconciliation worker policy boundary | Optional tenant-only supplier remains compatible for unscoped runs; an explicit `(tenant, workspace, entity)` supplier, persisted workspace/organization/entity attribution, hierarchy-aware claim predicate, and exact GUC propagation now gate configured runs before and during processing | `tests/test_postgres_reconciliation.py` plus persisted-JSON/Alembic contracts pass 66 tests with two declared live-PostgreSQL skips; exact workspace/organization/entity policy, GUC, claim-parameter, scoped completion, and legacy-supplier rejection are asserted; the preceding full local regression reached 2,627/2,627; Ruff/Mypy/Bandit pass on the slice | Local fake-connection and synthetic PostgreSQL worker contracts; universal scheduler/outbox/export/UI adoption, revocation, federation, distributed invalidation, live providers, independent HA/DR, and production IAM remain unverified | Experimental / opt-in scoped worker IAM control | "Can gate one configured PostgreSQL reconciliation worker lane by exact tenant/workspace/organization/legal-entity policy and carry that hierarchy through claim and persistence"; not universal distributed authorization, live PostgreSQL promotion, HA/DR, or production readiness |
| PostgreSQL scheduler/outbox worker policy boundary | Scheduler now supports an optional deterministic `(tenant, workspace, entity)` lane supplier, exact scope-aware policy context, and row-lock filters that preserve schedule RLS through dispatch writes; outbox retains the existing tenant-only policy lane | `tests/test_postgres_scheduler_worker.py` passes scoped exact-policy, stable-lane, pre-connection rejection, and propagation contracts; the existing scheduler/outbox suite remains green with one declared live-service skip; Ruff/Mypy pass on the changed slice | Local scheduler worker contracts plus capability-gated live PostgreSQL schedule/RLS runtime; outbox scope, revocation re-evaluation, universal export/UI adoption, federation, distributed invalidation, live providers, HA/DR, and production IAM remain unverified | Experimental / opt-in scheduler scoped worker IAM control | "Can gate one configured PostgreSQL scheduler lane by exact tenant/workspace/entity policy and filter due schedules before dispatch"; not scoped outbox authorization, universal enterprise IAM, HA/DR, or production readiness |
| PostgreSQL acquisition deferred-tax evidence | Migrations `0065_pg_deferred_tax` and `0077_pg_imp_tax_scope`, `AcquisitionDeferredTaxApplicationService`, and `PostgresConsolidationDeferredTaxRepository` persist canonical request/result JSONB under hierarchy-aware forced RLS, recompute digests, replay-verify reads, enforce maker-checker actors, emit audit evidence, and reject `posted: true` | E-654 `tests/test_api_consolidation_tax_impairment_live.py` runs `alembic upgrade head` to `0085_pg_reversal_definer` on a clean PostgreSQL 16 Alpine runtime, then passes authenticated POST/GET/replay/idempotency/sibling-tenant assertions with a non-superuser/non-`BYPASSRLS` role | Synthetic single-node tenant-scoped evidence with no workspace-header claim; no statutory tax accounting, legal-book posting, live rates, ERP/bank write-back, downgrade/rollback, restore, HA/DR, or production evidence | Experimental / bounded non-posting PostgreSQL evidence | "Persists and serves a deterministic, replay-verifiable acquisition deferred-tax bridge with optional hierarchy scope through an authenticated PostgreSQL API"; not statutory tax accounting, tax advice, journal posting, live provider/write-back, HA/DR, or production readiness |
| Full local regression after deferred-tax persistence/API | Current tree includes the deterministic domain/CLI bridge, PostgreSQL migration/adapter, authenticated server routes, FI-034 JSON parser inventory, and updated parity/authorization inventories | `uv run pytest -q --tb=short -ra` exits 0 in 351.9s; Ruff, Mypy (452 files), Bandit, pip-audit, package build, and `git diff --check` pass; declared skips/warnings remain visible | One Windows workstation plus a separate disposable PostgreSQL 16 runtime gate; no hosted matrix, statutory close, live ERP/bank provider/write-back, independent HA/DR, distributed IAM, scale/soak, breadth, or production approval | Local regression/package evidence | "Current tree passes the complete local regression and package gate"; not hosted release approval or global objective completion |
| Full local regression after hosted worker IAM | Current tree includes the shared PostgreSQL scheduler/outbox service-worker policy guard, additive settings, and focused allow/deny contracts | `uv run pytest -q --tb=short -ra` exits 0 in 345.7s; declared external-service/platform skips and existing warnings remain visible; package build, Mypy, Ruff, and `git diff --check` pass on the changed slice | One Windows workstation; no hosted matrices, live provider/write-back, statutory close, independent HA/DR, distributed IAM, scale/soak, coherent breadth, or release approval | Local regression/package evidence | "Current tree passes the complete local regression after hosted worker IAM"; not hosted release approval or global objective completion |
| Local static/security/package gates after hosted worker IAM | Current tree includes the shared worker policy guard, ADR/evidence records, and additive worker settings | Ruff passes; Mypy reports no issues in 453 source files; Bandit and package build pass; pip-audit 2.10.1 reports no known vulnerabilities after excluding the unpublished local project distribution; `git diff --check` passes | One local Windows environment; pip-audit does not audit the unpublished project itself, and hosted scans, signed SBOM/provenance, trusted builder, and release approval remain open | Local security/package evidence | "Current tree passes local static, dependency, package, and diff gates"; not hosted supply-chain approval or release readiness |
| Current live Redis session/policy adapter drill | `.github/scripts/verify_redis_live.py` exercises tenant-key isolation, hashed-session-token storage, shared policy-generation visibility, and cleanup through the real Redis adapter; E-661 adds bounded read reconnect and explicit mutation no-retry behavior | Current local disposable `redis:7-alpine` runtime passes all 13 foundation tests, including forced pool disconnect followed by session recovery; closed schema/digest test validates both dated reports and image/report digests | Single-node synthetic Redis on one host; no replication, Sentinel/Cluster failover, cross-site durability, distributed quota semantics, Redis HA, production SLO, or hosted attestation | Experimental / bounded runtime evidence | "Verifies Redis tenant isolation, hashed-session-token storage, shared policy generation, cleanup, and one bounded read reconnect in a disposable single-node drill"; not Redis HA, server failover, production readiness, or global objective completion |
| Write-back crash-window recovery | `WritebackNetworkExecutor.recover` and the server recovery boundary use the original idempotency key and status-only transport; the append-only SQLite intent history preserves `DISPATCHED` until a verified acknowledgement is persisted | E-660 spawns a worker that records one accepted provider mutation and exits before acknowledgement persistence, then proves version-3 `DISPATCHED` recovery to version-4 `ACKNOWLEDGED` with one status lookup and an empty POST transport; focused network tests and Ruff pass | Synthetic injected provider plus one-host SQLite process boundary; no vendor status API, distributed idempotency, ERP/bank authentication or semantics, accounting posting, HA/DR, or production operation | Experimental / bounded recovery evidence | "Recovers an uncertain synthetic write-back result through an idempotency-status lookup without issuing a second mutation"; not live provider interoperability, accounting posting, HA/DR, or production write-back |
| Current grouped-matching 1M deterministic rerun | `grouped-matching/1m-record-true-many-to-many-v1` runs the bounded true many-to-many strategy over 250,000 independent partitions with canonical effect/manifest digests | Current tree rerun once on Windows 11/Python 3.14.6 matches 1,000,000/1,000,000 records in one run with zero ambiguous/unmatched, cross-engine, or permutation mismatches and equal digests; report test verifies the artifact | One Windows host/process and exact-USD four-record synthetic partitions; no PostgreSQL parity, distributed capacity, soak, provider I/O, SLO, or production readiness | Experimental / bounded algorithm benchmark | "Current tree reproduces the declared 1M grouped-matching profile once with equal deterministic digests"; not distributed capacity, production throughput, or global objective completion |
| Current PostgreSQL durable-job backpressure rerun | `postgres-durable-job-load/backpressure-tier-v1` uses bounded atomic submission, forced-RLS tenant lanes, independent producer/worker connections, and a four-job queue cap | Local PostgreSQL 17.10 non-privileged runtime completes 64/64 jobs and 256/256 effects with 24 rejected/retried submissions, zero duplicate effects, exact 16/tenant fairness, queue cap four, and zero final residue; current report test verifies the digest-bound artifact | One host and synthetic workload; no capacity, queue HA, automatic failover, host-loss, cross-host fairness, soak, RPO/RTO, production SLO, or hosted attestation | Experimental / bounded runtime evidence | "Verifies a bounded PostgreSQL durable-job queue cap and drain with tenant-lane fairness"; not queue HA, production capacity, or production readiness |
| Current full local regression after scale evidence | Current tree includes the World Bank reference connector, Redis/S3/HA-DR reports, grouped-matching 1M report, and PostgreSQL backpressure report | `uv run --no-sync pytest -q --tb=short -ra` exits 0 in 364.3s with no collection or executed failure; declared capability skips and existing warnings remain visible | One local Windows environment; hosted matrices, live ERP/bank providers, independent HA/DR, signed provenance, and release approval remain external | Local regression evidence | "Current tree passes the complete local pytest regression"; not hosted release approval or global objective completion |
| Repeated durable-job soak boundary | `DurableJobSoakProfile` reuses the public durable-job load harness over isolated SQLite databases for a declared 2-32 iteration tier and closes only structural invariants | Focused soak/load tests verify declared job/effect counts, stable effect digests across iterations and independent replays, zero duplicate effects, zero queued/running residue, manifest closure, and package membership | One-host SQLite repetition; runtime/memory are observations only; PostgreSQL/distributed soak, capacity, queue HA, host-loss recovery, SLO, and production readiness remain open | Experimental / bounded soak evidence | "Provides a bounded repeated SQLite durable-job soak contract with digest and queue-drain invariants"; not distributed soak, capacity, SLO, or production readiness |
| Current static/package gate after scale evidence | Current tree includes digest-bound benchmark/runtime artifacts and their schema/diff tests | Ruff passes; Mypy reports no issues in 466 source files; package build and `git diff --check` pass | Local Windows package evidence; hosted security/provenance/repository-security and release approval remain external | Local static/package evidence | "Current tree passes local lint, type, package, and diff gates"; not hosted supply-chain approval or production readiness |
| Persistent durable-job scheduler cursor | SQLite migration 34 and PostgreSQL migration `0079_pg_job_cursor` persist a tenant-scoped, lane-digest-bound round-robin cursor; the application reserves each candidate atomically before the lease-fenced worker claim, and backup/export includes cursor state | Restart, lane-drift, and two-independent-connection SQLite contention contracts pass; the new `test_live_postgres_persistent_scheduler_coordinates_spawned_processes` gate passes three local repetitions with two spawned workers, one exact claim per lane, cursor `(next_index=1, version=3)`, and zero remaining leases; PostgreSQL schema/RLS/Alembic/grant contracts and workflow invocation are covered | One-host PostgreSQL 16 service with synthetic tenants; cross-host fairness, throughput, queue HA/failover, soak, capacity, RPO/RTO, and production sizing remain unverified | Experimental / bounded coordination evidence | "Coordinates a declared durable-job lane sequence across scheduler restarts and two independent PostgreSQL worker processes"; not cross-host fairness, distributed SLO, queue HA, or production readiness |
| PostgreSQL durable-job crash/checkpoint recovery | Existing lease generations and partition checkpoints; spawned crash and database-fault workers exit only after committing one partition, and recovery reclaims only an expired lease | `test_live_postgres_worker_crash_after_checkpoint_resumes_without_duplicate_effect` and E-664's `test_live_postgres_worker_database_fault_after_checkpoint_resumes_without_duplicate_effect` pass repeated local runs with generation-2 takeover, exactly two partition effects, ordered `claimed/taken_over/released` lease events, completed terminal state, and zero residual lease; server-boundaries invocation is asserted | One-host PostgreSQL 16 synthetic process/database-session failure only; no queue HA, cross-host recovery, capacity, soak, RPO/RTO, or production SLO evidence | Experimental / bounded recovery evidence | "Resumes a PostgreSQL durable job after a committed checkpoint and worker/database-session failure without duplicating the partition effect"; not HA/DR or production recovery assurance |
| PostgreSQL close intercompany business-period binding | `PostgresConsolidationCloseRepository` validates intercompany artifact `period_name` against the replay-verified worksheet business period while retaining internal PGCCP row identities for relational scope | Isolated PostgreSQL 17.10 non-privileged runtime passes ownership, close/intercompany linking/certification/reversal, PPA, and deferred-tax contracts 18/18; focused Ruff/Mypy pass | One local single-node database with synthetic data; no statutory accounting, hosted CI, live ERP/bank provider, write-back, or HA/DR evidence | Experimental / bounded PostgreSQL correctness evidence | "Binds PostgreSQL intercompany evidence to the verified business period without conflating database row identity"; not statutory close, provider interoperability, or production readiness |
| Current PostgreSQL IAM/RLS contract focus | Selected access/identity/security-governance, delegation, policy-analysis, service-account, and privileged-session adapters run at migration head under forced tenant scope and a non-privileged role | Fresh PostgreSQL 17.10 runtime passes 23/23 selected live contracts with tenant isolation and lifecycle/policy assertions | One local synthetic database; no federation interoperability, complete API/job/export/UI adoption, distributed invalidation, HA/DR, or production IAM assurance | Experimental / bounded IAM runtime evidence | "Exercises selected tenant-scoped IAM and privileged-session contracts under a non-privileged PostgreSQL role"; not complete enterprise IAM or production readiness |
| Current PostgreSQL database-reference live focus | `reference-postgres-readonly` executes only named parameterized profiles in bounded read-only transactions and canonicalizes finite Decimal rows under tenant scope | Fresh PostgreSQL 17.10 migration-head runtime passes the live database-reference suite 3/3 with query, cursor/replay, digest, and tenant-isolation assertions | Deployment-provided PostgreSQL views only; no live ERP/bank vendor schema, credentials/vault, write-back, hosted provider, HA/DR, or production interoperability | Experimental / bounded connector runtime evidence | "Exercises the PostgreSQL named-query reference connector against a non-privileged tenant-scoped runtime"; not a live vendor connector or production write-back |
| Live PostgreSQL queue-health HTTP route | Authenticated `GET /api/v1/ops/durable-jobs/queue` uses the PostgreSQL identity pool, service-account `ops.read`, tenant RLS boundary, and the shared sanitized queue projection; the live fixture creates one queued job in tenant A only | Local PostgreSQL 16.14 non-privileged `TestClient` contract passes: tenant/workspace/entity projection, queued count/depth, redaction of job ID/digest, and sibling-tenant credential rejection | One disposable host and synthetic service account/job; no hosted attestation, distributed IAM invalidation, queue HA, throughput, host loss, provider behavior, or production SLO | Experimental / bounded HTTP/RLS evidence | "Exercises the authenticated queue-health route through a non-privileged PostgreSQL runtime"; not complete enterprise IAM, queue HA, or production readiness |
| Current full local regression after PostgreSQL period-binding fix | Current tree includes the PostgreSQL close business-period correction and the E-475/E-476/E-477 evidence slices | `uv run --no-sync pytest -q --tb=short -ra` exits 0 in 356.4s with no collection or executed failure; declared capability skips and existing warnings remain visible | One Windows/Python 3.12 environment; hosted matrices, native backup tooling, hosted security/provenance, live ERP/bank providers/write-back, statutory accounting, independent HA/DR, and production approval remain external | Local regression evidence | "Current tree passes the complete local pytest regression after the PostgreSQL period fix"; not hosted release approval or global objective completion |
| Live API FinanceRead authorization consistency | `_server_finance_workspace` re-evaluates FinanceRead with read/manage/validate any-of semantics; exact mutation permissions remain unchanged and the legacy identity fixture selects the ledger compatibility boundary explicitly | Fresh PostgreSQL 17.10 non-privileged runtime passes 24/24 selected live API tests for emergency access, WebAuthn, identity, federation, SCIM, service accounts, and metrics; Ruff/Mypy/diff-check pass | One disposable single-node database with synthetic identities; no complete enterprise IAM, federation provider assurance, live ERP/bank provider/write-back, HA/DR, or production SLO | Experimental / bounded route authorization evidence | "Preserves FinanceRead any-of authorization through the server scope re-check"; not complete enterprise IAM or production readiness |
| Current full local regression after API authorization fix | Current tree includes the FinanceRead any-of correction, legacy identity boundary fixture, and E-479 live API evidence | `uv run --no-sync pytest -q --tb=short -ra` exits 0 in 354.3s with no collection or executed failure; declared capability skips and existing warnings remain visible | One Windows/Python 3.12 environment; hosted matrices, hosted security/provenance, live ERP/bank providers/write-back, statutory accounting, independent HA/DR, and release approval remain external | Local regression evidence | "Current tree passes the complete local pytest regression after the API authorization fix"; not hosted release approval or global objective completion |
| Current PostgreSQL application-parity batch | Migration-head PostgreSQL adapters and the Finance Core API live route boundary execute under tenant RLS and a non-privileged role; the Finance Core test seam injects both scope resolvers/helpers | Fresh PostgreSQL 17.10 runtime passes all 117 selected application/scope tests; database removed after verification | Single-node synthetic database only; full parity inventory, hosted backup, live ERP/bank providers/write-back, HA/DR, scale/soak, and production SLO remain external | Experimental / bounded PostgreSQL parity evidence | "Passes the selected PostgreSQL application-parity batch under a non-privileged role"; not full parity, production readiness, or global objective completion |
| Live PostgreSQL write-back history persistence | `POSTGRES_WRITEBACK_SCHEMA_SQL` is created after the shared forced-RLS tenant foundation; `PostgresWritebackIntentRepository` stores immutable, digest-bound lifecycle versions with tenant/workspace scope and optimistic idempotency | Disposable PostgreSQL 16.14 runtime with a non-privileged role passes `tests/test_postgres_writeback.py` 2/2, including sibling-tenant exclusion, version conflict, append-only history, and immutable UPDATE refusal; database and role are removed after verification | Single-node synthetic persistence only; no live ERP/bank provider status API, provider authentication, accounting posting, distributed idempotency, HA/DR, or production write-back | Experimental / bounded PostgreSQL persistence evidence | "Persists tenant/workspace-scoped, append-only write-back intent history in PostgreSQL under forced RLS"; not live provider integration, accounting posting, distributed idempotency, HA/DR, or production readiness |
| Deterministic consolidation impairment bridge | `reconforge/domain/consolidation_impairment.py` computes exact per-unit loss/headroom/status and aggregate totals from source-bound carrying/recoverable Money, with canonical ordering, maker-checker lineage, replay digest, and explicit `posted: false`; read-only `reconforge consolidation impairment-bridge` CLI uses the closed request contract | `tests/test_consolidation_impairment.py` passes 5/5 for arithmetic, permutation digest, maker-checker/currency refusal, derived-field tamper detection, JSON Schema, and CLI; module registry, ADR 0393, Ruff, and package manifest align | Local synthetic inputs only; no valuation methodology, CGU policy, statutory impairment recognition, tax, journal posting, ERP/bank write-back, independent assurance, HA/DR, or production close runtime | Experimental / bounded non-posting financial calculation | "Provides a deterministic, replay-verifiable non-posting consolidation impairment bridge under declared carrying and recoverable inputs"; not a statutory impairment engine, valuation opinion, tax advice, journal posting, live provider/write-back, or production readiness |
| PostgreSQL consolidation impairment evidence | Alembic `0066_pg_impairment` plus `0077_pg_imp_tax_scope`, `ConsolidationImpairmentApplicationService`, and `PostgresConsolidationImpairmentRepository` persist canonical request/result JSONB under hierarchy-aware forced RLS, recompute digests, replay-verify reads, require distinct actors, emit audit evidence, and reject posted/update/delete paths | E-654 `tests/test_api_consolidation_tax_impairment_live.py` runs `alembic upgrade head` to `0085_pg_reversal_definer` on a clean PostgreSQL 16 Alpine runtime, then passes authenticated POST/GET/replay/idempotency/sibling-tenant assertions with a non-superuser/non-`BYPASSRLS` role | Synthetic single-node tenant-scoped evidence with no workspace-header claim; no valuation methodology, statutory recognition, journal posting, live provider/write-back, downgrade/rollback, restore, HA/DR, or production assurance | Experimental / bounded PostgreSQL evidence provenance | "Persists and serves a deterministic, replay-verifiable consolidation impairment bridge with optional hierarchy scope through an authenticated PostgreSQL API"; not a statutory impairment engine, valuation opinion, journal posting, write-back, HA/DR, or production readiness |
| Scope-bound consolidation impairment API | `POST /api/v1/consolidation-impairment` and `GET /api/v1/consolidation-impairment/{artifact_id}` are server-profile-only, use canonical strict Money models, bind preparation to the authenticated actor, require independent approval, re-evaluate tenant policy, and fail closed without PostgreSQL | E-654 `tests/test_api_consolidation_tax_impairment_live.py` runs Alembic head on a clean PostgreSQL runtime and passes the real authenticated POST/GET/replay/idempotency/sibling-tenant path; the role is non-superuser/non-`BYPASSRLS` and `posted=false` is asserted | Migration-head single-node tenant runtime only; no downgrade/rollback, valuation/statutory policy, journal posting, live provider/write-back, restore, HA/DR, or production assurance | Experimental / bounded financial API control | "Provides a tenant-policy-bound PostgreSQL server API for replay-verifiable non-posting consolidation impairment evidence"; not a statutory impairment engine, valuation opinion, journal posting, write-back, HA/DR, or production readiness |
| PostgreSQL close impairment evidence binding | `0067_pg_close_impairment_links` and `PostgresConsolidationCloseRepository.attach_impairment_artifact` replay-verify the immutable impairment artifact, bind business period/currency/entity to the worksheet, require an independent linker, and include sorted result digests in the close bundle | Static migration/schema contracts, close-bundle digest contract, and injected server API scope test pass; live test is capability-gated on `RECONFORGE_TEST_POSTGRES_DSN` | No unskipped current live PostgreSQL link/restore/HA test; non-posting evidence only and no valuation methodology, statutory recognition, journal posting, provider/write-back, or production assurance | Experimental / bounded close-evidence provenance | "Binds replay-verified impairment evidence to a PostgreSQL close run and exposes its digest in the close bundle"; not a statutory impairment engine, close approval by itself, posting, write-back, HA/DR, or production readiness |
| PostgreSQL close deferred-tax evidence binding | `0068_pg_close_deferred_tax_links` and `PostgresConsolidationCloseRepository.attach_deferred_tax_artifact` replay-verify the immutable deferred-tax artifact, bind business period/currency/entity to the worksheet, require an independent linker, and include sorted result digests in the close bundle | Static migration/schema contracts, close-bundle digest contract, and injected server API scope test pass; live test is capability-gated on `RECONFORGE_TEST_POSTGRES_DSN` | No unskipped current live PostgreSQL link/restore/HA test; non-posting evidence only and no statutory tax accounting, journal posting, provider/write-back, or production assurance | Experimental / bounded close-evidence provenance | "Binds replay-verified deferred-tax evidence to a PostgreSQL close run and exposes its digest in the close bundle"; not statutory tax recognition, posting, write-back, HA/DR, or production readiness |
| PostgreSQL close PPA evidence binding | `0069_pg_close_ppa_links` and `PostgresConsolidationCloseRepository.attach_ppa_artifact` replay-verify the immutable PPA artifact, bind business period/currency/entity to the worksheet, require an independent linker, and include sorted result digests in the close bundle | Static migration/schema contracts, close-bundle digest contract, injected server API scope test, and focused close/API/authorization tests pass; live test is capability-gated on `RECONFORGE_TEST_POSTGRES_DSN` | No unskipped current live PostgreSQL link/restore/HA test; non-posting purchase-accounting evidence only and no valuation policy, statutory recognition, journal posting, provider write-back, or production assurance | Experimental / bounded close-evidence provenance | "Binds replay-verified PPA evidence to a PostgreSQL close run and exposes its digest in the close bundle"; not purchase-accounting approval, statutory recognition, posting, write-back, HA/DR, or production readiness |
| PostgreSQL ownership-change evidence | `0070_pg_ownership_change`, `OwnershipChangeApplicationService`, `PostgresConsolidationOwnershipChangeRepository`, and the authenticated server-profile POST/GET routes persist canonical request/result JSONB under forced RLS, recompute request/result digests, bind the preparer to the authenticated identity, require distinct identity actors, emit audit creation evidence, and reject posted/update/delete paths | Schema/migration contracts plus `tests/test_api_consolidation_ownership_change.py` pass; the opt-in live PostgreSQL gate passes with a non-superuser/non-BYPASSRLS role for login, step-up, create/read replay, non-posting output, and sibling-tenant denial | Synthetic single-node runtime with the artifact intentionally tenant-scoped rather than workspace-scoped; direct schema SQL runtime did not exercise an Alembic upgrade in this gate; no statutory ownership-change treatment, legal-book posting, provider write-back, restore, HA/DR, or production assurance | Experimental / bounded PostgreSQL evidence provenance | "Persists and serves a tenant-isolated, replay-verifiable non-posting ownership-change proposal"; not statutory accounting, goodwill/tax policy, posting, write-back, HA/DR, or production readiness |
| Local ownership-change CLI | `reconforge consolidation ownership-change` parses one closed JSON request with exact Decimal percentages and canonical Money, invokes `prepare_ownership_change_adjustment`, and emits only a balanced digest-bound `posted: false` result | `tests/test_consolidation_cli.py` and `tests/test_consolidation_ownership_changes.py` pass focused CLI/domain contracts; package manifest includes the ADR and test | Local synthetic files only; no persistence, statutory accounting, journal posting, live provider/write-back, PostgreSQL runtime, HA/DR, or production assurance | Experimental / bounded local operator surface | "Prepares a deterministic non-posting ownership-change proposal from a closed local request"; not statutory accounting, posting, write-back, or production readiness |
| PostgreSQL close ownership-change evidence binding | `0071_pg_close_ownchg_links` and `PostgresConsolidationCloseRepository.attach_ownership_change_artifact` replay-verify the immutable artifact, bind period/currency/entity to the worksheet, require an independent linker, and include sorted result digests in the close bundle | Static migration/schema, bundle, focused close/API, and authorization contracts pass; live PostgreSQL runtime is capability-gated on `RECONFORGE_TEST_POSTGRES_DSN` | No unskipped current live link/restore/HA test; evidence provenance only and no statutory ownership-change accounting, journal posting, provider write-back, HA/DR, or production assurance | Experimental / bounded close-evidence provenance | "Binds replay-verified ownership-change evidence to a PostgreSQL close run and exposes its digest in the close bundle"; not statutory accounting, posting, write-back, HA/DR, or production readiness |
| Hierarchy-scoped PostgreSQL transactional outbox worker | Migration `0073_pg_outbox_scope` adds nullable workspace/organization/legal-entity attribution, transaction-local defaults, a scope-aware pending index, and explicit hierarchy RLS; atomic claims bind optional hierarchy predicates and return attribution; the worker authorizes deterministic four-part lanes before connection access and restores the same scope for every delivery transition; central ABAC and cache now include organization scope | Focused outbox/worker/migration/policy/cache contracts pass with exact claim parameters, policy-before-connection, GUC propagation, compatibility rows, organization deny-by-default, targeted invalidation, and hierarchy mismatch refusal; ADR 0409 and the migration helper are packaged; full regression/package gate is E-539 | Local static/fake-connection evidence plus capability-gated PostgreSQL tests; no live provider delivery, distributed queue fairness, throughput, HA/DR, federation, or production IAM assurance | Experimental / bounded worker and policy control | "Provides a tenant/workspace/organization/legal-entity-scoped PostgreSQL outbox worker contract with fail-closed lane checks and organization-aware policy evaluation"; not live provider integration, queue HA, production throughput, or production readiness |
| Hierarchy-scoped PostgreSQL outbox consumer receipts | Migration `0074_pg_outbox_consumer_scope` adds nullable hierarchy attribution, transaction defaults, a scope-aware receipt index, and explicit hierarchy RLS; the consumer checks source-event attribution before invoking effects and persists immutable receipts with matching scope | Focused consumer schema, validation, migration downgrade-safety, legacy compatibility, and package-membership tests pass; ADR 0410 and helper are included; full regression/package gate is E-542 | Local static/fake-connection evidence with capability-gated PostgreSQL runtime; no external broker exactly-once, provider acknowledgement, throughput, HA/DR, or production readiness | Experimental / bounded idempotency control | "Provides hierarchy-scoped PostgreSQL consumer receipt integrity with fail-closed event attribution"; not external broker exactly-once, provider delivery, HA/DR, or production readiness |
| Organization-scoped durable-job lanes | DurableJob, SQLite migration 33, PostgreSQL migration `0075_pg_job_organization_scope`, queue/replay/claim filters, backup import/export, and organization-aware RLS carry the same optional organization attribution | Focused durable-job, backup/restore, scheduler, migration-chain, package, and 2,642-test regression gates pass; Mypy 482 files, Ruff, Bandit, OSV pip-audit, build, and diff checks pass; ADR 0412 is packaged | Live PostgreSQL and hosted CI/security/provenance are capability-gated; no distributed queue fairness, provider IAM, HA/DR, or production release evidence | Experimental / bounded isolation primitive | "Preserves organization attribution through durable-job replay, queue bounds, claims, and RLS in local and PostgreSQL modes"; not distributed queue capacity, HA/DR, or production readiness |
| Reconciliation-as-Code sequential strategy adapters | RAC v1 maps `carry_forward`, `sequence_window`, and `reversal_pairing` to the existing bounded carry-forward/reversal strategy manifests and reports replay-verified allocations, pairs, and residual IDs | `tests/test_reconciliation_as_code_sequential.py` plus RAC duplicate/phase-2/strategy/carry-forward/reversal suites pass 45 tests; closed JSON Schema, incompatible-adapter refusal, ADR 0413, and source-distribution membership pass | E-693 extends the disposable PostgreSQL worker gate with live carry-forward, contiguous `sequence-window`, and explicit reversal runs whose persisted lineage/digests match direct strategies; cross-engine parity, large-scale performance, settlement posting, provider interoperability, and write-back remain unverified | Experimental / bounded matching contract | "Declares and exercises bounded sequential matching strategies through the versioned RAC adapter and a synthetic PostgreSQL worker boundary"; not production settlement, statutory posting, provider write-back, or scale readiness |
| Reconciliation API hierarchy read policy | Reconciliation run list/detail and input/result/exception reads call central any-of server policy immediately before PostgreSQL access; mutation guards pass optional organization and legal-entity scope alongside tenant/workspace | `tests/test_api_server_reconciliation.py` and `tests/test_api_execution_scope.py` pass 14 tests with exact hierarchy assertions; `tests/test_api_server_reconciliation_live.py` passes real PostgreSQL submit/list/detail/input HTTP, exact-decimal string, workspace, and sibling-tenant checks; Ruff/Mypy pass and ADRs 0414/0483 are packaged | One disposable single-node synthetic runtime; complete API/job/export/UI adoption, federation, distributed revocation, live bank/ERP sources, worker completion, HA/DR, and production IAM remain unverified | Experimental / bounded route IAM and HTTP/RLS control | "Exercises authenticated reconciliation HTTP persistence through PostgreSQL with hierarchy scope and exact-decimal output"; not complete enterprise IAM, live provider integration, posting, or production readiness |
| PostgreSQL bank statement evidence server boundary | Alembic `0084_pg_bank_statement`, `PostgresBankStatementRepository`, forced tenant/workspace RLS, immutable triggers, workspace-scoped idempotency, and authenticated server-profile route selection persist the replay-verified bank control as bounded JSONB; local mode remains SQLite and network dispatch is disabled | `tests/test_postgres_bank_statement.py` and `tests/test_api_server_bank_statement.py` pass focused/static checks and an opt-in disposable PostgreSQL 16 non-superuser run covering migration, replay/list/read verification, workspace isolation, sibling-tenant denial, and immutable-update refusal; workflow invokes both files; Mypy/Ruff pass | One local single-node synthetic runtime; no live bank/provider authentication, source authenticity, payment execution, posting, ERP write-back, workers, hosted CI, HA/DR, or production operation | Experimental / bounded PostgreSQL server evidence | "Provides tenant/workspace-scoped replay-verified PostgreSQL persistence for bank statement control evidence through the server API"; not live bank interoperability, payment execution, posting, write-back, HA/DR, or production readiness |
| Modern Studio English/Arabic accessibility gate | Current React/Vite Studio projections expose bounded synthetic evidence in English and Arabic with keyboard, contrast, reduced-motion, color-safe, redaction, and responsive landmark contracts | TypeScript typecheck passes; Vitest passes 12 files / 67 tests; production Vite build passes; Playwright accessibility passes 9/9 across critical routes and mobile landmarks | Local browser/runtime only; no authenticated live API browser session, provider authenticity, posting/write-back, hosted deployment, HA/DR, or production availability | Experimental / bounded browser evidence | "Provides a locally verified English/Arabic read-only Studio accessibility surface for synthetic evidence"; not live operations or production readiness |
| Public World Bank reference connector online evidence | `world-bank-public-readonly` uses the shared no-auth pinned-HTTPS executor, exact page offsets, finite-decimal schema, canonical response digest, and cross-format parity | `tests/test_connector_world_bank_public.py` passes 7/7 with the opt-in live fetch plus schema, pagination, digest, parity, and unsafe-input/network-policy contracts | One public endpoint and bounded synthetic/public records; no bank/ERP customer authentication, source-authenticity attestation, provider SLA, payment execution, write-back, hosted deployment, or production operations | Experimental / public-data reference evidence | "Exercises a bounded no-auth public-data reference connector against the World Bank endpoint"; not a live bank/ERP connector or production readiness |
| PostgreSQL consolidation-close hierarchy persistence | Migration `0078_pg_close_scope` persists nullable organization/legal-entity attribution across all eleven close tables, adds tenant-safe foreign keys and hierarchy indexes, applies workspace-aware period/run RLS, hierarchy RLS, and NULL-aware scoped uniqueness; repositories restore the same transaction-local scope | Focused migration/schema/close contracts pass; Ruff, Mypy, diff-check, and package membership are tracked by E-570/E-571; live runtime remains capability-gated | No current unskipped live PostgreSQL hierarchy isolation, statutory consolidation posting, provider write-back, HA/DR, or production approval | Experimental / bounded PostgreSQL persistence evidence | "Persists optional hierarchy attribution and fail-closed RLS for PostgreSQL consolidation-close records"; not statutory posting, provider write-back, HA/DR, or production readiness |
| PostgreSQL hierarchy downgrade safety | Migrations `0072`, `0073`, `0075`, `0076`, and `0077` run pre-mutation database guards and refuse to drop scope columns while non-NULL hierarchy attribution exists | Static migration contracts and local regression/package gates; live downgrade drill is still required | No current live PostgreSQL downgrade execution, provider, statutory, HA/DR, or production release evidence | Experimental / bounded migration-safety control | "Refuses unpublished PostgreSQL hierarchy downgrades that would discard non-NULL attribution"; not proof of live recovery, HA/DR, or production readiness |
| Bounded PostgreSQL server-profile connection reuse | `PostgresPooledConnectionFactory` preserves the existing factory subtype, routes server-profile leases through `PostgresConnectionPool`, and registers bounded shutdown cleanup | Focused foundation/API contracts pass with one declared live-PostgreSQL skip; 2,655-test regression, package build, Ruff, Mypy, and diff-check pass under E-575 | No throughput, capacity, soak, distributed fairness, HA/DR, or production sizing evidence | Experimental / bounded resource-control evidence | "Bounds PostgreSQL server-profile connection reuse with rollback-on-release"; not a throughput, capacity, HA/DR, or production-readiness claim |
| PostgreSQL metrics parity fixture | The live metrics test installs every schema family referenced by close, exception, evidence, control, reconciliation, and matching metric queries before the metrics schema | Focused/static contracts pass with one declared local live-PostgreSQL skip; 2,655-test regression, package build, and diff-check pass under E-576; hosted runtime rerun remains required | No current live runtime, provider/statutory metrics, throughput, HA/DR, or production evidence | Experimental / test-environment correctness | "Keeps the PostgreSQL metrics parity fixture schema-complete"; not a production metrics or readiness claim |
| Bandit coverage for hierarchy-scoped PostgreSQL reads | PPA, impairment, and deferred-tax scoped reads retain parameterized values and document the fixed internal `_scope_where()` selector at every B608 site | Full Bandit reports no failed findings; focused repository/security contracts, Ruff, Mypy, and the current full regression pass under E-577 | No independent penetration test, hosted security rerun, provider, HA/DR, or production assurance | Experimental / static security coverage | "Keeps fixed hierarchy SQL clauses visible to Bandit without weakening parameter binding"; not penetration-test or production-security assurance |
| Local retail settlement persistence | SQLite migration 35 and `SQLiteRetailSettlementRepository` persist one digest-bound retail settlement report per workspace/decision digest, enforce immutable triggers and `finance_core.manage`, verify outer and nested replay digests on read, and include the table in local backup/restore | `tests/test_sqlite_retail_settlement.py` and backup/export regressions pass 39/39; inventory-focused contracts pass 11/11; Ruff, Mypy, package build, and diff-check are release gates | Local synthetic SQLite only; no retail API/Studio, live processor authenticity/conformance, settlement finality/fraud, posting, write-back, PostgreSQL parity, HA/DR, or production evidence | Experimental / bounded local persistence | "Persists and restores workspace-scoped, replay-verifiable retail settlement evidence locally"; not live retail settlement, posting, write-back, HA/DR, or production readiness |
| Local retail settlement API and CLI boundary | `reconforge retail settlement settlement-run --persist --database` and authenticated `/api/v1/retail/settlements` persist/list/read the same replay-verified SQLite evidence; write requires `finance_core.manage`, reads require the closed finance read/manage/validate any-of policy, and PostgreSQL server mode refuses without SQLite fallback | `tests/test_retail_settlement.py`, `tests/test_api_retail_settlement.py`, and `tests/test_api_authorization_inventory.py` pass; current inventory is 249 routes with digest `39f5354d214e6454d008ca778ccf126609a7c64aa9fae4478a424823e4684736`; Ruff, Mypy, package build, and diff-check pass | Local authenticated API/CLI composition only; no Retail Studio/accessibility evidence, PostgreSQL parity, live processor authenticity/conformance, settlement finality/fraud, posting, write-back, HA/DR, or production retail evidence | Experimental / bounded local API surface | "Provides an authenticated local API and CLI boundary for replay-verifiable retail settlement evidence"; not live retail operations, posting, write-back, HA/DR, or production readiness |
| PostgreSQL retail settlement evidence adapter and server API boundary | `0080_pg_retail_settlement`, `PostgresRetailSettlementRepository`, and `server_retail_settlement` persist the same bounded report as JSONB under forced tenant/workspace RLS, immutable trigger protection, transaction-scoped idempotency, and no SQLite fallback | `tests/test_postgres_retail_settlement.py` covers schema/migration and an opt-in non-privileged live drill; `tests/test_api_server_retail_settlement.py` covers authorized scope propagation, server route selection, and real HTTP list/read; focused API/migration/module/threat tests pass; Mypy and Ruff pass | Disposable PostgreSQL 16 non-superuser HTTP/RLS run passes locally; hosted parity remains unverified | Experimental / bounded PostgreSQL persistence | "Provides tenant/workspace-scoped replay-verified PostgreSQL persistence for retail settlement evidence through the server API"; not hosted parity, HA/DR, live processor authenticity, posting, write-back, or production readiness |
| Retail settlement modern Studio projection | `apps/web/src/components/RetailSettlementStudio.tsx` renders the strict synthetic projection at `/retail-settlement`, preserves exact decimal strings, exposes status/variance/reason/digest evidence, and has explicit non-posting/provider boundaries in English and Arabic | `apps/web/src/components/RetailSettlementStudio.test.tsx` passes with malformed-contract rejection; web typecheck, 58 component tests, production build, and `apps/web/e2e/accessibility.spec.ts` pass 6/6 including WCAG checks in English and Arabic | Local synthetic browser contract only; no authenticated live API browser session, provider authenticity, payment finality, posting, write-back, hosted deployment, HA/DR, or production evidence | Experimental / bounded modern Studio | "Provides a read-only English/Arabic Studio view for synthetic, replay-digested retail settlement evidence"; not live retail operations, source authenticity, settlement finality, posting, write-back, or production readiness |
| CI all-extra optional test dependency surface | `.github/workflows/ci.yml` keeps `uv sync --locked --all-extras` and runs an immediate import probe for `cbor2`, `cryptography`, and `opentelemetry.sdk` before collection | `tests/test_phase4_execution_contract.py` passes 7/7 for installation order and module coverage; isolated local Python 3.11 all-extra sync imports all three modules | Local workflow and installation observation; no hosted rerun, package provenance, or live service evidence | Quality-control contract | "Fails fast with a named diagnostic when the locked all-extra CI test environment is incomplete"; not hosted CI success or production readiness |
| Benchmark evidence index | `benchmark-evidence-index-v1` binds selected matching/durable-job JSON artifacts to repository-relative paths, canonical LF file hashes, profile IDs, digest-field allowlists, workload families, status, and explicit boundaries; the verifier is packaged and called before test collection | `tests/test_benchmark_evidence_index.py` passes 6/6 for valid artifacts, direct-script source-tree import, CRLF/LF canonicalization, and hash/path/claim rejection; `verify_benchmark_index.py --root .` emits eight verified entries; current Gitleaks 8.30.1 history/tree scan is clean with narrowly fingerprinted synthetic false-positive entries | Checked-in artifact traceability and current-tree secret scanning only; no workload rerun, hosted cross-engine result, distributed capacity, provider authenticity, SLO, or production-sizing evidence | Quality-control / benchmark provenance | "Verifies that selected benchmark artifacts are the intended, untampered files with bounded claims"; not a throughput, capacity, SLO, or production-readiness claim |
| Professional invoice/payment modern Studio projection | `apps/web/src/components/ProfessionalInvoicePaymentStudio.tsx` renders the strict synthetic projection at `/professional-invoice-payment`, preserves exact decimal/day strings, exposes status/reason/payment evidence, and has explicit non-posting/billing boundaries in English and Arabic | `apps/web/src/components/ProfessionalInvoicePaymentStudio.test.tsx` plus web typecheck, component suite, production build, and `apps/web/e2e/accessibility.spec.ts` cover the route and WCAG path | Local synthetic browser contract only; no authenticated live API browser session, billing/payment authenticity, receivables allocation, revenue recognition, posting, write-back, hosted deployment, HA/DR, or production evidence | Experimental / bounded modern Studio | "Provides a read-only English/Arabic Studio view for synthetic, replay-digested professional invoice/payment evidence"; not live billing operations, source authenticity, allocation, posting, write-back, or production readiness |
| Individual/freelancer cashflow control | `reconforge.domain.individual_cashflow_control`, application boundary, local CLI, authenticated stateless API, JSON Schema, examples, `control-packs/individual-cashflow`, and the strict read-only `/individual-cashflow` Studio projection provide exact period/type/category budget decisions and nested/outer digest verification | `tests/test_individual_cashflow_control.py`, `tests/test_individual_cashflow_cli.py`, `tests/test_api_individual_cashflow.py`, `apps/web/src/components/IndividualCashflowStudio.test.tsx`, accessibility route coverage, module/threat/authorization inventory tests; Python and web gates pass locally | Local synthetic/operator-selected exports only; no bank authenticity/connectivity, tax/legal classification, posting, write-back, hosted persistence, HA/DR, or production evidence | Experimental / bounded local control and modern Studio | "Provides a local deterministic non-posting income/expense budget control and read-only English/Arabic Studio projection for individual and freelancer exports"; not tax advice, bank integration, posting, write-back, or production readiness |
| Bounded repeated PostgreSQL durable-job soak | `reconforge.benchmark.postgres_durable_job_soak` reuses the PostgreSQL multi-worker scale contract over three unique synthetic tenant-lane iterations while holding the job-ID prefix and profile shape stable; verifier requires exact completion, zero duplicates/residue, and equal effect-set digests | Live E-674 run completes 192/192 jobs and 768/768 effects with three equal effect digests; focused profile/packaging tests pass; digest-bound report and ADR 0510 are packaged | One disposable PostgreSQL 16 host and synthetic workload only; no distributed soak, queue HA, automatic failover, host loss, capacity, throughput, RPO/RTO, or production scheduling evidence | Experimental / bounded repeatability evidence | "Provides bounded repeated PostgreSQL durable-job correctness evidence with deterministic effect-set replay"; not distributed capacity, HA, SLO, or production readiness |
