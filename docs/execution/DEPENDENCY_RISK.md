# Dependency Risk

Measured through 2026-08-22. Vulnerability scan success is not the same as reproducible resolution or low operational risk.

| Area | Evidence | Risk | Priority / control |
| --- | --- | --- | --- |
| Python runtime/server/observability/backup/federation/MFA/tool resolution | `uv.lock` format v1/revision 3 closes 128 non-root packages across runtime, build, dev, docs, DuckDB, observability, backup, server, federation, and MFA extras. Every registry distribution record has an official PyPI/files HTTPS source, SHA-256, positive size, and upload time; uv 0.11.32 and the absolute 2026-08-02 cutoff are policy-pinned. `defusedxml 0.7.1` protects XLSX XML parsing; boto3 1.43.56 and its locked transitives provide the S3 boundary; OpenTelemetry API/SDK and official OTLP HTTP/protobuf exporter 1.44.0 provide optional no-export-by-default instrumentation plus explicitly configured collector delivery; cryptography 50.0.0 provides AES-256-GCM for optional encrypted backups; joserfc 1.7.4 and python3-saml 1.16.0 provide reviewed OIDC/SAML adapters; webauthn 3.0.0 and its cryptographic/CBOR transitives use pyOpenSSL 26.4.0, rather than custom cryptography | The prior 49.0.0 lock was rejected by hosted pip-audit for CVE-2026-69247; the reviewed lock refresh removes that finding without an exception. Advisory services, hosted cross-version execution, reachability, provenance, malware, and license suitability remain time-bounded; python3-saml currently emits an internal `datetime.utcnow()` deprecation warning; OTLP backend/retention/alert operation and direct dependencies require future review | Medium/high: rerun both supported hosted audits on every change/weekly/release, review advisories within the emergency trigger, monitor the SAML dependency warning, exercise a pinned Collector/backend with TLS/mTLS and alert delivery, and retain audit/test evidence without weakening `--locked` |
| Engine parity direct dependencies | `supported-engine-parity-v1` configures exact lower/current-compatible NumPy, Pandas, and DuckDB pins across Python 3.11/3.12, verifies resolution, and rejects selected-test skips; other dependencies now start from `uv.lock` before the three deliberate no-dependency engine overrides | Read-only GitHub audit on 2026-07-25 found the `engine-parity` job exists only in the dirty local workflow and in no remote run. Exact engine overrides are compatibility-test inputs, not production resolution or artifact-hash evidence | High until publication/execution: isolate/publish the matrix, obtain one identified all-green four-cell run, and refresh dated engine pins intentionally |
| Python build dependencies | The signed-candidate definition installs `build 1.5.0`, Windows-conditional `colorama 0.4.6`, `packaging 26.2`, `pyproject-hooks 1.2.0`, `setuptools 83.0.0`, and `wheel 0.47.0` from exact official PyPI wheel SHA-256 values before `--no-isolation` build; application resolution remains separately locked | Two same-machine Python 3.14.6 builds produced identical source, wheel, and normalized-sdist bytes. This does not prove supported Python 3.11/3.12, cross-platform, clean-tag, hosted-runner, or OCI reproducibility | Medium/high: retain the separate reviewed release hash set/normalizer and execute clean supported-version and OCI comparisons |
| Property-test dependency | Hypothesis `6.161.2` is exact in the dev extra and the universal lock; it requires Python >=3.10 and is not a runtime dependency | An exact version still requires deliberate upgrades and does not establish package safety | Medium: keep it inside reviewed lock updates and run property/full gates before accepting an upgrade |
| Python audit | On 2026-08-22 the all-extras hash export exposed `pip 26.1.2` as affected by `PYSEC-2026-3721`; `uv.lock` was refreshed within the existing cutoff to `pip 26.2`, and isolated Python 3.11/3.12 locked runners reported zero known findings with pip-audit 2.10.1 and no active exception. CI and release now call that same fail-closed runner after their locked sync. | The advisory service is time-varying; the changed workflow still needs a hosted Python 3.11/3.12 rerun, and reachability, malware, provenance, and license suitability remain unproven. | High until hosted rerun, then medium/high: keep the two-cell audit on every change/weekly/release and retain exact report/tool/lock identity; never equate a clean scan with safety. |
| Web dependencies | `apps/web/package-lock.json` exists; the current local install audits 161 packages with 0 reported vulnerabilities. The lock inventories 211 non-root entries with HTTPS registry resolution and SRI, including development-only axe-core accessibility tooling | Lock and SRI improve reproducibility and transport integrity but do not prove package provenance, safety, licensing suitability, reachability, or hosted enforcement; caret ranges and future lock updates still require review | Medium: keep `npm ci`, automated review, exact-subject SBOM evidence, license review, and reviewed lock refreshes; obtain hosted evidence for the current 211-entry revision |
| GitHub Actions | Inspected actions use full commit SHAs | Pinned action code reduces tag drift; comments/version labels can become stale | Low/medium: automated digest review and least-privilege permissions |
| Container base and Python install | Python 3.11 slim is digest-pinned; Docker checksum-pins official uv 0.11.32 and performs a runtime-only non-editable `uv sync --locked`; the candidate separately pins/checks Syft 1.49.0 and scans the exact image manifest | The local Docker daemon is unavailable, so neither the revised install nor image inventory ran in a container; synthetic image-SBOM normalization is not image evidence | Medium: execute clean build/doctor/validation/exact-image scan, independently verify signed SBOM, and review base/uv/scanner refreshes per policy |
| CI service containers | PostgreSQL `16` and Redis `7` mutable tags | Server-boundary behavior can drift between runs | Medium: pin image digests and document upgrade cadence |
| Optional server stack | Alembic/psycopg/redis/sqlalchemy remain lower-bounded public metadata but resolve exactly inside the universal lock; local Python 3.11 all-extra sync installed the complete profile; local PostgreSQL 17 tests and one native backup/restore/failed-restore drill provide bounded current-environment evidence | Service images remain mutable major tags; hosted Python 3.12, cross-platform adapter execution, Redis failure recovery, HA, and host-loss restore remain unverified | High for Team profile: execute the locked Python 3.12 hosted live service/migration/adapter/restore gates, add HA/host-loss drills, and pin service-image digests |

## Current allowed conclusion

The isolated locked Python 3.11/3.12 all-extra graph and current npm lock reported no known vulnerabilities in the 2026-08-22 executed audits after the `pip 26.2` remediation. A deterministic universal Python/server lock, exact-subject SBOM definitions, bounded exception policy, weekly update definitions, and fail-closed candidate/security workflow gates exist. This does not prove hosted enforcement of the changed runner, safety, reachability, provenance, license suitability, or container contents. The current npm lock has no recorded SRI gap; the Docker daemon is unavailable, and no hosted signed result exists for this slice.
