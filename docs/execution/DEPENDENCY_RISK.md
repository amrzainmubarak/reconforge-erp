# Dependency Risk

Measured through 2026-07-27. Vulnerability scan success is not the same as reproducible resolution or low operational risk.

| Area | Evidence | Risk | Priority / control |
| --- | --- | --- | --- |
| Python runtime/server/tool resolution | `uv.lock` format v1/revision 3 closes 107 non-root packages across runtime, build, dev, docs, DuckDB, and server extras. Every registry distribution record has an official PyPI/files HTTPS source, SHA-256, positive size, and upload time; uv 0.11.32 and the absolute 2026-07-26 cutoff are policy-pinned. `defusedxml 0.7.1` protects XLSX XML parsing; boto3 1.43.56 and its locked transitives provide the advertised S3 server boundary | A lock can become stale or resolve unsafe code; local sync exercised Python 3.11, and no hosted Python 3.12 or cross-platform install result exists. Both direct dependencies require future advisory and release review | Medium/high: review/regenerate at most every 30 days or on advisory, execute both supported hosted profiles, and retain audit/test evidence without weakening `--locked` |
| Engine parity direct dependencies | `supported-engine-parity-v1` configures exact lower/current-compatible NumPy, Pandas, and DuckDB pins across Python 3.11/3.12, verifies resolution, and rejects selected-test skips; other dependencies now start from `uv.lock` before the three deliberate no-dependency engine overrides | Read-only GitHub audit on 2026-07-25 found the `engine-parity` job exists only in the dirty local workflow and in no remote run. Exact engine overrides are compatibility-test inputs, not production resolution or artifact-hash evidence | High until publication/execution: isolate/publish the matrix, obtain one identified all-green four-cell run, and refresh dated engine pins intentionally |
| Python build dependencies | The signed-candidate definition installs `build 1.5.0`, Windows-conditional `colorama 0.4.6`, `packaging 26.2`, `pyproject-hooks 1.2.0`, `setuptools 83.0.0`, and `wheel 0.47.0` from exact official PyPI wheel SHA-256 values before `--no-isolation` build; application resolution remains separately locked | Two same-machine Python 3.14.6 builds produced identical source, wheel, and normalized-sdist bytes. This does not prove supported Python 3.11/3.12, cross-platform, clean-tag, hosted-runner, or OCI reproducibility | Medium/high: retain the separate reviewed release hash set/normalizer and execute clean supported-version and OCI comparisons |
| Property-test dependency | Hypothesis `6.161.2` is exact in the dev extra and the universal lock; it requires Python >=3.10 and is not a runtime dependency | An exact version still requires deliberate upgrades and does not establish package safety | Medium: keep it inside reviewed lock updates and run property/full gates before accepting an upgrade |
| Python audit | The locked Python 3.11.15 environment synchronized all extras from `uv.lock`; the E-078 hash-exported requirements view SHA-256 `ca41d86b...f9598f` passed pip-audit 2.10.1 with zero known vulnerabilities. E-078 adds no dependency or lock change | The advisory service is time-varying; Python 3.12 hosted execution, reachability, malware, provenance, and license suitability remain unproven | Medium/high: run the two-cell hosted audit on every change/weekly/release and retain exact report/tool/lock identity; never equate a clean scan with safety |
| Web dependencies | `apps/web/package-lock.json` exists; `npm ci` installed 158 packages and reported 0 vulnerabilities. The clean-HEAD source SBOM inventories 209 non-root locked npm entries and verifies root manifest parity | Lock improves reproducibility, but 155 non-root entries currently omit integrity values, caret ranges remain in the manifest, and future lock updates still require review | Medium/high: investigate missing integrity metadata, keep `npm ci`, automated review, exact-subject SBOM evidence, and reviewed lock refreshes |
| GitHub Actions | Inspected actions use full commit SHAs | Pinned action code reduces tag drift; comments/version labels can become stale | Low/medium: automated digest review and least-privilege permissions |
| Container base and Python install | Python 3.11 slim is digest-pinned; Docker checksum-pins official uv 0.11.32 and performs a runtime-only non-editable `uv sync --locked`; the candidate separately pins/checks Syft 1.49.0 and scans the exact image manifest | The local Docker daemon is unavailable, so neither the revised install nor image inventory ran in a container; synthetic image-SBOM normalization is not image evidence | Medium: execute clean build/doctor/validation/exact-image scan, independently verify signed SBOM, and review base/uv/scanner refreshes per policy |
| CI service containers | PostgreSQL `16` and Redis `7` mutable tags | Server-boundary behavior can drift between runs | Medium: pin image digests and document upgrade cadence |
| Optional server stack | Alembic/psycopg/redis/sqlalchemy remain lower-bounded public metadata but resolve exactly inside the universal lock; local Python 3.11 all-extra sync installed the complete profile | Locally skip-guarded live tests still provide no PostgreSQL/Redis compatibility, migration, restore, or failure proof | High for Team profile: execute the locked Python 3.12 live service contract/migration/restore gates and pin service-image digests |

## Current allowed conclusion

The isolated locked Python 3.11 all-extra graph and current npm lock reported no known vulnerabilities in the executed audits. A deterministic universal Python/server lock, exact-subject SBOM definitions, bounded exception policy, weekly update definitions, and fail-closed candidate/security workflow gates now exist locally. This does not prove hosted enforcement, safety, reachability, provenance, license suitability, Python 3.12 behavior, or container contents. Npm still lacks embedded SRI for 155 entries, the Docker daemon is unavailable, and no hosted signed result exists.
