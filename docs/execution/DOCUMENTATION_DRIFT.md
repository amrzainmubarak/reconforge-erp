# Documentation Drift

Measured 2026-07-24. Items are ordered by impact on user decisions and release truth.

| ID | Surface | Observed drift | Required correction | Status |
| --- | --- | --- | --- | --- |
| DOC-001 | `docs/execution/*` | Prior Baseline said Ruff/Mypy passed and Pytest was only environment-blocked; current gates fail Ruff, Mypy, Pytest, and whitespace | Replace with current command evidence and snapshot boundary | corrected; final local gates pass |
| DOC-002 | Claims matrix | Prior wording labeled local mode `Stable`, product `Beta`, and Team/Enterprise `Beta` without qualifying runtime evidence; three CLI-visible module descriptors also remained Beta | Use alpha/experimental/blocked labels, real evidence paths, and enforce module/public ceilings | corrected; schema-v1 maturity policy and synthetic-promotion/public-surface tests pass |
| DOC-003 | README showcase links | Root `DEMO.md` is deleted in the current worktree, but README lines 23/179 and `docs/showcase.md` linked to it | Link to the maintained demo/showcase and scenario guides | corrected; link tests pass |
| DOC-004 | Determinism wording | README said matching uses stable ordering while forced DuckDB partition replay changed the digest on row permutation | Make determinism a release gate, fix the tested regression, and keep wording bounded | corrected; targeted and full gates pass |
| DOC-005 | Product category wording | README headline said "ERP ... Platform" while positioning says finance-controls platform beside ERPs, not an ERP replacement | Use financial reconciliation/integrity controls wording | corrected |
| DOC-006 | Docker evidence | Docker assets/docs exist, but the local daemon is stopped | State current verification boundary rather than inferring runtime success | open; matrix corrected |
| DOC-007 | Performance wording | Strategy and roadmap documents discuss global/enterprise scale, while only a 1K smoke benchmark is verified here | Keep strategic targets clearly planned and bind any runtime claim to hardware/dataset/digest | open |
| DOC-008 | Security wording | Prior security baseline said formal threat-model docs were missing although `docs/security/threat-model.md` exists | Describe actual documents and distinguish presence from verified control effectiveness | corrected in baseline |
| DOC-009 | Repository assessment | `docs/strategy/repository-assessment.md` listed deleted root `ROADMAP.md` and showcase surfaces referenced deleted `DEMO.md` | Point to `docs/roadmap.md`, `docs/showcase.md`, and maintained demo documents | corrected; link tests pass |
| DOC-010 | Variance JSON contract | Exact Decimal migration would have serialized previously numeric threshold/variance values as strings, while the unversioned schema still required numbers; later v2 lacked ingress-policy provenance | Preserve exact JSON number lexemes, retain unversioned/v1 and v2 readers, and emit schema-v3 canonical policy with strict/legacy ingress inside its digest | corrected; current and historical report/schema tests pass |
| DOC-011 | Anonymization sharing boundary | Product docs described safe sharing while default output included a reversible original-to-mask table and amount noise was deterministic/reversible | Remove the raw map from shareable defaults, add an explicit private path and versioned manifest, and change public wording to reviewed risk reduction rather than safe-publication assurance | corrected; 56 focused/broader privacy, exactness, CLI, and schema tests pass |
| DOC-012 | Security architecture narrative | Prose-only architecture could not enforce mode labels, data classes, trust boundaries, control ownership/evidence, or residual-risk parity; data/API descriptions also drifted from optional S3 and bounded server-profile code | Make a closed schema-v2 YAML registry normative, retain a generated-readable companion, link existing evidence, and label Regulated planned-only without implying control operation | corrected; four architecture contracts and the final 832-test suite pass |
| DOC-013 | Module threat-model coverage | The readable threat model listed cross-cutting controls but could not fail when a runtime module, interface, data classification, or declared test lacked module-specific assets, actors, threats, control links, evidence, ownership, and residual limitations | Make a closed schema-v1 active-module index normative and require exact runtime-registry plus readable-view parity without promoting bounded threat evidence | corrected; five dedicated contracts pass; final full-suite evidence is recorded in E-048 |
| DOC-014 | OWASP ASVS alignment | No version-pinned current ASVS mapping existed, so a mutable or incomplete reference could be mistaken for current standards alignment | Pin official stable ASVS 5.0.0 source identity and digest, cover every chapter, assess only 55 selected requirements with evidence-bounded statuses, and mark the other 290 unassessed | corrected; five dedicated contracts pass; no ASVS level, compliance, certification, or full-assessment claim |
| DOC-015 | NIST SSDF alignment | No version/status-pinned SSDF mapping existed, while SSDF 1.2 is now a draft and final SP 800-218A is a separate AI community profile; generic alignment wording could hide missing tasks and operating evidence | Pin final SSDF 1.1 PDF/table identities, map every one of its 42 tasks with owners/gaps/actions/review cadence, exclude the draft from normative use, and keep SP 800-218A separate | corrected; five dedicated contracts pass; all tasks remain partial/planned and no conformance claim is allowed |
| DOC-016 | SLSA/provenance wording | Build, SBOM, pinned-action, and digest assets could be mistaken for signed provenance or a SLSA level without artifact identity, builder trust, independent expectations, failure, and rollback contracts | Pin Approved SLSA 1.2, keep both tracks UNEVALUATED, and version exact artifact/boundary/attestation/verification/rollback gates before pipeline implementation | corrected; five dedicated contracts pass; no attestation, signature, level, or verified property is claimed |
| DOC-017 | Signed-release workflow wording | A keyless attestation workflow definition or one local package rebuild comparison can be mistaken for a signed/reproducible release even when no tag run, OCI comparison, registry push, bundle verification, immutable release, or external control assessment occurred | Name it a non-publishing candidate, require exact signed-tag/identity checks, scope same-machine source/wheel/normalized-sdist parity explicitly, preserve strict verifier bundles, keep P0-SEC-006 in progress, and separate irreversible publication behind a human gate | corrected in definition/runbook; hosted execution, OCI/cross-platform reproducibility, and publication remain open and no signed-release claim is allowed |
| DOC-018 | SBOM wording and identity | Public/security docs described one manual/tag runner-environment SBOM as implemented even though it was not bound to any release subject and used floating tooling | Replace it with release-integrated exact-subject CycloneDX 1.7 definitions, deterministic bounded local outputs, explicit unknown completeness, and consistent hosted/signature/image limitations | corrected; local source/package and synthetic-image contracts pass, but no hosted signed SBOM or completeness assurance is claimed |
| DOC-019 | Dependency/secret gate wording | CI called a broad `pip install -e` path "locked", ambient pip-audit could be mistaken for project-graph evidence, and repository summaries listed security automation without a secret gate or bounded exception process | Add an exact universal Python/server/build lock, machine-count npm SRI limits, checksum-pin history/tree scanning, close update/exception/audit contracts, and distinguish local definitions/results from hosted enforcement | corrected locally under ADR 0069; locked Python 3.11 sync/audit/build/full-suite plus npm/history/tree evidence pass, while hosted 3.11/3.12, Docker/release operation, and 155 missing npm SRI entries remain open |
| DOC-020 | File-ingestion safety wording | Reader/type/path references did not enumerate every parser surface, and selected CSV/XLSX checks could be mistaken for upload, malware, legacy XLS, JSON/YAML, or connector safety | Make a closed schema-v1 inventory normative, fail CI on unregistered direct tabular parsers, document exact budgets/rejections, and preserve partial surfaces plus R-018 | corrected for the bounded tabular slice under ADR 0070; canonical CSV/XLS/XLSX, mapping, DuckDB, and built-in adapter paths are preflighted, while non-tabular/generated readers, malware/quarantine, upload, authenticity, and legacy XLS internals remain open |
| DOC-021 | Structured-document safety wording | Safe YAML tags and post-parse schemas could be mistaken for bounded JSON/YAML parsing while duplicate-key, alias, depth, and direct-parser bypasses remained possible | Version byte/node/depth/collection/scalar/alias ceilings, reject ambiguous structures, close direct PyYAML calls with AST inventory, and distinguish the bounded FI-010 slice from partial generated/close readers | corrected for FI-010 under ADR 0071; configuration/mapping/control YAML is bounded, while close-workflow and generated/restore/Studio/report readers plus malware/authenticity remain open |
| DOC-022 | Close-workflow parser wording | FI-011 was still described as unbounded after its JSON and YAML readers moved behind the shared policy, while generic parse messages could be mistaken for absent structured rejection evidence | Preserve the public generic errors, record their bounded internal cause, remove the stale direct-PyYAML entry, and distinguish structural acceptance from template authenticity, task correctness, approval, or audit sign-off | corrected for FI-011 under ADR 0072; generated/restore/Studio/report readers, legacy XLS internals, malware/authenticity, uploads, and connectors remain open |
| DOC-023 | Generated-manifest safety wording | Versioned digest verification could be mistaken for safe pre-parse resource handling or safe redistribution, while FI-012 combines manifests with redaction/copy paths that have different financial and malware constraints | Bound only the two manifest readers, preserve exact current/legacy verification, keep FI-012 partial, and state that hashes do not prove authenticity, scanning, disclosure approval, or redaction completeness | corrected for the FI-012 manifest-reader slice under ADR 0073; JSON redaction was subsequently bounded under DOC-024, while CSV/text, provenance, malware quarantine, and redistribution controls remain open |
| DOC-024 | JSON-redaction compatibility wording | Exact strict bucketing depended on a direct `parse_float` hook, while calling the whole path bounded would hide unrestricted reads, malformed-JSON text fallback, and destination mutation order | Add exact-float-lexeme representation to the bounded reader, preflight before output preparation, fail closed on invalid JSON, preserve strict/legacy bucket fixtures, and keep the broader FI-012 boundary partial | corrected for JSON redaction under ADR 0074; CSV was subsequently bounded under DOC-025 and text/non-redacted copy/staging under DOC-026; provenance, disclosure authorization, malware quarantine, crash recovery, and redistribution controls remain open |
| DOC-025 | CSV-redaction resource wording | CSV used an inventoried parser but still accumulated all rows and wrote directly, so calling FI-012 bounded would hide missing budgets, duplicate-header ambiguity, and partial-file behavior | Reuse tabular preflight before hashing/output, stream exact field strings, publish one file by temporary replace, register both parser sites, and preserve the broader partial boundary | corrected for CSV redaction under ADR 0075; text/non-redacted copy/staging was subsequently bounded under DOC-026; provenance, disclosure authorization, malware quarantine, crash recovery, and redistribution controls remain open |
| DOC-026 | Client-pack redistribution/atomicity wording | Documentation showed nested demo output without explaining why it can be safe only outside the inventoried evidence subtree, while whole-file replacement decoding, unbounded copies, destructive pre-generation clearing, and the Windows directory-replacement crash window were hidden behind generic sharing language | Freeze candidate/selected sets, reject source-containing and evidence-nested outputs, retain the compatible top-level nested demo path, publish exact budgets, stage the complete pack, prove handled-failure rollback, and distinguish fresh single-rename publication from two-rename existing-directory crash behavior | corrected under ADR 0076; malware/authenticity/disclosure authorization, crash recovery, remaining parser surfaces, and safe redistribution assurance remain open |
| DOC-027 | Interrupted-publication recovery/authentication wording | Calling an unkeyed local marker authenticated would imply trusted authorship, while telling operators only to inspect rollback siblings leaves ambiguous destructive choices after abrupt loss | Use integrity/self-consistency wording, publish the exact recovery command and four-state policy, refuse unknown/tampered state, and retain non-atomicity plus real host/filesystem durability limitations | corrected under ADR 0077; explicit simulated local recovery exists, while authenticated provenance, real loss testing, malware/disclosure controls, and observer/crash atomicity remain open |
| DOC-028 | Database backup/restore integrity wording | Manifest checksums and temporary-database relationship checks could be read as a bounded or authenticated restore boundary even though both JSON files were parsed without byte/depth/ambiguity ceilings and manifest byte count was ignored | Publish the exact database-backup JSON profile, enforce stable bounded reads and closed manifest/backup validation before restore allocation, publish schemas, state that no archive is accepted, and retain unkeyed/encryption/authorization/DR limitations | corrected under ADR 0078; backup restore JSON is bounded, while adjacent DB import JSON and operational security/recovery controls remain open |
| DOC-029 | Legacy database import safety wording | The migration guide said imports create references “where safe” while account/control JSON used unrestricted parsing, permitted alias ambiguity, and silently converted some invalid objects to zero imported records | Publish the exact importer profile and accepted shapes, fail visibly before database inspection, record parsed-byte provenance, and retain semantic/authentication/authorization/malware/DR limitations | corrected under ADR 0079; both FI-009 parser entrypoints are bounded, while broader operational and semantic controls remain open |
| DOC-030 | Platform business-record safety and provenance wording | FI-008 listed only four entrypoints although matching and six services consumed the shared reader; JSON silently dropped non-object records and built fractional values as floats, CSV had no uniform budgets, and four services rehashed mutable paths after parsing | Inventory every affected module, publish exact CSV/JSON ceilings and compatibility shapes, preserve fractional lexemes, reject ambiguity, and bind audit/outbox evidence to the parsed bytes | corrected under ADR 0080; FI-008 parser coverage is bounded, while business semantics, authentication, centralized authorization, malware/classification controls, uploads, connectors, and supported scale remain open |
| DOC-031 | Studio generated-artifact safety and companion wording | FI-007 named one shared helper but `create_studio_app` still contained three direct pandas readers; CSV/JSON had no common budgets or change detection, direct routes could expose parser failures, and the phrase “manifest-aware” could imply cryptographic binding absent from current producer formats | Centralize all FI-007 parser calls, preserve display typing and CSV-only compatibility, publish exact budgets and safe empty behavior, check companion shape/count when present, and explicitly withhold authentication/binding claims | corrected under ADR 0081; FI-007 parser coverage is bounded, while companion binding, source authentication, disclosure/classification, malware controls, remaining report readers, and supported scale remain open |
| DOC-032 | FI-006 generated-report bounded status and numeric representation | FI-006 was marked bounded although variance and management paths still called pandas/JSON directly, and the existing shared CSV helper inferred display numbers unsuitable for financial policy inputs | Route every FI-006 parser entrypoint through the stable bounded helper, add an explicit exact-text CSV mode, preserve optional-artifact fail-closed behavior, and retain authentication/malware/scale limits | corrected under ADR 0082; FI-006 parser coverage is bounded with exact variance lexemes, while authenticated producer/schema binding, disclosure/classification, malware controls, legacy XLS, uploads/connectors, and supported scale remain open |
| DOC-033 | FI-005 bounded status, repeated parsing, and evidence fingerprint wording | Evidence/review CSV used direct pandas reads, binder re-read match/rule files per case, and v3 fingerprints were calculated independently after parsing, allowing resource amplification and a parsed-byte/provenance race | Centralize explicit strict/legacy representations, reject hostile present files safely, cache each selected CSV once, freeze the selected set, and derive/finally verify v3 CSV fingerprints from the parsed documents | corrected under ADR 0083; FI-005 CSV coverage is bounded and parsed-byte-linked, while adjacent review-state JSON, authenticated producer/schema binding, disclosure/classification, malware controls, legacy XLS, uploads/connectors, and supported scale remain open |
| DOC-034 | Review-state malformed fallback and ingestion coverage | Public behavior described malformed persisted review JSON as safely equivalent to an absent file, hiding invalid workflow evidence; the adjacent JSON boundary was not inventoried | Inventory FI-014, retain empty only for a missing file, apply a named bounded duplicate-safe stable-byte profile to present JSON, preserve valid legacy entry coercion, and surface path-free failures without overwrite | corrected under ADR 0084; valid historical coercion remains, while producer/schema authentication, actor authorization, disclosure/classification, malware controls, legacy XLS, uploads/connectors, and supported scale remain open |
| DOC-035 | Direct JSON parser closure and numeric compatibility | File-ingestion governance claimed feature coverage but enforced only tabular/PyYAML direct calls; six filesystem `json.load(s)` calls and 12 persisted decoders were not in an exact parser-call inventory | Add schema/AST closure, migrate all direct filesystem calls, classify persisted/package-text exclusions, and record display versus exact-text JSON number semantics | corrected under ADR 0085; current direct calls are closed and file readers bounded, while semantic/authenticated provenance, actor authorization, malware controls, persisted-value contracts, legacy XLS, uploads/connectors, and supported scale remain open |
| DOC-036 | FI-013 idempotency replay wording | Treating all persisted JSON as excluded hid that AP/AR replay state controls whether a financial mutation is repeated, while both producer and consumer lacked size, ambiguity, and numeric-representation contracts | Mark FI-013 partial, publish the shared producer/consumer schema and budgets, reject binary/fractional numbers, and prove replay rejection plus producer rollback without implying coverage of the remaining stored fields | corrected for AP/AR under ADR 0086; after the audit-metadata slice, seven direct FI-013 decoders and producer authenticity remain open |
| DOC-037 | Audit metadata sentinel and hash wording | SQLite/PostgreSQL list and verification paths silently wrapped malformed/non-object metadata, while PostgreSQL JSONB display text differs from the canonical text used by its historical hashes | Publish one bounded audit object contract, make invalid metadata an independent integrity failure, fail list/export closed, and preserve each backend's established hash bytes | corrected under ADR 0087; seven generic export/outbox/reconciliation/Redis/matching/worker FI-013 decoders and authenticity remain open |
| DOC-038 | PostgreSQL audit/outbox producer coverage | E-072 described all PostgreSQL audit producers as bounded although three repository families retained local encoders; outbox delivery also wrapped non-object payloads and had fragmented unbounded writers | Route all audit/outbox writer families through named contracts, enforce call-site inventory, and reject corrupt claims before publisher handoff without changing event identity or lease state | corrected under ADR 0088; six generic export/reconciliation/Redis/matching/worker FI-013 decoders, authenticity, and live service evidence remain open |
| DOC-039 | PostgreSQL reconciliation JSONB safety and fingerprint wording | Rules, attributes, lineage, and evidence were described as canonical JSONB although three readers remained direct/unbounded, three producer families had no resource ceiling, and raw JSONB text could diverge from compact fingerprint bytes | Publish four bounded profiles over one schema, preserve the existing attributes ceiling, canonicalize JSONB text before fingerprinting, and fail corrupt rows before matcher/public use | corrected under ADR 0089; three generic export/Redis/matching FI-013 decoders, authenticity, and live service evidence remain open |
| DOC-040 | SQLite matching-rule safety and compatibility wording | Matching rules selected persisted financial/identity policies but were written and replayed through unrestricted JSON, while changing their established whitespace or rejecting historical finite floats would alter valid stored/API behavior | Publish one bounded structural profile, encode once before the transaction into both tables, retain exact spaced text and legacy replay defaults, and fail corrupt rows before replay or public return | corrected under ADR 0090; two generic export/Redis FI-013 decoders and stored-row authenticity remain open |
| DOC-041 | Redis session safety and deployment wording | The optional store was described as bounded coordination although its credential-adjacent session value used unrestricted JSON, accepted implicit expiry text, and had no closed field/size contract; tests covered tenant revocation but not live session TTL | Publish a narrow four-field profile, require digest plus explicit UTC, preserve key/TTL behavior, extend the existing live test, and distinguish a bounded primitive from production login integration | corrected under ADR 0091; generic export was the sole direct FI-013 decoder at that boundary and was closed by ADR 0092; live Redis/login integration remains open |
| DOC-042 | Public database export sentinel and field-shape wording | The generic exporter silently replaced malformed summary/rule JSON with a plausible sentinel, never validated matching lineage, and created the destination before proving all stored rows valid; decoding lineage would also be an unversioned format-v1 change | Register every reachable `_json` field, apply field-specific budgets and producer contracts, preflight all payloads before destination creation, remove sentinels, and retain `lineage_json` as its historical validated string | corrected under ADR 0092; all current exported JSON fields are bounded while row authenticity, disclosure authorization, crash-atomic multi-file publication, malware controls, and supported throughput remain open |
| DOC-043 | Public database export atomicity wording | Preflight validation was described as pre-publication safety although nine files were still written sequentially into an existing directory, allowing partial mixtures and stale files after failure | Distinguish payload preflight from filesystem publication; stage and manifest the closed set, restore handled failures, provide explicit verified recovery, and state observer/pre-marker/cross-resource limits | corrected under ADR 0093; handled local replacement is recoverable while abrupt host loss, remote filesystems, authentication, authorization, malware, and throughput remain open |
| DOC-044 | Deny-by-default and self-approval wording | The central policy engine granted identity without a named permission, scope fields were inert, and generic override text plus unresolved trusted-local actors allowed self-approval despite broad SoD wording | Require an explicit permission, enforce every supplied scope, default ownership checks on, prohibit ordinary override authority, and inventory every current approve/review service surface | corrected under ADR 0094 for the P0 property contract; complete API/repository ABAC, amount/region/data-class policy, emergency access, and deployed effectiveness remain open |
| DOC-045 | Direct Python financial-input defaults | Current documentation said CLI paths were strict while direct Python defaults remained warning legacy readers; after the approved breaking boundary that wording would encourage approximate financial input | Make all policy defaults strict, retain legacy only explicitly for historical replay, publish a migration guide/changelog entry, and preserve historical ADR/evidence wording as dated decisions | corrected under ADR 0097; supported-version/backend execution remains separate and explicit historical readers are not removed |

## Claim boundary

DOC-006 and DOC-007 remain open, and the worktree is not an isolated release candidate. Public surfaces may say only that ReconForge is alpha-stage, local/file-first, open source, and intended for evaluation or controlled pilots. They must not state general cross-engine determinism, scale, enterprise readiness, bank-grade quality, compliance, or certification.
